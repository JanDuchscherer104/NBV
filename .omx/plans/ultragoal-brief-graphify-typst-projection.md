# Ultragoal brief: Graphify shared Typst projection

Implement the approved RALPLAN handoff at
`.omx/plans/ralplan-handoff-graphify-typst-projection.md` without modifying the
upstream Graphify skill or committing generated artifacts.

### Stories

1. **Implement deterministic semantic projection**
   Extend the existing glossary read model and Graphify Markdown projection with
   glossary-term, symbol, equation, and active-section usage contracts, plus
   hermetic regression tests from the approved test specification.
2. **Verify ingestion and document evidence**
   Run focused repository checks, an isolated bounded upstream Graphify smoke
   through native Codex agents without provider backends, and record the required
   ARIA debrief.
3. **Pass final quality gates and publish**
   Run cleanup, repeated verification, architecture invariant audit, independent
   code-reviewer and architect gates, then commit, push, and open the authorized
   draft PR to main.
