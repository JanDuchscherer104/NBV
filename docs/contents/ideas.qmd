---
title: "Ideas and Future Directions Archive"
id: ideas
updated: 2026-04-11
phase: archive
audience: public
status: scratch
owner: jan
agent_editing: read-only
format: html
scope: repo
tags: [read-only, project-ideas, future-directions, multi-step rri based rl]
---

::: {.callout-note}
This page is retained as renderable scratch history. Current thesis decisions
belong in [roadmap](../typst/thesis/development/roadmap.typ), active research questions belong in
[questions](../typst/thesis/sections/01-research-questions.typ), and actionable work belongs in the
`.agents/*.toml` backlog.
:::

# Ideas and Future Directions

## Highest Priority (to resolve in next meeting)

- Decide: can we stick with the current eco-system and dataset?
- Decide: how much time to invest into an updated VIN? How much ablation and architecture search for single step vs going directly to multistep?
- apply for Aria Gen2 research kit and simulator that was used for generating the ASE dataset.
- clean up data handling.
- move to university workstation.
- figure out how to do multi-step RL.

- update quarto docs (based on current state of the source code, main.typ and slides_4.typ)

## Problems in current implementation

- when we're intreresteed in finer details we must not downsample the pointcloud or mesh! We were downsampling the mesh to 10% of the original faces.
- we're overfitting! tbh I don't know what I meant with this. I think I might have referred to the calibration plot!?

- perspective / philosophy: what would be helpful to me as a human trying do next best view prediction?
- why do we choose azimuth and elevation bounds? wouldn't it be leading to to better generalization if no bounds have higher variances.

    > Allowing full azimuth ranges would lead to more diverse candidate views, but given the strong directivity in the 20 views per historic trajectory, the agent might prioritize making 180 degree turns as most rewarding action.
    > Allowing full elevation ranges would lead to unrealistic candidate views.

---

request acees to ASE simulator to do RL based data collection
- question: why not use mutliple losses (i.e. entropy, various ordinal classification losses) together?

val confusion matrix indicates that it never predicts the lowest two classes!

---

- First predict relative movement, encode new relative pose, then predict look at (shell).
- or idea as described in .agents/tmp/image.png or Hestia paper.
- supervise an intermediate target / look-at proposal head (entity center, uncertainty peak, expected-RRI centroid), then predict the continuous camera translation conditioned on that target.
- for continuous policies: project the predicted pose to the nearest feasible / collision-free pose instead of relying on penalties only.
- How can we encode trespasses to new areas that will yield really high rri?
- Explicit 3D gaussian state representations instead of voxel fields?

---

## How to move to multi-step, non-myopic RL; how to scale up?

**Problems**:

- We only have the full set of modalities for the offline ego-trajectories.
- Trajectory: (i.e. >= 2 minutes per scene), single snippet / training sample is two seconds long and consists of 20 frames with temporal stride of 10 frames until the next snippet starts.

- So far we only tried to predict the RRI for a single next best view, anchored at the last frame of the snippet / ego-trajectory. If we wanted to do multi-step prediction, we would not have modalities like:
    > **RGB**: We could use something like Gaussian splatting or world models to generate the full modalities for counterfactual poses that are not part of the original ego-trajectory. Weren't there one-shot reconstruction methods in the aria ecosystem: [egocentric_splats](https://github.com/facebookresearch/egocentric_splats). We only have 100 scenes with GT meshes, we could easily train 3D GS on those and the offline trajectories and then resue them to train our RRI prediction model.

    > **SLAM PC**: We could emulate their slam algorithm or we simply generate own slam pc for ASE.

- Once we have a solid mutli-step RRI scorer we we can use it as reward function for all the scenes without GT meshes.

**Questions**:

- How can we formulate a multi-step RL task here?
- How can we use our RRI prediction model to implement a ciritic for actor-critic RL - i.e. expected cumulative RRI?
- Can we simply use all available modalities for the ego-trajectory, and compute the embeding of the intermediate counterfactual views based solely on the available modalities - i.e. the GTMesh? SDF, surface normals, (directional, counted) visibility, camera poses, <TODO: what else can we compute for the counterfactual views based on the GTMesh?)>
- How does the TD target look like for multi-step prediction based on the RRI (reward) prediction model?
- What RL approaches / methods exist and are suitable for our task where we don't have the full set of modalities for the counterfactual views?
- How could the MDP look like for our task?
- Can the Critic network use GT information as input during training? I.e. GT OBBs, GT Segmentation Masks
- Generate synthetic offline multi-step trajectories (with same transition dymanics as currently present for single step)
- Should the first RL variant be close-greedy / low-discount receding-horizon control rather than full long-horizon RL?
- Can we factor planning into semantic-global planning + local geometric execution, i.e. VLM/VLA chooses grounded subgoals like `cross_portal`, `inspect_entity`, or `approach_frontier`, and the NBV controller executes them?
- What compact world-memory / descriptor schema do we need for portals, frontiers, entities, and regions so the semantic planner reasons over grounded IDs instead of free-form text?
- Do we need a verifier / replanner after each executed subgoal to reduce hallucinated drift?

### Object Centric Multi-Step Oracle-RRI Planning

future iterations of the multi-step oracle rri pipeline will be "object" centric - i.e. the rri will be computed only for subsets of the pointcloud / mesh that are part of an object of interest. that means that we will have to include a policy to select suited objects for each of the ATEK snippets, adapt the rri computation such that only backprojected points that belong to the object of interest are considered for the objective.
we will need to define the branching and selection policy more rigourously (i.e. we might want to avoid a gemoetric groth in the number of branches, such that after a horizon of N steps, we have only K counterfactual trajectories all of which are distinct chains rather than generating a tree with ever increasing number of leave nodes...).
here we also need to consider how to generate the multi-step samples in a way such that batching stays efficient - i.e. all multi-step samples must be easily paddable to the same horizon and number of branches.

## Scaling up

- Our current oracle RRI pipeline relies on the GTMesh, these are only available for 100 of the 100,000 scenes (4608 snippets, 19.2% were used for training of VinV3)
- We can generate much more than 60 candidate poses per snippet and generate them within broader distribution of candidate view generation parameters.
- So far we haven't really used the GT Obbs, GT depth or GT semantic segmentation masks, these (especially the latter two) could be used to generate the missing modalities for the counterfactual views (i.e. Gaussian splatting, one-shot GS pre-trained model ?).
- Choose different poses in the ego-trajectories as anchor points (instead of always using the last frame ?)


## Modifications to the VIN (RRI prediction) model

### Ablations

- surface reconstruction is an incredibly powerful input modlity - however we should ablate that!
- ablate our modification of CORAL Loss and coral layer.
- ablate the auxiliary huber loss.

### Feature Encodings and Architecture

- we should increase the number of points in the semi dense pointcloud as well as the semidense grid.
- also use pretrained resnet here!
- How can we implement vin v4 as pure transformer? Let projections (with *all* information that correspond to a certain frustum or unit of space attend to each other)?
  - (scene tokens: voxels / splats / entities, candidate query tokens, projection / frustum tokens, history tokens, entity tokens)
- Maybe early fusion into tokens than one QA branch?
- Tiny **pre-trained** cnn for projection features (i.e. small resnet)? Maybe pretrain on OBB detection? Would be optimal if the model was pre-trained on depth images.
- Let different modalities from the same sample modulate its cosignals.
- Look at network from video analysis - query-centric-attention - i.e. [QCNet](https://openaccess.thecvf.com/content/CVPR2023/papers/Zhou_Query-Centric_Trajectory_Prediction_CVPR_2023_paper.pdf) and [LMFormer](https://arxiv.org/abs/2504.10275)
- do iterative layer refinement - residual learning similar to LMFormer.
- For each point we can encode from which direction they have been observed - similarly to how [Hestia](https://arxiv.org/abs/2508.01014) did it. Here we can average over the different observations or we encode them as incidence-angle histogram.
- add target-conditioned local feature reads from EVL / voxel state at predicted target points, entity centers, or surface-deficit peaks.


### Objective and Optimization

- How can we combine the imitation learning signal (i.e. computing the RRI for the given candidate views based on the snippet encodings and supervision with oracle rri) with a continous Action policy that aims to predict the best next view based on the same encodings (without encodings of the candidate poses)?
- why not use a higher value for aux huber loss? Does the aux loss even help? Does it only play a role for initial convergence to the mean? -> ablate
- we can do semantic segmentation and bbox prediction as auxiliary tasks.
- perspective: presence of bboxes might indicate presence of high complexity regions.
- When using a continous action policy, predict relative translation before or after the relative rotation and condition the later on the former?
- Transition to "object-centric" views where the RRI of certain objects (with existing GT and pred OBBs) needs to be maximized could offer a greater variety of different counterfactual views (trajectories).

## Concise pointers from recent reports

### Hestia / hierarchical planning

- treat Hestia as a planning-and-control pattern, not as a replacement for the RRI objective.
- add directional observability bookkeeping: six-face bins, incidence-angle histograms, normal-aligned bins, or learned directional descriptors for points / voxels / entity tokens.
- factor control into target selection (`look-at` point, entity center, uncertainty peak, expected-RRI centroid) and view realization.
- supervise a target proposal head from missing-surface / expected-RRI / uncertainty geometry, then predict continuous relative translation conditioned on that target.
- add target-conditioned local feature reads / multiscale interpolation at predicted target points, entity centers, or surface-deficit peaks.
- use feasibility projection and possibly an auxiliary feasibility loss so predicted poses are snapped to collision-free states instead of relying only on penalties.
- start continuous RL with close-greedy, low-discount, receding-horizon control before attempting full long-horizon RL.
- scale more through training diversity: broader scene coverage, more anchor poses, wider candidate distributions, positional shifts, and entity-centric supervision.

### VLM / VLA semantic-global planning

- use VLM / VLA for semantic-global planning only; keep local metric motion, NBV scoring, and collision handling in the geometric controller.
- model the high-level planner as a semantic POMDP over grounded portals, frontiers, entities, and regions.
- restrict planner outputs to grounded subgoals such as `CROSS_PORTAL(portal_id)`, `INSPECT_ENTITY(entity_id)`, `APPROACH_FRONTIER(frontier_id)`, `COMPLETE_ENTITY(entity_id)`, and `COMPLETE_REGION(region_id)`.
- build a compact structured scene memory from recent RGB views, topological graph, entity memory, uncertainty / quality map, directional coverage summaries, and the task / mission query.
- define compact descriptor tokens for portals, frontiers, entities, regions, and local geometry summaries; use retrieval over the top-k candidates instead of dumping the full scene.
- portal selection is a strong first use case because portals are both topological bottlenecks and semantic transitions.
- reduce hallucination risk with grounded IDs, a restricted action vocabulary, verifier / replanner loops after each executed chunk, and never letting the VLM emit raw continuous poses.

<!-- THE subsequent notes are the most up-to-date. -->

## Master's Thesis

- Do entity-aware multi-step NBV planning.
- Compute RRI based on the subset of the pointcloud that belongs to a certain entity by only considering geometry inside the entity's bounding box. Here the GT OBBs can be used.
- The object of interest can be declared to the model by encoding the OBBs of the objects of interest (as predicted by EFM) into the state representation and conditioning the VIN on these.rule
- Currently candidate views are generated by sampling from a shell around the reference pose. For multi-step object centric NBV we could use the `look-at` object of interest as base direction and perturbing the final direction with some jitter.
  - Also: how to generate views that are not the first counter factual candidate view? I.e. we should generate a trajectory of views that is somewhat natural and feasible!
- Furthermore we can generate a subset of views based on the "look-at" object and others using the current default "look-away" to ensure a good variety of candidate views.
- Currently invalid views are being dropped during oracle rri generation. We should switch to a penalty-based system.
  - How can this be integrated with coral? Can we simply add another class-head for invalid poses?
  - Should this be a normal CORAL neuron (i.e. the lowest class)
- Selection of the candidate views to roll out?
  - Gumbel-Top-k / Stochastic Beam Search w.r.t. the oracle RRIs?


### Open Questions
- How do we determine suited objects for our current offline ATEK snippets?
  - min visibility of object in the reference view?
  - max distance from the reference pose?


---

## Issues

- First RRI computation compares the reconstruction improvement of the first counterfactual view's *dense* pointcloud against the sparse SLAM pointcloud, whilst the subsequent RRIs compare the dense single step pointclouds against the fused PC which now includes the union of the semi-dense and previous dense pointclouds. This leads to a bias in the RRI computation, favoring the first candidate views.
- We need to investigate the current target selection policy so that we get a well balanced distribution of targets for the multi-step rollouts.
- we actually shouldn't create a dedicated single step rollout dataset. just allow adding new steps to the same rollout roots!
  - add simplified visualization of the rollout trees per snippet in streamlit app. there also plot important summary statistics as currently found in the online counterfactual rollout page to monitor the distributions of the generated multi-step samples (i.e. RRI values, multi step returns, relative candidate pose changes (translation and rotation), number of steps and branches per rollout, ...)
