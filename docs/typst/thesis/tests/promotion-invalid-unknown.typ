#import "../draft_markers.typ": promotion_entry

#promotion_entry(
  [Unknown disposition must fail],
  source: [fixture.typ:1],
  target-section: [04-method],
  gate: [test gate],
  disposition: "promoted",
)
