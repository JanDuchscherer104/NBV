#import "../../shared/symbols.typ": symb
#import "../draft_markers.typ": thesis_status, scientific_core_todo, prune_todo

= Discussion <sec:thesis-discussion>

#prune_todo(
  [Rewrite the discussion after confirmatory results exist. The present implementation-readiness narrative and architecture bridge registry are development material; the final chapter must interpret measured answers, alternative explanations, scope, limitations, and failure modes.],
  source: [@sec:thesis-results and the frozen analysis plan],
  gate: [every interpretation points to a reported result or is explicitly bounded as a limitation],
)

The current evidence supports a narrow implementation conclusion. ARIA-NBV has an executable finite-candidate path that keeps oracle gains, meshes, associations, and candidate renders outside the scorer graph, applies invalidity as a hard decision constraint, evaluates target-specific reconstruction change offline, and exposes the resulting store through one typed reporting seam. The selected experiment still conditions on privileged `v0_gt_input` target geometry; `v1_observed` admission, persistence, and actor-only descriptor construction are implemented, but no frozen observed-target-protocol corpus or evaluation exists. Actor-only target construction alone does not establish deployability, which additionally requires causal actor-visible state and actor-observed action support. Nor does the current evidence establish that one candidate family, rollout policy, representation, or learned model performs better than another.

The rollout attempts also expose a systems limitation. Counterfactual scoring repeatedly renders a large mesh for a candidate set, so renderer memory and latency constrain feasible branch factor and rollout volume before statistical efficiency becomes relevant. An out-of-memory observation is evidence for batching and resource measurement, not for or against the candidate distribution or planning objective. Completed validated stores are required before throughput, storage, or failure rates are generalized.

The scientific limitations are more restrictive. Current attempts use training sources, no held-out paired policy table supports the endpoint estimand, metric repeatability and meaningful oracle-lookahead headroom remain unestablished, and the target-conditioned finite-horizon comparison has not yet been demonstrated. The present evidence therefore cannot distinguish negligible non-myopic structure from inadequate candidate support, target observability, replay coverage, representation support, or model failure.

== Evidence-conditioned architecture bridges

#thesis_status(
  implementation: "exploratory",
  evidence: "pending",
  source: [@tab:thesis-counterfactual-state-protocols; @tab:architecture-feature-integration-ladder],
  gate: [a measured limitation that the proposed bridge directly tests],
)[Alternative representations and policies remain part of the thesis design registry, but they are promoted only as responses to diagnosed bottlenecks.]

If the local EFM3D field is the limiting variable, the first readout-level test
pools target support, candidate-frustum support, and their intersection from the
already persisted local EVL field. This separates missing state information from
spatial correspondence destroyed by root moments without rerunning EFM3D or
introducing a new carrier. Broad semidense memory, sparse occupied/free/unknown
state, target-centred re-lifting, or logged appearance features are progressively
stronger representation changes. If the current query contains the necessary
geometry but A0/A1 still leaves relation-structured residuals after matched
state, support, label, optimization, seed, and capacity controls, F2 becomes the
first eligible architectural promotion. If errors instead remain correlated
with a frozen candidate-context set, F3/F4 become separately versioned
support-conditioned ranking hypotheses rather than ordinary action-value
extensions. If selected-view history fails specifically by approach direction,
signed first- plus second-moment memory, followed only if needed by spherical
harmonics, becomes the targeted ablation. A renderable 3DGS state is appropriate
only when candidate rendering or soft instance membership is itself the measured
missing capability.

Continuous and hierarchical policies change the action contract rather than merely increasing model capacity. A target-then-pose policy could first select or mask a target token and then condition pose queries on target, horizon, and step. Multiple targets may share one scene encoding, but their descriptors, candidate supports, masks, and endpoint aggregation remain target-specific: the method requires either one candidate table per target or a union table with an explicit target-by-candidate availability mask. Parallel scoring must not imply that every candidate exists for every target. This remains post-core work because no multi-target policy evidence exists and continuous pose generation requires separate feasibility, safety, support, fairness, and simulator-realism evidence. The finite-candidate model is retained as the calibrated control even if a continuous policy is later introduced.

Sequence or recurrent models are similarly conditional. They become scientifically motivated when errors persist after explicit history, time, horizon, and scene-memory conditioning, not simply because the dataset contains trajectories. Looping or recurrent refinement must preserve causal masks and candidate-row equivariance and must be compared against the same fixed-budget endpoint oracle evaluation.

#scientific_core_todo(
  domain: "architecture",
  priority: "C2",
  readiness: "contingent",
  claim: [which representation or interaction limitation explains residual held-out value error],
  source: [method design-space registry],
  gate: [artifact-backed failure analysis],
)[After the primary evidence chain is complete, promote only bridges tied to a measured failure: EVL support, target observability, directional history, valid-set interaction, long-horizon credit, or action-support mismatch.]

The next evidential step remains procedural: complete validated stores under resolved manifests, freeze the eligible population and exclusions, establish oracle stability and headroom, train the matched controls, and only then interpret architectural differences. The registry above preserves concrete follow-up hypotheses without presenting them as validated conclusions.
