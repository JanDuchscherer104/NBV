#import "../../../shared/macros.typ": *

== Related Work

Classical active perception establishes that sensing actions and inference must be designed jointly, while three-dimensional view planning supplies the finite generate--score--select abstraction used here @ActivePerception-bajcsy1988 @ActiveVision-aloimonos1988 @ViewPlanningSurvey-scott2003. Receding-horizon and submodular formulations explain why lookahead and greedy comparators can be informative under specific utility assumptions @RecedingHorizonNBV-bircher2016 @SubmodularNBV-lauri2020. They do not establish such guarantees for target-specific RRI, whose value depends on the evaluated target, current reconstruction support, and admissible candidates. This thesis therefore tests non-myopic headroom empirically under fixed support rather than assuming it from generic coverage objectives.

Quality-driven NBV replaces geometric coverage proxies with reconstruction-quality supervision. VIN-NBV is the closest precedent because it ranks sampled candidates using oracle RRI rather than visibility alone, while PB-NBV contributes efficient finite projection-based proposals @VIN-NBV-frahm2025 @PB-NBV-jia2025. Neither establishes target-cropped finite-horizon value under an Aria-native actor/oracle separation. ARIA-NBV consequently uses VIN-style one-step scoring as a myopic control and asks separately whether bounded lookahead improves endpoint target quality.

Target- and object-aware NBV shows why scene-wide utility can conceal the reconstruction needs of one entity. Object-centric NBV motivates conditioning view utility on a designated object, while OANBV and ObjViewBench contribute visibility, feasibility, occlusion, and target-difficulty analyses @ObjectCentricNBV-jeong2026 @OANBV-hu2026 @ObjViewBench-pan2026. The current ARIA-NBV data generator does not solve actor-visible target discovery: it samples geometry-valid GT OBB rows as target tasks and evaluates their target crops. Observed or predicted target records are therefore a separate requirement for learned-policy claims, not an implemented consequence of target-specific supervision.

Project Aria defines the calibrated multimodal egocentric sensing regime, ASE supplies synthetic trajectories and GT geometry, and EFM3D/EVL supplies local actor-visible spatial evidence and object hypotheses @projectaria-engel2023 @ProjectAria-ASE-2025 @EFM3D-straub2024. This stack enables controlled counterfactual labels but does not itself define an NBV objective or make oracle information actor-visible. Meshes, GT boxes, and all-candidate renders remain supervision and evaluation assets; candidate selection must ultimately use logged observations, accumulated selected-view evidence, and an observed or predicted target record.

Offline value learning and set models provide controls for the finite-action problem. Double DQN separates successor-action selection from evaluation, while CQL and BCQ illustrate the risk of unsupported actions in offline learning @DoubleDQN-vanHasselt2015 @CQL-kumar2020 @BCQ-fujimoto2019. Deep Sets and Set Transformer provide permutation-aware baselines for an unordered candidate table @DeepSets-zaheer2017 @SetTransformer-lee2019. These methods do not replace the domain contract: invalid actions remain hard-masked, candidate support is reported, and every selected trajectory is re-evaluated by endpoint target quality rather than accepted on predicted value alone.
