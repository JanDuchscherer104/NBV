= Conclusion <sec:thesis-conclusion>

This thesis formulates target-specific egocentric next-best-view planning as a
relational finite-action problem: value depends jointly on target, causal
information state, admissible candidate support, horizon, update rule, and
continuation policy. It contributes a target-cropped reconstruction outcome, an
explicit actor--oracle information boundary, a strict separation of feasibility
from utility, causal selected-transition replay, a scalar-horizon fitted-value
method, and a claim-dependency evaluation from shared foundations through
parallel oracle-headroom and learned-value paths to endpoint recovery.

The current evidence does not yet answer RQ2. Metric repeatability, the held-out
target and action population, and actor-visible immediate-value recovery remain
unestablished; without those premises, neither oracle-lookahead headroom nor
learned-versus-exact $Q_2$ agreement supports a confirmatory claim, and endpoint
recovery is blocked. Online interaction and continuous control remain extensions
of the setting rather than missing parts of this evaluation.

Training-source rollouts establish that the pipeline reaches mesh rendering,
target-specific oracle scoring, selected-action replay, and the fitted-value
interface; renderer memory limits the evaluated scale. These observations
justify an auditable method and study design, not policy superiority, a
population effect, deployment readiness, or a scale estimate.

The eventual conclusion on each claim path is determined by its earliest
non-passing prerequisite. Unstable measurement or inadequate support blocks
both inferential lanes. Negligible headroom is a setup-specific negative result
that blocks the recovered-fraction claim but does not erase independent learned-
value diagnostics. A non-pass in actor-visible $Q_1$ or learned-versus-exact
$Q_2$ agreement localizes a learning prerequisite; passed headroom and learned
value without endpoint recovery is a bounded learned-policy failure. This graph
preserves informative negative outcomes without extending them beyond the
frozen finite-candidate experiment.
