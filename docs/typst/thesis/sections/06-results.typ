#import "../../shared/macros.typ": *
#import "../../shared/symbols.typ": symb
#import "../draft_markers.typ": *

= Results <sec:thesis-results>

This chapter is reserved for generated evidence from the final rollout manifests, scorer runs, value-model ablations, and paired oracle re-evaluation. Until those artifacts are frozen, planned scale bars, historical seminar subsets, and exploratory diagnostics remain in the experimental-design chapter or appendices as evidence gates rather than final results.

#validation_todo(
  [Replace this scaffold with manifest-backed result tables: target eligibility and match coverage, candidate validity and invalid-reason distributions, one-step scorer ranking/calibration, measured oracle-lookahead headroom, #symb.rl.qh recovery, endpoint target gain, runtime, storage, and failure strata.],
  source: [thesis migration plan],
  gate: [final experiment manifest and result-table generation],
)
