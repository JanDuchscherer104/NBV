[project]
name            = "aria-nbv"
version         = "0.1.0"
description     = "Oracle Relative Reconstruction Improvement for Next-Best-View Planning in the Aria Synthetic Environments Dataset"
readme          = "README.md"
authors         = [{ name = "Jan Duchscherer", email = "duchsche@hm.edu" }]
requires-python = ">=3.11"

dependencies = [
    # PyTorch ecosystem – installed from CUDA 12.1 wheels
    "torch==2.4.1",
    "torchvision==0.19.1",
    "torchaudio==2.4.1",
    "pytorch3d @ git+https://github.com/facebookresearch/pytorch3d.git@main",
    # Scientific computing
    "numpy>=1.26.4",
    "scipy",
    # Core EFM3D / Aria ecosystem
    "omegaconf==2.3.0",
    "hydra-core==1.3.2",
    "webdataset==0.2.86",
    "vrs==1.2.1",
    "fsspec==2024.6.0",
    "einops==0.8.0",
    "pandas==2.2.2",
    "pydantic>=2.0.0",
    "pyquaternion==0.9.9",
    "pytransform3d==3.14.3",
    "opencv-python==4.10.0.84",
    "tqdm==4.66.4",
    "matplotlib==3.9.0",
    "seaborn>=0.13.2",
    "moderngl==5.8.2",
    "trimesh==4.12.2",
    "scikit-image==0.24.0",
    "tensorboard==2.14.0",
    "torchmetrics==0.10.1",
    # Project Aria ecosystem
    "projectaria-tools==2.1.2",
    "projectaria-atek==1.0.0",
    # Our additional deps
    "efm3d",
    "plotly>=6.4.0",
    "open3d>=0.19.0",
    "rerun-sdk==0.26.2",
    "rich>=13.9.4",
    "devtools>=0.12.2",
    "pydantic-settings>=2.12.0",
    "tqdm-stubs>=0.2.1",
    "types-requests>=2.32.4.20250913",
    "streamlit>=1.52.2",
    "tomli-w>=1.0.0",
    "tomlkit>=0.13.3",
    "fast-simplification>=0.1.13",
    "rtree>=1.4.1",
    "power-spherical>=0.8.1",
    # Training stack (VIN head)
    "pytorch-lightning==2.6.0",
    "wandb>=0.17.0",
    # VIN pose encoding + ordinal loss
    "e3nn==0.5.9",
    "coral-pytorch==1.4.0",
    "torch-summary>=1.4.5",
    "optuna>=4.6.0",
    "optuna-integration[pytorch-lightning]>=4.5.0",
    # OpenPoints support is optional because the shim builds PointNeXt CUDA
    # extensions and should not participate in CPU-only package smoke tests.
    "pyyaml>=6.0; sys_platform == 'linux'",
    "multimethod>=1.7; sys_platform == 'linux'",
    "termcolor>=1.1.0; sys_platform == 'linux'",
    "easydict>=1.9; sys_platform == 'linux'",
    "shortuuid>=1.0.11; sys_platform == 'linux'",
    "msgspec>=0.20.0",
    "zarr>=3.1.5",
    "setuptools<81",
    "typer>=0.25.1",
]

    [project.scripts]
    nbv-downloader            = "aria_nbv.data_handling.atek_downloads.downloader:main"
    nbv-summary               = "aria_nbv.lightning.cli:summarize_main"
    nbv-st                    = "aria_nbv.streamlit_app:streamlit_entry"
    nbv-fit-binner            = "aria_nbv.lightning.cli:fit_binner_main"
    nbv-build-offline         = "aria_nbv.oracle.pipelines.cli:offline_main"
    nbv-offline-info          = "aria_nbv.data_handling.vin_store.info_cli:main"
    nbv-plan-rollout-shards   = "aria_nbv.oracle.pipelines.cli:plan_main"
    nbv-plan-rollout-source   = "aria_nbv.oracle.pipelines.cli:source_manifest_main"
    nbv-build-rollouts        = "aria_nbv.oracle.pipelines.cli:main"
    nbv-rollout-campaign      = "aria_nbv.oracle.pipelines.cli:campaign_main"
    nbv-status-rollout-shards = "aria_nbv.oracle.pipelines.cli:status_main"
    nbv-rollouts-info         = "aria_nbv.rollouts.info_cli:main"
    nbv-rerun-inspect         = "aria_nbv.rerun_inspector._cli:main"
    nbv-train                 = "aria_nbv.lightning.cli:train_main"
    nbv-optuna                = "aria_nbv.lightning.cli:optuna_main"
    nbv-wandb                 = "aria_nbv.lightning.cli:wandb_main"
    nbv-cli                   = "aria_nbv.lightning.cli:main"


    [project.optional-dependencies]
    dev = [
        "pytest>=8.4.2",
        "pytest-cov>=7.0.0",
        "ruff>=0.1.0",
        "mypy>=1.0.0",
        "syrenka>=0.5.7",
        "debugpy>=1.8.17",
    ]

    pointnext = ["openpoints-shim; sys_platform == 'linux'"]

    notebook = [
        "ipympl>=0.9.0",
        "jupyter>=1.0.0",
        "ipykernel>=7.1.0",
        "notebook>=7.5.0",
        "jupyterlab>=4.5.0",
        # jupyter-cache nbclient nbformat ipykernel jupyter
    ]

    # Optional performance dep used by EFM3D's DinoV2 attention utils.
    # NOTE: Pinned to avoid upgrading our Torch (2.4.1+cu121).
    xformers = ["xformers==0.0.28.post1; sys_platform == 'linux'"]

[build-system]
requires      = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aria_nbv"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.uv]
# Ensure pytorch3d is built with the runtime torch
extra-build-dependencies = { pytorch3d = [
    "torch",
], openpoints-shim = [
    "torch==2.4.1",
    "ninja",
    "pyyaml",
    "multimethod",
    "termcolor",
    "easydict",
    "shortuuid",
] }

    [[tool.uv.index]]
    name     = "pytorch-cu121"
    url      = "https://download.pytorch.org/whl/cu121"
    explicit = true

    [tool.uv.sources]
    torch = [{ index = "pytorch-cu121", marker = "sys_platform == 'linux'" }]
    torchvision = [
        { index = "pytorch-cu121", marker = "sys_platform == 'linux'" },
    ]
    torchaudio = [
        { index = "pytorch-cu121", marker = "sys_platform == 'linux'" },
    ]
    xformers = [{ index = "pytorch-cu121", marker = "sys_platform == 'linux'" }]
    openpoints-shim = { path = "../external/openpoints_shim", editable = false }
    efm3d = { path = "../external/efm3d", editable = true }
    aria_nbv = { path = ".", editable = true }

[tool.mypy]
python_version         = "3.11"
strict                 = true
ignore_missing_imports = true
check_untyped_defs     = true
disallow_untyped_defs  = true

[tool.ruff]
target-version = "py311"
line-length    = 120
indent-width   = 4

    [tool.ruff.lint]
    select = [
        "E",  # pycodestyle errors
        "W",  # pycodestyle warnings
        "F",  # Pyflakes
        "I",  # isort
        "B",  # flake8-bugbear
        "C4", # flake8-comprehensions
        "UP", # pyupgrade (Python 3.11+ features)
        "N",  # pep8-naming
    ]
    ignore = [
        "E501",  # line too long (handled by formatter)
        "UP037",
        "F722",
    ]

    [tool.ruff.format]
    quote-style               = "double"
    indent-style              = "space"
    skip-magic-trailing-comma = false
    line-ending               = "auto"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short --strict-markers"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
]

[tool.coverage.run]
source = ["aria_nbv"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

files = ["tests"]

[dependency-groups]
dev = [
    "pytest-xdist>=3.8.0",
]

    [[tool.mypy.overrides]]
    module                 = ["atek.*", "plotly.graph_objects.*"]
    ignore_missing_imports = true
