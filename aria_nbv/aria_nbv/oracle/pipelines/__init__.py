"""Operational pipelines that generate Oracle-supervised datasets.

This package provides offline/online VIN labeling and rollout-store construction
that compose lower-level
candidate generation, rendering, and RRI owners without moving those contracts
into the dashboard or immutable storage codecs.
"""
