# Inspector Owner Lookup

This file preserves a stable routing path for backlog and historical records;
it does not own inspector or NBV contracts.

- Read `aria_nbv/AGENTS.md` for package-wide geometry and verification rules.
- Read `aria_nbv/aria_nbv/rerun_inspector/` and
  `aria_nbv/tests/rerun_inspector/` for live inspector behavior.
- Follow the nearest package `AGENTS.md`, implementation, and tests for data,
  frames, geometry, candidates, validity, labels, RRI, and storage.
- Use `rerun-python-patterns.md` only for official SDK evidence and the saved
  `.rrd` smoke loop.
