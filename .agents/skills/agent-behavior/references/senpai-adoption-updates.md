# SENPAI Adoption Provenance

This retained path preserves historical debrief references. It is not linked
from the `agent-behavior` routing tree and does not define an update workflow.

Current external grounding is registered as `senpai-performance-loop` and
`karpathy-autoresearch-program` in `.agents/skill-sources.toml`. Local behavior
remains owned by `senpai-performance.md`.
