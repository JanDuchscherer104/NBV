# ARIA-NBV Resolved Items

[[issue]]
id = "issue-001"
title = "Stale agents-db skill and missing helper"
description = "The agents-db skill referenced PRML VSLAM, pointed at docs/Questions.md, and promised make agents-db even though no helper target existed."
type = "bug"
priority = "high"
status = "resolved"
labels = ["scaffold", "agent", "backlog"]
resolved_at = "2026-04-29"
resolution_note = "Replaced PRML wording, added scripts/agents_db.py, wired make agents-db, validated schema, and updated the skill prompt."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-002"
title = "Wrong code-review skill"
description = "The code-review skill references litkg-rs and Rust instead of ARIA-NBV and Python."
type = "bug"
priority = "high"
status = "resolved"
labels = ["scaffold", "agent"]
resolved_at = "2026-04-29"
resolution_note = "Rewrote the copied code-review skill and prompt for ARIA-NBV Python/docs/config review surfaces; remaining PR/issue lifecycle work is tracked by refactor-002."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-008"
title = "Public docs identity and navigation leaks stale project state"
description = "Public docs exposed outdated project naming, old GitHub repo links, top-level TODOs, and generated agent scaffold navigation that should not be first-line public thesis content."
type = "maintenance"
priority = "high"
status = "resolved"
labels = ["docs", "identity", "nav"]
resolved_at = "2026-04-29"
resolution_note = "Bounded public identity/nav cleanup completed; broader docs role mixing remains tracked by issue-003 and refactor-004."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-009"
title = "Dirty worktree classification blocks M0 handoff"
description = "The active worktree contains large unrelated data, app, RL, docs, and agent-memory changes; thesis work needs a classified handoff before larger implementation slices."
type = "maintenance"
priority = "high"
status = "resolved"
labels = ["repo-hygiene", "handoff", "m0"]
resolved_at = "2026-04-29"
resolution_note = "Dirty worktree classification no longer blocks M0: cleanup/runtime, docs, KG tooling, and simplification backlog were reviewed and committed as separate packages on 2026-04-29."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-006"
title = "Dense root AGENTS.md"
description = "The root AGENTS.md is too dense and should eventually become a thin dispatcher into scoped guides, skills, and references."
type = "refactor"
priority = "medium"
status = "resolved"
labels = ["agent", "guidance"]
resolved_at = "2026-04-29"
resolution_note = "Root AGENTS.md is now a thin dispatcher; detailed guidance lives in nested AGENTS files, .agents/skills, and .agents/references."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-014"
title = "offline_only.toml local vin_offline store is partial and version-blocked"
description = "`cd aria_nbv && uv run nbv-summary --config-path offline_only.toml` targets a local `.data/offline_cache/vin_offline` store with manifest, sample index, split arrays, and shards, but the current reader rejects the manifest as version 5 while `OFFLINE_DATASET_VERSION` expects 6."
type = "data-blocker"
priority = "high"
status = "resolved"
labels = ["data", "offline-store", "vin", "rerun", "m0"]
context = ["The current local offline VIN store is useful as a diagnostic smoke asset but is marked interrupted/partial, so tooling must report its exact validation state rather than treating it as complete training data.", "As of 2026-05-02, `uv run nbv-summary --config-path offline_only.toml` resolves the bare config path correctly but fails with `Unsupported VIN offline dataset version 5; expected 6. Rebuild the store with the current VIN offline writer.`", "Rerun and VIN smoke commands should use the store only after corrected config resolution and strict offline-store validation agree on what is present."]
references = ["repo:README.md#offline-data", "repo:.configs/offline_only.toml", "repo:docs/contents/impl/rerun_offline_inspector.qmd", "repo:.agents/memory/state/GOTCHAS.md"]
resolved_at = "2026-05-02"
resolution_note = "The local vin_offline store is no longer version-blocked: manifest is v6, sample index has 48 rows, strict reader opens it, and nbv-summary --config-path offline_only.toml passes. Remaining partial-diagnostic caveats are covered by M1/Rerun/offline-store contracts."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-015"
title = "No first-class Rerun offline dataset inspector"
description = "The project lacks a TOML-configurable Rerun CLI that can render an immutable VIN offline-store sample with semidense points, candidate frusta, RRI/validity metadata, optional mesh/OBBs, and stable recording output."
type = "feature"
priority = "high"
status = "resolved"
labels = ["diagnostics", "offline-store", "geometry", "rerun", "thesis"]
context = ["The Rerun offline inspector note defines the needed visual smoke path for immutable offline samples, but the project still needs a first-class CLI/config workflow that produces stable recordings.", "This is a trust diagnostic for geometry, candidate ordering, validity, and labels before samples feed VIN training or rollout evidence."]
references = ["repo:docs/contents/impl/rerun_offline_inspector.qmd", "repo:README.md#important-cli-commands", "repo:aria_nbv/aria_nbv/rerun_inspector/_cli.py", "url:https://rerun.io/docs"]
resolved_at = "2026-05-04"
resolution_note = "The first-class Rerun offline inspector now exists as a TOML-configurable CLI and documented smoke workflow with passing rerun_inspector tests."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-016"
title = "Rerun geometry logging lacks a frame-safe contract"
description = "Candidate and reference camera visualization needs PoseTW/CameraTW-aware Rerun helpers and regression tests so manual frusta, native Pinhole logging, and display-only CW90 corrections cannot silently produce plausible but wrong geometry."
type = "research-debt"
priority = "high"
status = "resolved"
labels = ["geometry", "camera", "rerun", "tests", "contracts"]
context = ["Pose/camera frames, LUF conventions, PyTorch3D cameras, and display-only CW90 corrections can all produce plausible but wrong visualizations if logged through raw matrices.", "Rerun helpers need typed PoseTW/CameraTW boundaries and tests so visual diagnostics do not silently bless corrupted geometry."]
references = ["repo:.agents/skills/nbv-geometry-contracts/SKILL.md", "repo:docs/contents/impl/rerun_offline_inspector.qmd#frame-safety", "repo:aria_nbv/aria_nbv/rerun_inspector/_frusta.py", "url:https://docs.rs/rerun/latest/rerun/struct.LineStrips3D.html"]
resolved_at = "2026-05-04"
resolution_note = "Rerun geometry now has frame-safe helpers and regression coverage for frusta, root coordinates, top-oracle validity, metric depth camera context, and snippet-frame OBB transforms."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-017"
title = "Offline-store visual primitive inventory is missing"
description = "Diagnostics currently have no single strict inventory that tells a viewer which sample primitives are present, which fields are required, and which optional visual layers should be reported as unavailable before rendering starts."
type = "diagnostics"
priority = "high"
status = "resolved"
labels = ["offline-store", "diagnostics", "data", "contracts"]
context = ["The inspector should fail early when required offline-store fields are absent and report optional layers explicitly instead of guessing from ad hoc sample attributes.", "A visual primitive inventory also gives future rollout/entity diagnostics a reusable contract for what can be rendered and what remains unavailable."]
references = ["repo:docs/contents/impl/rerun_offline_inspector.qmd", "repo:aria_nbv/aria_nbv/data_handling/_offline_visual_inventory.py", "repo:aria_nbv/tests/rerun_inspector/", "repo:.agents/skills/dataset-cache-ops/SKILL.md"]
resolved_at = "2026-05-04"
resolution_note = "Visual primitive inventory is implemented and used as a pre-logging contract for the Rerun offline inspector; optional payloads are surfaced through metadata."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-004"
title = "Missing root CI and pre-commit"
description = "The repository lacks a root-level pre-commit configuration, make ci contract, and comprehensive CI workflow; the visible workflow coverage is Quarto-oriented."
type = "feature"
priority = "high"
status = "resolved"
labels = ["ci", "dev-ergonomics"]
context = ["Root guidance names package tests, context refresh, KG checks, agent-memory checks, and docs renders as verification surfaces, but there is no single root CI/pre-commit contract that composes them.", "The only visible GitHub workflow is Quarto-oriented, so source, agent-memory, and package smoke checks can drift until run manually."]
references = ["repo:AGENTS.md#commands", "repo:AGENTS.md#verification", "repo:Makefile", "repo:.github/workflows/quarto-publish.yml"]
resolved_at = "2026-05-06"
resolution_note = "Root CI, pre-commit config, and make ci now exist as lightweight source/docs/agent-memory gates; package/data-heavy checks remain opt-in."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-005"
title = "Dedicated setup guide missing"
description = "The README now provides a concise project front door, but setup details still need a dedicated portable guide for environment, CUDA, dataset access, cache paths, and first-run smoke commands."
type = "maintenance"
priority = "high"
status = "resolved"
labels = ["docs", "identity"]
context = ["README.md is now a concise project front door, while setup.qmd and operator references hold pieces of environment, dataset, cache, and first-smoke guidance.", "A portable setup path needs to cite Project Aria/ASE access, uv-managed package setup, CUDA/device expectations, and offline-store smoke commands without committing host-local assumptions."]
references = ["repo:README.md#documentation-map", "repo:docs/contents/setup.qmd", "url:https://facebookresearch.github.io/projectaria_tools/docs/open_datasets/aria_synthetic_environments_dataset/ase_getting_started"]
resolved_at = "2026-05-06"
resolution_note = "Dedicated SETUP.md and one-scene smoke docs now provide the portable environment/data/cache/smoke path and are linked from README and Quarto setup."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-026"
title = "Candidate-to-label ordering is not independently guarded"
description = "Large oracle and rollout generation still needs an explicit guard that candidate poses, validity masks, depths, point clouds, RRI labels, and diagnostics remain aligned after sampling, pruning, batching, shuffling, and serialization."
type = "bug"
priority = "high"
status = "resolved"
labels = ["data", "rri", "candidate-generation", "rollouts", "tests"]
context = ["The alignment distillation called out a known candidate/RRI shuffling risk that should be fixed before generating large rollout datasets.", "Rerun and rollout traces now expose candidate tables, but the M1 contract gate still needs direct ordering tests across candidate generation, oracle labeling, offline stores, and trace serialization.", "If candidate order drifts, rankings and top-oracle diagnostics can look plausible while training labels are attached to the wrong view."]
references = ["repo:docs/contents/thesis/roadmap.qmd#roadmap-m1", "repo:aria_nbv/aria_nbv/pose_generation/types.py", "repo:aria_nbv/aria_nbv/oracle/pipelines/scene_labels.py", "repo:aria_nbv/aria_nbv/pose_generation/rollout_trace.py", "bib:VIN-NBV-frahm2025"]
resolved_at = "2026-05-06"
resolution_note = "Candidate-to-label ordering now has independent synthetic guards plus renderer/offline-writer fail-fast checks; remaining real-store proof is tracked under the M1 contract gate."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-029"
title = "kg-claim-check verdict contract was missing"
description = "The kg-claim-check wrapper used the context-pack route without a verdict, confidence, supporting_evidence, or contradicting_evidence fields, making advisor-facing claim discipline unenforceable."
type = "integration"
priority = "high"
status = "resolved"
labels = ["kg", "litkg", "claim-check", "thesis"]
context = ["Read-only agent probes on todo-026 and issue-013 showed kg-claim-check returned a generic context pack with verb=check instead of checking the claim.", "DECISIONS.md requires kg-claim-check for advisor-facing proposal, roadmap, research-question, and literature-synthesis claims, so the verdict shape is load-bearing."]
references = ["repo:.agents/memory/state/DECISIONS.md", "repo:.agents/external/litkg-rs/crates/litkg-core/src/context_pack.rs", "repo:.agents/external/litkg-rs/crates/litkg-cli/src/main.rs", "repo:.agents/issues.toml#issue-023", "repo:.agents/issues.toml#issue-025", "repo:.agents/refactors.toml#refactor-016", "repo:.agents/todos.toml#todo-061"]
resolved_at = "2026-05-08"
resolution_note = "claim-verdict logic shipped in litkg-rs commit 152f13b (ClaimVerdictParts at context_pack.rs:1627, claim_verdict at line 1634); DB-side scaffolding shipped in commit d3e980d. Smoke confirmed: V0-OBB claim verdict=supported(1.0); V1-actor-visible verdict=contradicted(0.9); Habitat claim verdict=unverifiable(0.2)."
resolved_from = ".agents/issues.toml"

[[issue]]
id = "issue-030"
title = "kg-query duplicated kg-route without synthesis"
description = "The kg-query Make target promised question retrieval but called the same context-pack path as kg-route without answer synthesis or a distinct contract."
type = "maintenance"
priority = "medium"
status = "resolved"
labels = ["kg", "litkg", "tooling", "scaffold"]
context = ["The command produced the same context-pack shape as kg-route and trained agents to expect synthesis that did not exist.", "The chosen fix is deletion: keep kg-search for fast retrieval and kg-route for context packs; reserve synthesis for a future real answer layer."]
references = ["repo:Makefile", "repo:.agents/references/litkg_quick_reference.md", "repo:.agents/skills/aria-litkg-memory/SKILL.md", "repo:.agents/skills/semantic-scholar-litkg/SKILL.md", "repo:.agents/issues.toml#issue-023", "repo:.agents/refactors.toml#refactor-016", "repo:.agents/refactors.toml#refactor-017"]
resolved_at = "2026-05-08"
resolution_note = "kg-query duplication eliminated by deleting the Make target (commit d3e980d) and the supporting refactor record (refactor-017). Documented in litkg_quick_reference.md and aria-litkg-memory/SKILL.md updates from the same pass."
resolved_from = ".agents/issues.toml"

[[todo]]
id = "todo-009"
title = "Bounded public docs identity cleanup"
description = "Update public README/docs identity, stale GitHub links, generated-agent nav leaks, and typo-facing Quarto links without full docs-tree restructuring."
priority = "high"
status = "resolved"
labels = ["docs", "identity", "bounded-cleanup"]
issue_ids = ["issue-003", "issue-005", "issue-008"]
loc_min = 80
loc_expected = 220
loc_max = 520
context = ["README.md still opens with seminar-era naming.", "docs/_quarto.yml points GitHub links to JanDuchscherer104/NBV and exposes TODOs plus Agent Scaffolding in primary nav.", "docs/index.qmd links to typo pages coral_intergarion and generated agent scaffold mirrors."]
implementation_notes = ["Keep docs/contents/todos.qmd source in place but remove it from primary public navigation.", "Remove public Agent Scaffolding nav exposure without deleting generated mirror sources.", "Rename typo-facing pages and links only where bounded and renderable."]
acceptance = ["README and docs home use ARIA-NBV / thesis-era naming.", "Active docs links use JanDuchscherer104/ARIA-NBV.", "Top-level nav no longer exposes TODOs or Agent Scaffolding as primary public pages.", "coral_intergarion and hesita typo-facing links are replaced or intentionally absent from active nav."]
verification = ["cd docs && quarto render index.qmd contents/roadmap.qmd contents/questions.qmd", "scripts/nbv_qmd_outline.sh --compact", "rg -n \"Seminar|hesita|coral_intergarion|JanDuchscherer104/NBV|PRML VSLAM|litkg-rs\" README.md docs .agents/skills"]
resolved_at = "2026-04-29"
resolution_note = "Completed bounded public docs cleanup: README/docs identity updated, old repo links fixed, TODO and generated scaffold nav exposure removed, and typo-facing Quarto pages renamed."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-010"
title = "Classify dirty worktree for M0 handoff"
description = "Group current dirty worktree changes into data migration, app diagnostics, RL scaffold, docs cleanup, agent-memory updates, and unrelated operator work."
priority = "high"
status = "resolved"
labels = ["repo-hygiene", "m0", "handoff"]
issue_ids = ["issue-009"]
loc_min = 40
loc_expected = 140
loc_max = 360
context = ["The current worktree contains large tracked deletions, modified configs, app changes, RL scaffold files, docs edits, and untracked .agents DB files.", "Roadmap M0 explicitly requires dirty worktree classification before larger thesis implementation work."]
implementation_notes = ["Use git status --short and targeted git diff --stat by subsystem.", "Do not stage, revert, or delete unrelated user changes.", "Produce a handoff note that identifies which changes belong to the current slice and which need owner review."]
acceptance = ["A concise worktree classification exists in .agents/memory/history or a canonical handoff doc.", "Current-task files are separable from unrelated pending work.", "No destructive git commands are used."]
verification = ["git status --short", "git diff --stat -- . ':!docs/_site'"]
resolved_at = "2026-04-29"
resolution_note = "Resolved by the cleanup/refactor worktree review and commit packaging on 2026-04-29; current parent and litkg-rs submodule worktrees were clean after commits."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-013"
title = "Implement nbv-geometry-contracts skill"
description = "Create an ARIA-specific skill for pose, camera, coordinate-frame, projection, PyTorch3D, NDC, and CW90 contracts."
priority = "high"
status = "resolved"
labels = ["agent", "geometry", "contracts"]
issue_ids = ["issue-007", "issue-012"]
loc_min = 80
loc_expected = 160
loc_max = 360
context = ["The PRML cross-review recommended a dedicated geometry-contract skill because frame bugs are thesis-grade failure modes.", "Root and nested guides already require PoseTW/CameraTW, config-as-factory, and explicit frame semantics, but the guidance is spread across AGENTS.md, vin/AGENTS.md, rendering tests, and GOTCHAS.md."]
implementation_notes = ["Base the skill on aria_nbv/AGENTS.md, aria_nbv/aria_nbv/vin/AGENTS.md, rendering tests, and GOTCHAS.md.", "Make it route tasks touching candidate poses, rig poses, cameras, frusta, CW90 display correction, PyTorch3D NDC, and depth backprojection.", "Keep the skill concise and contract-focused; link to tests instead of embedding long explanations."]
acceptance = ["A .agents/skills/nbv-geometry-contracts/SKILL.md exists and names ARIA-NBV frame surfaces only.", "The skill lists targeted geometry/rendering verification commands.", "No wrong-repo or PRML domain content appears in the skill."]
verification = ["make check-agent-memory", "rg -n \"PRML VSLAM|prml_vslam|litkg-rs|Rust\" .agents/skills/nbv-geometry-contracts || true"]
resolved_at = "2026-04-29"
resolution_note = "Added nbv-geometry-contracts skill with ARIA pose/camera/CW90/PyTorch3D contract workflow and metadata."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-014"
title = "Implement counterfactual-rollout-planner skill"
description = "Create an ARIA-specific skill for bounded counterfactual rollouts, non-myopic planning evaluation, invalid action handling, and the M6 value/RL gate."
priority = "medium"
status = "resolved"
labels = ["agent", "rollouts", "planning"]
issue_ids = ["issue-007", "issue-012"]
loc_min = 80
loc_expected = 160
loc_max = 360
context = ["Roadmap M5 and M6 require greedy, stochastic/beam, model-scored, and optional value/RL comparisons under explicit horizon and branch-factor limits.", "The PRML cross-review recommended a planning skill so agents keep rollout work bounded and do not overclaim continuous RL."]
implementation_notes = ["Route to pose_generation counterfactual APIs, RL tests, roadmap M5/M6, and questions RQ5/RQ6.", "State that online discrete Q_H over finite candidates is the RQ5 bridge after offline evidence, while full continuous control and generic simulator-backed RL remain gated stretch work.", "Require rollout artifacts to report cumulative RRI, acquisition cost, invalid rates, and runtime."]
acceptance = ["The skill protects rollout/planning contract edits with targeted tests and docs checks.", "It distinguishes one-step VIN scoring, counterfactual rollout evaluation, and exploratory RL baselines.", "It uses current ARIA-NBV terminology and does not import PRML VSLAM domain rules."]
verification = ["make check-agent-memory", "rg -n \"PRML VSLAM|prml_vslam|ViSTA\" .agents/skills/counterfactual-rollout-planner || true"]
resolved_at = "2026-04-29"
resolution_note = "Added counterfactual-rollout-planner skill for bounded rollout, invalid-action, and value/RL gate workflows."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-016"
title = "Implement docs-curator skill"
description = "Create an ARIA-specific skill for keeping README, SETUP, Quarto docs, Typst paper, slides, bibliography, generated context, and canonical memory aligned."
priority = "medium"
status = "resolved"
labels = ["agent", "docs", "hygiene"]
issue_ids = ["issue-003", "issue-012"]
loc_min = 80
loc_expected = 140
loc_max = 320
context = ["The PRML cross-review recommended a docs-paper-sync/docs-curator skill because public docs, paper, slides, roadmap, and agent memory can drift.", "Source order now separates current thesis docs and memory, proposal wording, and historical seminar evidence so public docs do not compete with the owning source."]
implementation_notes = ["Base the skill on docs/AGENTS.md, roadmap KG-friendly authoring rules, questions KG-friendly writing, and agent memory templates.", "Include when to render Quarto pages, compile Typst, update memory, and avoid generated/public docs pollution.", "Add a wrong-repo leakage check that allows intentionally repo-independent KG references but catches PRML examples in ARIA-specific skills."]
acceptance = ["A docs-curator skill exists and gives compact routing for docs/paper/slides/memory updates.", "It states source-of-truth precedence and verification commands.", "It avoids duplicating long Quarto or Typst authoring instructions already owned elsewhere."]
verification = ["make check-agent-memory", "rg -n \"PRML VSLAM|prml_vslam|CODEOWNER\" .agents/skills/docs-curator .agents/skills/python-standards .agents/references"]
resolved_at = "2026-04-29"
resolution_note = "Added docs-curator skill for README/Quarto/Typst/bibliography/generated-context/canonical-memory alignment and public/internal docs boundary."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-017"
title = "Implement dataset-cache-ops skill"
description = "Create an ARIA-specific operator skill for ASE download, ATEK shards, meshes, VIN offline stores, split manifests, storage estimates, and dataset smoke checks."
priority = "medium"
status = "resolved"
labels = ["agent", "data", "offline-store"]
issue_ids = ["issue-007", "issue-012"]
loc_min = 80
loc_expected = 160
loc_max = 360
context = ["The PRML cross-review recommended a dataset/cache operations skill; ARIA-NBV has data-handling guidance, downloader commands, offline store configs, and operator references but no compact operator skill.", "Migration functionality must not be restored; the canonical path is immutable VinOfflineDataset and available stores only."]
implementation_notes = ["Route to aria_nbv/aria_nbv/data_handling/AGENTS.md, README offline data notes, SETUP once created, and operator_quick_reference.md.", "Cover list/download smoke commands, offline_only.toml, split manifests, config hashes, storage estimates, and failure triage.", "Explicitly state that legacy cache migration is not supported and should not be reintroduced."]
acceptance = ["The skill helps agents operate current VIN offline stores without reviving legacy cache APIs.", "It names dry-run/list/smoke commands and data-handling tests.", "It stays operator-focused and does not widen the public Python API."]
verification = ["make check-agent-memory", "rg -n \"OracleRriCacheDataset|VinSnippetCacheProvider|migrat\" .agents/skills/dataset-cache-ops || true"]
resolved_at = "2026-04-29"
resolution_note = "Added dataset-cache-ops skill for ASE/ATEK/mesh/VIN offline-store operations and smoke checks without restoring legacy cache migration."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-019"
title = "Audit active skills for residual wrong-repo examples"
description = "Find and fix leftover PRML/litkg/Rust/example leakage in ARIA-specific skills and references without removing intentionally repo-independent KG tooling."
priority = "medium"
status = "resolved"
labels = ["agent", "hygiene", "scaffold"]
issue_ids = ["issue-012"]
loc_min = 40
loc_expected = 100
loc_max = 240
context = ["The original PRML cross-review found copied skills with wrong repo names and commands.", "Most high-risk skills were fixed, but at least one ARIA skill reference still contains a prml_vslam example and some checks need to distinguish intentional litkg-rs KG tooling from wrong-repo leakage."]
implementation_notes = ["Search .agents/skills and .agents/references for PRML VSLAM, prml_vslam, CODEOWNER placeholders, old repo paths, and Rust/litkg review gates.", "Do not flag .agents/skills/semantic-scholar-litkg merely for owning the litkg-rs submodule; flag only ARIA-specific skills that import unrelated repo assumptions.", "Update verification commands in existing DB records if they are now too broad."]
acceptance = ["ARIA-specific skills and references contain no unrelated PRML examples or wrong paths.", "Intentional repo-independent KG/litkg guidance remains scoped to semantic-scholar-litkg.", "DB verification commands no longer fail on intentional KG references."]
verification = ["rg -n \"PRML VSLAM|prml_vslam|CODEOWNER|JanDuchscherer104/NBV\" .agents/skills .agents/references AGENTS.md README.md", "make check-agent-memory"]
resolved_at = "2026-04-29"
resolution_note = "Audited active ARIA-specific guidance and removed stale PRML/NBV absolute-path examples while keeping intentional litkg-rs references scoped to semantic-scholar-litkg."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-015"
title = "Implement entity-aware-rri skill"
description = "Create an ARIA-specific skill for target/entity selection, OBB-cropped RRI, target-specific labels, and entity-conditioned VIN evaluation."
priority = "medium"
status = "resolved"
labels = ["agent", "entity", "rri"]
issue_ids = ["issue-007", "issue-012"]
loc_min = 80
loc_expected = 160
loc_max = 360
context = ["Roadmap M3/M4 centers target-aware RRI and target-conditioned VIN, while todo-005 tracks the implementation work.", "The PRML scaffold recommendation called for dedicated domain skills that prevent agents from treating entity-aware RRI as generic docs or model work."]
implementation_notes = ["Route to roadmap M3/M4, questions RQ1/RQ2/RQ4, rri_metrics guidance, data-handling containers, and VIN batch contracts.", "Require invalid or unsupported targets to be represented explicitly rather than encoded as low-RRI candidates.", "Keep predicted OBB realism and SceneScript/global planning as later ablations unless directly tied to the target-aware thesis claim."]
acceptance = ["The skill names target RRI, GT OBB crop, target selection, target-conditioned batch fields, and diagnostics boundaries.", "It links to existing todo-005 rather than duplicating implementation scope.", "No wrong-repo scaffold text remains."]
verification = ["make check-agent-memory", "rg -n \"PRML VSLAM|prml_vslam|VSLAM-specific\" .agents/skills/entity-aware-rri || true"]
resolved_at = "2026-04-29"
resolution_note = "Added entity-aware-rri skill for target/entity selection, OBB-cropped RRI, target-conditioned VIN contracts, and diagnostics boundaries."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-023"
title = "Document Rerun inspector smoke workflow"
description = "Add public-facing operator documentation and README pointers for producing one Rerun .rrd recording from an offline sample."
priority = "medium"
status = "resolved"
labels = ["docs", "diagnostics", "rerun", "setup"]
issue_ids = ["issue-015", "issue-005", "issue-003"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["The Rerun tool should not become an undocumented operator-only workflow.", "The review asked for docs/contents/impl/rerun_offline_inspector.qmd, `.configs/rerun_offline.toml`, and a README link to the smoke command.", "Public docs should stay aligned with the thesis and avoid exposing internal agent work-management surfaces."]
implementation_notes = ["Add a concise Quarto implementation page for the Rerun offline inspector once the CLI/config exist.", "Link the smoke command from README.md and, if appropriate, the implementation docs overview.", "Explain store prerequisites, spawn/save modes, candidate filtering/coloring, and the expected output artifact without duplicating internal TOML backlog details."]
acceptance = ["A new user can find the intended Rerun `.rrd` smoke command for one immutable offline sample.", "README.md links to the Rerun inspector smoke command and implementation page.", "Docs make clear that visualization downsampling is display-only and never mutates oracle labels."]
verification = ["cd docs && quarto render contents/impl/rerun_offline_inspector.qmd", "rg -n \"nbv-rerun-inspect|rerun_offline\" README.md docs/contents/impl .configs"]
resolved_at = "2026-04-30"
resolution_note = "Documented the Rerun offline inspector smoke workflow in README and docs/contents/impl/rerun_offline_inspector.qmd, including prerequisites, smoke command, output modes, geometry safety, and display-only downsampling."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-024"
title = "Lock non-myopic rollout state, reward, and VIN evidence gates"
description = "Record the first thesis-grade non-myopic comparison and related reward/state/evidence decisions before larger rollout or RL work proceeds."
priority = "medium"
status = "resolved"
labels = ["research", "planning", "rri", "vin", "thesis"]
issue_ids = ["issue-007", "issue-013"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["The project state already recommends discrete search, beam rollouts, close-greedy control, or discrete-shell RL before continuous control.", "Counterfactual multi-step states still lack full RGB, SLAM, and semantic modalities unless synthesized or approximated.", "The review recommended bounded oracle-RRI lookahead versus one-step greedy RRI as the first thesis-grade comparison and a geometry-first actor-visible state."]
implementation_notes = ["Update thesis questions, roadmap, or canonical memory to state the first comparison as bounded lookahead over oracle RRI versus one-step greedy under the same candidate budget.", "Define first rollout state as geometry-first: accumulated semidense/current points, candidate poses, persisted candidate diagnostics, and oracle labels for supervision/evaluation only.", "Choose episode-normalized additive reward or log-improvement reward for multi-step return while keeping current RRI for one-step label compatibility.", "Keep the first target-aware metric as GT-OBB-cropped target RRI and compare scene-level versus target-level RRI.", "Set the VIN evidence gate as held-out ranking quality, oracle-evaluated VIN-selected rollouts, calibration/stage-shift analysis, Rerun failure visualization, and only then Q/return heads."]
acceptance = ["The first non-myopic experiment, actor-visible state, reward convention, target-aware metric, and VIN evidence gate are documented in current thesis-facing or canonical memory surfaces.", "The documentation explicitly keeps continuous actor-critic and synthesized rich modalities out of the first milestone.", "The recorded decisions are linked to existing RQ and roadmap sections rather than added as public TODO prose."]
verification = ["cd docs && quarto render contents/thesis/questions.qmd contents/thesis/roadmap.qmd", "make check-agent-memory"]
resolved_at = "2026-04-30"
resolution_note = "Locked the first non-myopic comparison, rollout actor-visible state, multi-step reward convention, first target-aware metric, and VIN evidence gate in thesis docs and canonical memory."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-025"
title = "Define RolloutTrace schema and one-scene trace writer"
description = "Create a durable rollout trace contract for candidate tables, selected actions, per-step metrics, cumulative metrics, termination reasons, and replay lineage."
priority = "high"
status = "resolved"
labels = ["rollouts", "data", "contracts", "storage"]
issue_ids = ["issue-018", "issue-007"]
loc_min = 220
loc_expected = 520
loc_max = 1100
context = ["CounterfactualPoseGenerator currently returns in-memory trajectories, but the thesis sequence-data direction needs durable per-step traces before generating many rollouts.", "The trace must distinguish actor-visible state, oracle-only signals, and critic/model predictions.", "The context synthesis listed required fields such as scene_id, snippet_id, rollout_id, step index, candidate set, validity, selected action, RRI/entity RRI, generated candidate observations, cumulative metrics, and termination reason."]
references = ["repo:aria_nbv/aria_nbv/pose_generation/rollout_trace.py", "repo:aria_nbv/aria_nbv/pose_generation/counterfactuals.py", "repo:aria_nbv/tests/pose_generation/test_counterfactuals.py", "repo:docs/contents/thesis/questions.qmd#rq4-planning"]
implementation_notes = ["Start with typed Python dataclasses or DTOs that wrap the existing CounterfactualRolloutResult rather than a parallel MDP stack.", "Store full candidate tables and masks, but materialize expensive candidate observations only for selected actions or retained chains.", "Include deterministic lineage fields: mesh version, candidate config hash, oracle config hash, model checkpoint hash when applicable, random seed, rollout policy, and source cache version.", "Keep the first writer small and one-scene smokeable before choosing a large Zarr layout."]
acceptance = ["A typed RolloutTrace or equivalent schema can represent a fixed-horizon rollout with full per-step candidate metadata.", "A one-scene smoke command writes a replayable trace for at least greedy and random-valid policies.", "Trace metadata records enough lineage to compare or reject mixed-generation samples.", "Docs or docstrings define which fields are actor-visible, oracle-only, and optional heavy diagnostics."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation/test_counterfactuals.py", "cd aria_nbv && uv run nbv-rollout-trace-smoke --output-path ../.artifacts/rollouts/synthetic_rollout_traces.msgpack --horizon 2 --num-samples 8 --seed 0", "rg -n \"RolloutTrace|rollout_id|termination_reason|candidate_config_hash|oracle_config_hash\" aria_nbv/aria_nbv aria_nbv/tests docs/contents/thesis/questions.qmd"]
resolved_at = "2026-05-02"
resolution_note = "Added RolloutTrace/RolloutStepTrace/RolloutLineage schema, MessagePack trace writer/reader, synthetic greedy/random trace smoke command, and rollout trace roundtrip tests."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-036"
title = "Add M1 geometry and candidate-order guard tests"
description = "Add focused tests and diagnostics for pose-frame/CW90 consistency, candidate/RRI order alignment, validity-mask alignment, and depth/backprojection convention before large rollout generation."
priority = "high"
status = "resolved"
labels = ["geometry", "rri", "tests", "contracts"]
issue_ids = ["issue-007", "issue-021"]
loc_min = 160
loc_expected = 360
loc_max = 820
context = ["The current context repeatedly flags coordinate-frame bugs and candidate/RRI shuffling as silent label-corruption risks.", "GOTCHAS.md maintains candidate validity and frame caveats, while roadmap M1 requires frame/CW90 risks to be documented.", "Rollout traces will amplify any ordering or frame bug across multiple steps."]
references = ["repo:.agents/skills/nbv-geometry-contracts/SKILL.md", "repo:.agents/memory/state/GOTCHAS.md", "repo:docs/contents/ase_dataset.qmd#coordinate-systems", "repo:aria_nbv/tests/pose_generation/test_counterfactuals.py"]
implementation_notes = ["Add tests that candidate poses, p3d cameras, validity masks, RRI vectors, and selected indices remain aligned through serialization/collation.", "Include CW90 display-only checks so visual rotations cannot mutate training/cache geometry.", "Cover world<-camera versus camera<-world naming and PyTorch3D unprojection sign/frame conventions where the current code touches them.", "Treat failures as blockers for scale generation rather than paper-only caveats."]
acceptance = ["Targeted tests fail on candidate/RRI shuffles or validity-mask misalignment.", "Pose-frame tests document physical frame versus display-only CW90 behavior.", "Depth render/backprojection conventions are covered by at least one regression path used by oracle labels.", "The tests are part of M1 verification guidance."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation tests/rendering tests/data_handling/test_vin_batch_collate.py tests/rri_metrics", "make context-contracts"]
resolved_at = "2026-05-02"
resolution_note = "Added candidate/RRI order, CW90 display-only, full-shell candidate index, and depth/backprojection guard coverage; targeted M1 tests and make context-contracts pass."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-040"
title = "Add aria-litkg-memory skill"
description = "Create and adopt a compact ARIA-NBV skill that routes broad memory retrieval, claim checks, source-backed task routing, and consolidation through litkg before manual browsing."
priority = "high"
status = "resolved"
labels = ["agent", "skills", "kg", "litkg", "memory"]
issue_ids = ["issue-012", "issue-023"]
loc_min = 40
loc_expected = 100
loc_max = 220
context = ["Root routing now requires broad cross-surface coding, docs, KG, or thesis work to start from a litkg context pack, but ordinary agent memory retrieval still needs an explicit skill distinct from litkg-rs implementation work.", "The new skill should make litkg the default context router while leaving semantic-scholar-litkg responsible for KG toolkit/config/backend implementation.", "This skill is also the agent-facing place to encode source authority until litkg retrieval exposes authority/freshness metadata directly."]
references = ["repo:AGENTS.md#routing", "repo:.agents/skills/aria-litkg-memory/SKILL.md", "repo:.agents/skills/semantic-scholar-litkg/SKILL.md", "repo:.agents/skills/agents-db/SKILL.md#provenance-contract", "repo:.configs/litkg.toml", "repo:.agents/kg/README.md"]
implementation_notes = ["Keep the skill concise and focused on usage protocol, not litkg-rs internals.", "List the main Make targets for capability checks, query, brief, route, search, related-source retrieval, and claim checking.", "Document the authority order from code/canonical memory through generated context, debrief history, archives, and external papers.", "Validate with the local skill validator and make check-agent-memory."]
acceptance = ["`.agents/skills/aria-litkg-memory/SKILL.md` exists with valid skill frontmatter.", "Root routing points broad memory retrieval and consolidation work to the skill.", "The skill tells agents to inspect cited canonical sources before acting on retrieved context.", "The skill separates routine memory retrieval from semantic-scholar-litkg implementation work."]
verification = ["python3 ${CODEX_HOME:-$HOME/.codex}/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/aria-litkg-memory", "make check-agent-memory"]
resolved_at = "2026-05-02"
resolution_note = "Implemented in commit 3a3a4ff by adding .agents/skills/aria-litkg-memory/SKILL.md, root routing, validation, and memory checks."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-042"
title = "Expand litkg source coverage for agent history, skills, references, generated context, and experiment placeholders"
description = "Split ARIA-NBV litkg sources into canonical state, active backlog, workflow guidance, episodic history, generated context, and future experiment/cache evidence so retrieval has the right material and authority metadata."
priority = "high"
status = "resolved"
labels = ["kg", "litkg", "config", "memory", "experiments"]
issue_ids = ["issue-023", "issue-025"]
loc_min = 60
loc_expected = 160
loc_max = 380
context = ["The current litkg config ingests canonical memory and active backlog, but the agent-memory plane also needs debrief history, work notes, skills, references, generated context, and curated experiment/cache manifests.", "Source classes need explicit descriptive authority such as canonical, active_backlog, workflow, episodic, generated, and evidence so future retrieval can rank conflicting context.", "Experiment/cache ingestion should target manifests and summaries, not raw tensor blocks, sample shards, or full EVL backbone stores."]
references = ["repo:.configs/litkg.toml", "repo:.agents/kg/README.md", "repo:.agents/skills/aria-litkg-memory/SKILL.md#source-authority", "repo:.agents/skills/semantic-scholar-litkg/SKILL.md", "repo:.agents/external/litkg-rs/docs/kg-stack.md", "repo:docs/_generated/context/source_index.md"]
implementation_notes = ["Keep legacy source sections until current litkg-rs consumers understand the split source classes.", "Add descriptive authority metadata in TOML even if current litkg-rs ignores it; future retrieval should consume it.", "Exclude raw heavy stores and shard directories from experiment/cache source globs.", "Run litkg capabilities after config edits to catch stale or missing required paths."]
acceptance = ["`.configs/litkg.toml` names separate source classes for agent_state, agent_backlog, agent_history, agent_skills, generated_context, and experiments.", "Generated context and skill/reference docs are discoverable through litkg retrieval.", "Experiment placeholders point at curated manifests/summaries and exclude heavy tensors/shards.", "Capability output does not regress because of the added source metadata."]
verification = ["make kg-capabilities KG_FORMAT=json", "make kg-search KG_QUERY=\"aria-litkg-memory source authority\" KG_LIMIT=5", "make check-agent-memory"]
resolved_at = "2026-05-02"
resolution_note = "Implemented in commit 3a3a4ff by expanding .configs/litkg.toml source classes for canonical state, active backlog, skills/references, episodic history, generated context, and experiment placeholders."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-041"
title = "Add root kg-query / kg-brief / kg-route retrieval targets"
description = "Expose one-command root litkg retrieval and routing targets so agents can query canonical memory, docs, code, papers, and backlog without hand-assembling cargo commands."
priority = "high"
status = "resolved"
labels = ["kg", "litkg", "makefile", "agent", "retrieval"]
issue_ids = ["issue-023"]
loc_min = 50
loc_expected = 140
loc_max = 320
context = ["litkg-rs already exposes capabilities, context-pack, literature search/show, and KG find operations, but ARIA agents need a stable root surface that matches the root dispatcher and skill workflow.", "The first retrieval layer should cover kg-capabilities, kg-search, kg-show-paper, kg-query, kg-brief, kg-route, kg-claim-check, and kg-related, with JSON output available for agents or scripts.", "These wrappers should remain thin and local-first; richer claim/consolidation behavior belongs in litkg-rs once the repo-independent command exists."]
references = ["repo:Makefile", "repo:AGENTS.md#commands", "repo:.agents/skills/aria-litkg-memory/SKILL.md", "repo:.agents/skills/semantic-scholar-litkg/SKILL.md#agent-contract", "repo:.agents/external/litkg-rs/AGENTS.md", "repo:.agents/external/litkg-rs/docs/architecture.md"]
implementation_notes = ["Use the checked-in litkg-rs submodule through `cargo run --manifest-path .agents/external/litkg-rs/Cargo.toml -p litkg-cli -- ...`.", "Keep variables such as LITKG_CONFIG, LITKG_PROFILE, KG_QUERY, KG_TASK, KG_TOPIC, KG_FORMAT, and KG_LIMIT overridable.", "Prefer context-pack for query/brief/route and kg find for quick search or related-path retrieval.", "Do not make these targets depend on a live Neo4j/Graphiti/MemPalace service."]
acceptance = ["`make kg-capabilities` reports backend/source readiness.", "`make kg-search KG_QUERY=...` and `make kg-query KG_QUERY=...` return useful text results.", "`make kg-brief`, `make kg-route`, `make kg-show-paper`, `make kg-claim-check`, and `make kg-related` have documented required variables.", "Targets fail with actionable messages when required variables are missing."]
verification = ["make kg-capabilities KG_FORMAT=json", "make kg-search KG_QUERY=\"entity-aware RRI\" KG_LIMIT=5", "make kg-query KG_QUERY=\"current RRI contract\""]
resolved_at = "2026-05-02"
resolution_note = "Implemented in commit 3a3a4ff by adding thin root Make wrappers for kg-capabilities, kg-search, kg-query, kg-brief, kg-route, kg-claim-check, kg-related, and kg-show-paper."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-008"
title = "Validate partial offline_only.toml VIN offline store smoke path"
description = "Make offline_only.toml load at least one immutable VIN offline-store train/val sample from the current partial local store after corrected command behavior and validation land."
priority = "high"
status = "resolved"
labels = ["m0", "vin", "diagnostics", "offline-store", "data-blocker", "rerun"]
issue_ids = ["issue-007", "issue-014"]
loc_min = 40
loc_expected = 120
loc_max = 260
context = ["Roadmap M0 requires validating that offline_only.toml can load the available VIN offline store into the VIN diagnostics page.", "aria_nbv/tests/vin/test_vin_utils.py now checks that the diagnostics config preserves the offline source, disables W&B, and keeps apply_cw90_correction=false.", "The local `.data/offline_cache/vin_offline` store has manifest.json, sample_index.jsonl, split arrays, and shard directories, but the current reader rejects it as manifest version 5 while OFFLINE_DATASET_VERSION expects 6.", "A Rerun offline inspector can target the partial store for diagnostic smoke only after the store is rebuilt or a deliberate compatibility path is chosen."]
references = ["repo:README.md#offline-data", "repo:.configs/offline_only.toml", "repo:docs/contents/impl/rerun_offline_inspector.qmd", "repo:aria_nbv/tests/vin/test_vin_utils.py"]
implementation_notes = ["Prefer nbv-summary as a non-interactive smoke before manual Streamlit or Rerun inspection.", "Use `--config-path offline_only.toml`; bare config names resolve under `.configs/`.", "Validate the partial store strictly and surface actionable validation errors instead of assuming the manifest is missing.", "Do not hand-edit store manifests, sample_index.jsonl, split arrays, shards, or payloads to silence failures.", "Keep apply_cw90_correction=false unless candidate poses, p3d_cameras, and reference pose are rotated in lockstep."]
acceptance = ["`uv run nbv-summary --config-path offline_only.toml` loads at least one train/val batch from the immutable offline store after the version mismatch is resolved.", "Streamlit VIN diagnostics can point at offline_only.toml without falling back to online data once validation passes.", "The same partial store can be used by `nbv-rerun-inspect` for diagnostic smoke once that CLI exists and the store is compatible.", "Any remaining failure has an issue with exact command, traceback summary, and affected contract."]
verification = ["cd aria_nbv && uv run nbv-summary --config-path offline_only.toml", "cd aria_nbv && uv run pytest tests/vin/test_vin_utils.py tests/data_handling/test_vin_offline_store.py"]
resolved_at = "2026-05-02"
resolution_note = "Resolved by migrating the local vin_offline diagnostic store artifact from v5 to v6, appending five current-writer snippets 000043..000047 without recomputing old payloads, and verifying nbv-summary loads an offline train batch."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-020"
title = "Add offline-store visual primitive inventory"
description = "Create a strict inventory of visualizable primitives and required fields for immutable VIN offline-store samples."
priority = "high"
status = "resolved"
labels = ["offline-store", "diagnostics", "data", "contracts", "rerun"]
issue_ids = ["issue-017", "issue-007"]
loc_min = 120
loc_expected = 260
loc_max = 520
context = ["A Rerun inspector should be a contract verifier before it is a renderer.", "The review identified required fields such as sample_id, split, scene_id/snippet_id, reference_pose_world_rig, candidate_poses_world_cam, p3d_cameras or intrinsics, candidate_valid_mask, oracle_rri, accuracy_delta, completeness_delta, and semidense points.", "Optional visual layers include invalid_reason, candidate_depth, candidate_points, mesh references, OBBs, fused points, voxel bounds, EVL extents, and collision segments."]
references = ["repo:docs/contents/impl/rerun_offline_inspector.qmd", "repo:aria_nbv/aria_nbv/data_handling/_offline_visual_inventory.py", "repo:aria_nbv/tests/rerun_inspector/", "repo:.agents/skills/dataset-cache-ops/SKILL.md"]
implementation_notes = ["Add a typed inventory helper such as OfflineVisualInventory with booleans for semidense points, candidate poses, candidate points/depths, GT mesh refs, OBBs, oracle RRI, accuracy/completeness, masks, and warnings.", "Use active aria_nbv.data_handling immutable offline-store samples only; do not revive removed cache modules or online runtime training APIs.", "Make required-field failures actionable before any Rerun logging starts, and report missing optional fields in metadata.", "Keep this inventory small enough to be reusable by CLI, tests, and future diagnostics."]
acceptance = ["One function returns the available visualizable fields for a loaded offline sample.", "Missing required pose/camera/RRI fields fail before rendering with rebuild or config guidance.", "Missing optional fields are reported as metadata warnings rather than guessed from ad hoc sample attributes.", "The inventory is covered by fixture-based tests and does not mutate derived store artifacts."]
verification = ["cd aria_nbv && uv run pytest tests/rerun_inspector tests/data_handling/test_vin_offline_store.py", "ruff check aria_nbv/aria_nbv/rerun_inspector aria_nbv/tests/rerun_inspector"]
resolved_at = "2026-05-04"
resolution_note = "Implemented OfflineVisualInventory and required/optional visual payload reporting for immutable offline-store samples; covered by rerun_inspector and offline-store tests."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-021"
title = "Add TOML-configurable Rerun offline inspector CLI"
description = "Implement nbv-rerun-inspect as a CLI-generated Rerun recording workflow for immutable VIN offline-store samples."
priority = "high"
status = "resolved"
labels = ["diagnostics", "offline-store", "geometry", "rerun", "thesis"]
issue_ids = ["issue-015", "issue-017", "issue-016"]
loc_min = 320
loc_expected = 700
loc_max = 1400
context = ["Temporal, multimodal, 3D candidate-set debugging is not covered by the current Plotly/Streamlit diagnostics surface.", "The intended first tool should inspect one real offline sample/snippet with semidense points, candidate camera frusta, candidate validity, RRI labels, optional mesh/OBBs/geometric primitives, and metadata.", "The default viewer must avoid excessive entity counts by batching candidate frusta and downsampling point clouds or meshes by config."]
references = ["repo:docs/contents/impl/rerun_offline_inspector.qmd", "repo:README.md#important-cli-commands", "repo:aria_nbv/aria_nbv/rerun_inspector/_cli.py", "url:https://rerun.io/docs"]
implementation_notes = ["Add aria_nbv/aria_nbv/rerun_inspector with config, sample, frusta, color, logger, metadata, and CLI modules.", "Add RerunOfflineInspectorConfig with config-as-factory construction and `.configs/rerun_offline.toml` for dataset, output, timeline, world, geometry, cameras, metrics, and primitives sections.", "Add the `nbv-rerun-inspect = aria_nbv.rerun_inspector._cli:main` entry point and an optional pinned rerun-sdk dependency.", "Support spawn, save, and connect output modes plus split/index or sample-id selection.", "Use stable batched entity paths such as `/world/candidates/frusta_all`, `/world/candidates/frusta_top_oracle`, and `/world/candidates/selected/<candidate_id>`.", "Keep the inspector read-only and scoped to the current immutable offline store; do not support legacy cache modules, online datasets, or premature counterfactual fields."]
acceptance = ["`nbv-rerun-inspect` exists and reads `.configs/rerun_offline.toml`.", "The CLI loads one immutable VIN offline-store sample and logs semidense points, candidate frusta, reference pose, optional mesh, and candidate RRI/validity metadata.", "Output modes include spawn, save, and connect.", "Candidate count, top-k, filter, coloring, line radius, labels, and point/mesh downsampling are TOML-configurable.", "Default rendering handles a 60-candidate sample with batched frusta and stable entity paths.", "Missing required sample fields fail with actionable errors before logging."]
verification = ["cd aria_nbv && uv run nbv-rerun-inspect --config-path ../.configs/rerun_offline.toml --split val --index 0 --save ../.artifacts/rerun/sample.rrd", "cd aria_nbv && uv run pytest tests/rerun_inspector", "ruff format aria_nbv/aria_nbv/rerun_inspector aria_nbv/tests/rerun_inspector", "ruff check aria_nbv/aria_nbv/rerun_inspector aria_nbv/tests/rerun_inspector"]
resolved_at = "2026-05-04"
resolution_note = "Implemented nbv-rerun-inspect, RerunOfflineInspectorConfig, TOML smoke config, stable Rerun entity paths, output modes, docs, and tests."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-022"
title = "Add frame-safe Rerun geometry logging helpers"
description = "Implement PoseTW/CameraTW-aware Rerun logging helpers for manual frusta and tested native camera logging."
priority = "high"
status = "resolved"
labels = ["geometry", "camera", "rerun", "tests", "contracts"]
issue_ids = ["issue-016", "issue-007"]
loc_min = 160
loc_expected = 360
loc_max = 760
context = ["ARIA/PyTorch3D/EFM coordinate conventions are fragile, and plausible-looking frusta can be wrong if logged through raw matrices or an untested native camera path.", "Manual batched LineStrips3D frusta are safer for candidate cameras until native rr.Pinhole alignment is proven.", "Display-only CW90 corrections must remain visual-only and never leak into logged model/cache geometry."]
references = ["repo:.agents/skills/nbv-geometry-contracts/SKILL.md", "repo:docs/contents/impl/rerun_offline_inspector.qmd#frame-safety", "repo:aria_nbv/aria_nbv/rerun_inspector/_frusta.py", "url:https://docs.rs/rerun/latest/rerun/struct.LineStrips3D.html"]
implementation_notes = ["Public helpers should accept PoseTW, CameraTW, or typed project DTOs rather than unlabelled raw matrices.", "Implement a manual_lines path that computes camera-center, image-plane corners, rectangle edges, center rays, and optional optical axes in world coordinates.", "Use batched rr.LineStrips3D for candidate frusta with colors, radii, and labels.", "Provide native rr.Transform3D plus rr.Pinhole logging only for tested reference/RGB/SLAM cameras.", "Keep candidate frustum helpers independent from Streamlit panels and reusable by tests."]
acceptance = ["Manual candidate frusta are generated from the camera model and pose and are the default candidate mode.", "Native Pinhole logging exists only for camera relationships covered by tests.", "A synthetic-camera test verifies frustum endpoints against projected or unprojected rays.", "CW90/display-only corrections cannot alter model/cache pose or camera values."]
verification = ["cd aria_nbv && uv run pytest tests/rerun_inspector/test_frusta.py tests/data_handling/test_vin_offline_store.py", "ruff check aria_nbv/aria_nbv/rerun_inspector aria_nbv/tests/rerun_inspector"]
resolved_at = "2026-05-04"
resolution_note = "Implemented PoseTW/CameraTW-aware Rerun frustum helpers plus tested world root coordinates, valid/all-invalid handling, OBB world transforms, and camera-context depth logging."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-002"
title = "Add SETUP.md"
description = "Create a dedicated SETUP.md for environment, CUDA, dataset access, cache paths, and first-run smoke commands."
priority = "high"
status = "resolved"
labels = ["scaffold", "dev-ergonomics"]
issue_ids = ["issue-005"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["README.md should stay concise, while setup details need a stable place for uv sync, Project Aria/ATEK/EFM dependencies, cache roots, and offline-only smoke tests.", ".agents/references/operator_quick_reference.md can be used for machine-specific recovery details, but shared setup docs must stay portable."]
references = ["repo:README.md#documentation-map", "repo:docs/contents/setup.qmd", "url:https://facebookresearch.github.io/projectaria_tools/docs/open_datasets/aria_synthetic_environments_dataset/ase_getting_started"]
implementation_notes = ["Create root SETUP.md or a docs/contents/setup.qmd-aligned root pointer without duplicating host-specific paths.", "Include uv sync --all-extras from aria_nbv/, VIN offline-store expectations, and Streamlit/VIN summary smoke commands.", "Link from README.md and docs/index.qmd."]
acceptance = ["A new contributor can find environment setup, cache prerequisites, and the first smoke commands from the README.", "No shared setup text contains operator-specific interpreter paths."]
verification = ["rg -n \"host-local home path|miniforge\" README.md SETUP.md docs/contents/setup.qmd", "cd docs && quarto render index.qmd contents/setup.qmd"]
resolved_at = "2026-05-06"
resolution_note = "SETUP.md, README links, and Quarto setup guidance now cover portable environment, data/cache, CUDA/CPU expectations, and first smoke commands."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-011"
title = "Add root CI, pre-commit, and make ci"
description = "Port the PRML-style local verification contract into ARIA-NBV with root make targets, pre-commit parity, and a general GitHub Actions CI workflow."
priority = "high"
status = "resolved"
labels = ["ci", "dev-ergonomics", "scaffold"]
issue_ids = ["issue-004"]
loc_min = 120
loc_expected = 260
loc_max = 620
context = ["The PRML cross-review identifies root make ci, lint/test/docs checks, pre-commit, and GitHub CI as scaffold discipline that ARIA-NBV still lacks.", "ARIA-NBV currently has a Quarto publish workflow but no root CI workflow or .pre-commit-config.yaml.", "The repo already has targeted Makefile checks for context, Typst, KG, and agent memory that can be composed into a lightweight default gate.", "The doc-alignment review flagged render/link status as the safety net for Quarto shortcodes, citations, Mermaid, glossary artifacts, and generated API references; the specific API links now exist locally, so this belongs in the general CI/docs gate rather than a separate repair todo."]
references = ["repo:AGENTS.md#commands", "repo:AGENTS.md#verification", "repo:Makefile", "repo:.github/workflows/quarto-publish.yml"]
implementation_notes = ["Add make fmt-check, lint, test, docs-check, agent-check, and ci targets that delegate to uv under aria_nbv/ and existing docs/memory checks.", "Keep GPU, full dataset, and long experiment tests opt-in; the default CI should be reliable on a clean CPU runner.", "Add .pre-commit-config.yaml for merge-conflict checks, TOML/YAML/basic whitespace checks, Ruff format check, and Ruff lint check where feasible.", "Docs-check should render roadmap, questions, and core literature pages individually so relative bibliography paths and generated API links are checked in their real page context."]
acceptance = ["make ci exists and is documented as the local verification gate.", ".github/workflows/ci.yml runs the lightweight source/docs/agent-memory checks on PR/push.", ".pre-commit-config.yaml exists and its core checks match CI rather than adding unrelated policy.", "The CI/docs gate renders roadmap, questions, and core literature pages and catches broken generated-reference links."]
verification = ["make ci", "pre-commit run --all-files", "make qmd-frontmatter-check", "cd docs && quarto render contents/thesis/roadmap.qmd", "cd docs && quarto render contents/thesis/questions.qmd", "cd docs && quarto render contents/literature/index.qmd", "find .github/workflows -maxdepth 1 -type f -name 'ci.yml' -print"]
resolved_at = "2026-05-06"
resolution_note = "Root make ci, local pre-commit hooks, and a lightweight GitHub CI workflow now validate agents DB, QMD frontmatter, agent memory, core docs render, and Typst paper render without dataset/GPU work."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-046"
title = "Add candidate-label ordering guard tests"
description = "Add focused tests and diagnostics that prove sampled candidate order remains aligned with validity masks, rendered depths, point clouds, oracle RRI labels, offline payloads, and rollout traces."
priority = "high"
status = "resolved"
labels = ["data", "rri", "candidate-generation", "rollouts", "tests"]
issue_ids = ["issue-026", "issue-007"]
loc_min = 120
loc_expected = 260
loc_max = 620
context = ["The alignment distillation identified candidate/RRI shuffling as a large-scale generation blocker.", "Current Rerun and rollout tests cover several candidate-count and trace roundtrip cases, but no single contract test follows candidate identity through generation, oracle scoring, offline serialization, and trace export.", "This is a correctness gate before large oracle or rollout cache generation because a shuffled label silently poisons ranking supervision."]
references = ["repo:aria_nbv/aria_nbv/pose_generation/types.py", "repo:aria_nbv/aria_nbv/oracle/pipelines/scene_labels.py", "repo:aria_nbv/aria_nbv/data_handling/_offline_writer.py", "repo:aria_nbv/aria_nbv/pose_generation/rollout_trace.py"]
implementation_notes = ["Use a tiny synthetic candidate set with identity-bearing poses or monotonic synthetic scores so order inversions are obvious.", "Assert full-shell indices, compact valid indices, candidate_valid masks, candidate_scores, p3d camera rows, candidate depth rows, and serialized trace vectors agree.", "Cover at least one invalid candidate and one all-valid case.", "Keep the test independent from large ASE assets; add real-data smoke only if already available and cheap."]
acceptance = ["A targeted test fails if candidate labels are permuted relative to poses after pruning or serialization.", "RolloutTrace full-shell score vectors preserve NaN/invalid slots and selected_shell_index semantics.", "Offline writer/reader or oracle-label payload tests assert candidate pose/RRI/depth/camera row alignment.", "The test is documented as part of the M1 data/cache/oracle contract gate."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation/test_counterfactuals.py tests/data_handling/test_vin_offline_store.py tests/rri_metrics", "rg -n \"selected_shell_index|candidate_count|candidate_valid|candidate_scores|candidate_poses_world_cam\" aria_nbv/tests aria_nbv/aria_nbv"]
resolved_at = "2026-05-06"
resolution_note = "Synthetic candidate-order guards now cover compact/full-shell indices, renderer ambiguity, offline writer label/pose/depth alignment, payload roundtrip, and rollout full-shell score/RRI alignment."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-047"
title = "Create one-scene smoke tutorial"
description = "Document a portable one-scene data/cache/Rerun/VIN smoke workflow that verifies the local environment before larger oracle, VIN, or rollout work."
priority = "high"
status = "resolved"
labels = ["setup", "docs", "offline-store", "smoke", "thesis"]
issue_ids = ["issue-005", "issue-007"]
loc_min = 120
loc_expected = 260
loc_max = 620
context = ["The alignment distillation listed a one-scene data/cache/training smoke tutorial as a required repo-governance item.", "README.md now has a concise command list, but setup details are still split across setup.qmd, operator references, and local configs.", "A portable tutorial should prove that data access, offline-store validation, Rerun inspection, and a minimal VIN or rollout command work without relying on host-local paths."]
references = ["repo:README.md#important-cli-commands", "repo:docs/contents/setup.qmd", "repo:docs/contents/impl/rerun_offline_inspector.qmd", "repo:.configs/offline_only.toml", "repo:.configs/rerun_offline.toml"]
implementation_notes = ["Keep host-specific cache roots and secrets out of public docs; point to operator references for local overrides.", "Start from one available ASE scene or the current immutable VIN offline smoke store, then run nbv-summary, nbv-rerun-inspect save mode, and the synthetic rollout trace smoke.", "Make failure modes explicit: missing ASE credentials, stale offline-store version, absent mesh, or missing optional Rerun layers.", "Link from README.md and SETUP.md once SETUP.md exists."]
acceptance = ["A new contributor can run one documented smoke path from a clean checkout plus configured data access.", "The tutorial distinguishes real ASE/offline-store smoke from synthetic rollout trace smoke.", "Expected artifacts and validation commands are named, including the Rerun .rrd path.", "The docs avoid personal absolute paths and do not commit generated smoke artifacts."]
verification = ["cd docs && quarto render contents/setup.qmd contents/impl/rerun_offline_inspector.qmd", "rg -n \"one-scene|nbv-summary|nbv-rerun-inspect|nbv-rollout-trace-smoke\" README.md SETUP.md docs/contents .configs"]
resolved_at = "2026-05-06"
resolution_note = "SETUP.md and docs/contents/impl/one_scene_smoke.qmd now define the one-sample offline-store, Rerun, VIN summary, and synthetic rollout trace smoke path."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-032"
title = "Design Zarr-first rollout and Q_H store"
description = "Define a rich Zarr-first rollout/Q storage layout with lineage, masks, target crops, config hashes, and optional heavy diagnostics."
priority = "high"
status = "resolved"
labels = ["rollouts", "storage", "data", "reproducibility"]
issue_ids = ["issue-018", "issue-022"]
loc_min = 160
loc_expected = 420
loc_max = 1000
context = ["The synthesis warned that full EVL/backbone cache payloads dominate storage and that rollout storage should keep replay/training/evaluation essentials rather than duplicating raw ASE/ATEK.", "The thesis plan now makes Zarr the first storage substrate for rollouts and Q_H datasets, with PT/JSONL limited to smoke or export helpers.", "Zarr is attractive for chunked arrays but variable-length rollouts, ragged point clouds, variable candidate counts, and target crops need masks, offsets, padding, per-sample groups, or separate crop groups.", "Full meshes remain external path/hash/version references, while high-detail target mesh crops should be embedded once per target with crop metadata."]
references = ["repo:.agents/memory/state/PROJECT_STATE.md", "repo:docs/contents/thesis/questions.qmd#rq4-planning", "url:https://zarr-specs.readthedocs.io/en/latest/v3/core/index.html", "repo:aria_nbv/aria_nbv/pose_generation/counterfactuals.py"]
implementation_notes = ["Start with a Zarr layout spec and a minimal smoke writer before full-scale adoption; store selected pose sequence, full candidate tables, validity masks, scores/RRI, invalid reason codes, cumulative metrics, target records, Q_H training tensors/return targets, and lineage.", "Represent ragged candidate point clouds through offsets or omit them by default unless selected-action replay or Q_H features require them.", "Record generation_version, config hashes, seed, policy name, source store version, mesh version, target crop version, and checkpoint hash when applicable.", "Add storage-footprint summaries per generated sample and per Zarr group.", "Do not aggressively downsample high-detail target crops until an ablation proves it is safe."]
acceptance = ["A smoke rollout/Q_H store can be replayed or verified without full EVL backbone tensors or raw ASE/ATEK duplication.", "Optional heavy arrays are gated by config and absent from lean smoke payloads.", "The manifest reports bytes per field, target crop metadata, external full-mesh references, and key lineage identifiers.", "The schema explicitly covers target records, candidate tables, validity reasons, rollout lineage, Q_H training tensors, retained target crop policy, and external mesh references.", "The storage format decision is documented with explicit padding/mask/offset/group handling for ragged data and target crops.", "The layout is sufficient to train and evaluate finite-candidate Q_H."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation tests/data_handling", "rg -n \"rollout.*manifest|config_hash|generation_version|point_offsets|zarr\" aria_nbv docs .agents"]
resolved_at = "2026-05-06"
resolution_note = "Zarr-first rollout/Q_H storage and invalidity schema is drafted in public docs plus an internal reference, covering lineage, targets, candidates, masks/reasons, target crops, mesh refs, and Q_H tensors."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-034"
title = "Add LRZ Slurm and DSS-safe generation templates"
description = "Document and template LRZ AI Systems batch jobs for oracle cache generation, rollout generation, VIN training, and diagnostics with Enroot/Pyxis and DSS storage constraints."
priority = "high"
status = "resolved"
labels = ["lrz", "slurm", "storage", "cluster"]
issue_ids = ["issue-022"]
loc_min = 160
loc_expected = 380
loc_max = 900
context = ["The synthesis identified LRZ batch jobs, Slurm arrays, Enroot/Pyxis containers, and DSS quotas as required before scaling data generation.", "DSS default quotas and high storage utilization make naive multi-hundred-GB caches risky.", "The root guidance routes LRZ work through the lrz-ai-systems skill and references.", "LRZ Slurm/DSS readiness is a hard M2/M3 gate before full GT coverage rollout generation and Q_H training data production; no workstation should be assumed for thesis scale."]
references = ["repo:.agents/skills/lrz-ai-systems/SKILL.md", "repo:.agents/skills/lrz-ai-systems/references/storage-dss.md", "repo:.agents/skills/lrz-ai-systems/references/containers-pyxis.md", "url:https://lightning.ai/docs/pytorch/stable/api/lightning.pytorch.plugins.environments.SLURMEnvironment.html"]
implementation_notes = ["Use the existing lrz-ai-systems skill references rather than inventing a second scheduler story.", "Template sbatch jobs for oracle cache generation, rollout/Q_H data generation, VIN/Q_H training, and validation/diagnostics.", "Include DSS staging rules: active shards only, compression/pruning, inode awareness, final artifact sync, and no blind full-backbone caches.", "Record container/runtime assumptions for Enroot/Pyxis without committing personal runtime config.", "Include a smoke-to-full-scale checklist keyed to scene/snippet shard counts."]
acceptance = ["There are repo-local templates or references for the four major LRZ job classes.", "Templates support job arrays and deterministic shard ids.", "Storage guidance explicitly covers DSS quota/inode constraints and active-shard staging.", "The templates can stage a small trusted subset before full 100-scene/4,608-snippet generation.", "The docs do not claim LSF or another unsupported scheduler."]
verification = ["rg -n \"sbatch|srun|scancel|SLURM_ARRAY_TASK_ID|Enroot|Pyxis|DSS\" .agents docs .configs scripts", "make check-agent-memory"]
resolved_at = "2026-05-06"
resolution_note = "LRZ dry-run Slurm/Pyxis templates and DSS staging layout are documented under .configs/lrz, scripts/templates/lrz, and docs/contents/impl/lrz_dry_runs.qmd."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-057"
title = "Fix rollout Zarr Q_H replay review blockers"
description = "Resolve the critical review findings in the standalone rollouts.zarr tracer bullet before any Q_H training or scaled rollout generation consumes it."
priority = "high"
status = "resolved"
labels = ["rollouts", "zarr", "q-learning", "data", "review"]
issue_ids = ["issue-018", "issue-021", "issue-028"]
loc_min = 120
loc_expected = 320
loc_max = 760
context = ["A review of the implemented rollouts.zarr tracer bullet found four training-facing blockers: non-oracle scores can silently become trainable target-RRI labels, multi-target identity is lost between targets/rollouts/q_h rows, pose_relative_root is computed by elementwise flattened-pose subtraction, and split/lineage fields are too thin for mixed-policy or mixed-config audit.", "These are not cosmetic smoke-test gaps: Q_H training requires oracle target-RRI provenance, actor-visible target identity, correct PoseTW relative transforms, and replayable lineage before selected-action TD rows or q_h masks can be trusted.", "Synthetic smoke tests passed, so the follow-up should add negative and multi-target tests that fail on the current bugs rather than broad refactoring.", "This TODO must be closed before Q_H evidence reports treat rollouts.zarr stores as thesis-grade replay data."]
references = ["repo:aria_nbv/aria_nbv/data_handling/_rollout_zarr_store.py", "repo:aria_nbv/tests/data_handling/test_rollout_zarr_store.py", "repo:aria_nbv/aria_nbv/pose_generation/rollout_trace.py", "repo:docs/contents/impl/rollout_storage_contract.qmd", "repo:.agents/memory/history/2026/05/2026-05-06_rollout_softmax_zarr_store.md", "repo:.agents/issues.toml#issue-018"]
implementation_notes = ["Require explicit oracle target-RRI metric provenance for q_train_mask and td_reward_target_rri; do not fall back from target_rri/rri to candidate_scores for training labels.", "Preserve target identity by writing per-target target_id dictionary ids and propagating each step's rollout target_row_id into q_h/target_row_id.", "Compute pose_relative_root as a true relative transform, e.g. T_root_cam = inverse(T_world_root) @ T_world_cam, using PoseTW semantics rather than flattened subtraction.", "Persist enough per-rollout split and lineage fields to reject or audit mixed stores: split id/source, candidate/oracle/rollout config hashes, source offline-store manifest/hash, target protocol version, reason-code version, policy/branch schedule, seed, and storage/schema version.", "Strengthen validation so selected candidates must be trainable only when oracle labels are present, invalid or padded labels stay NaN, every q_h target row maps to the right target, and mixed missing-lineage stores fail with actionable errors."]
acceptance = ["A non-oracle or model-scored trace without explicit target-RRI metric provenance writes NaN target labels and q_train_mask=false, not trainable Q_H targets.", "A multi-target smoke store preserves distinct target_row_id and target_id values in targets, rollouts, steps or q_h rows, and reader/validator checks the linkage.", "pose_relative_root roundtrips as the mathematically correct root-relative PoseTW transform for at least identity and non-identity root poses.", "The rollout store records per-rollout split and lineage fields needed to audit mixed policies/configs, and validation rejects missing required lineage for non-synthetic stores.", "Regression tests cover all four review findings and fail against the current buggy behavior.", "Only after these pass may todo-052 consume rollouts.zarr as a trusted Q_H training source."]
verification = ["cd aria_nbv && uv run pytest tests/data_handling/test_rollout_zarr_store.py", "cd aria_nbv && uv run pytest tests/pose_generation/test_counterfactuals.py", "cd aria_nbv && uv run ruff check aria_nbv/data_handling/_rollout_zarr_store.py tests/data_handling/test_rollout_zarr_store.py aria_nbv/pose_generation/rollout_trace.py", "make agents-db AGENTS_ARGS='validate'"]
resolved_at = "2026-05-07"
resolution_note = "Implemented rollout Zarr hardening: explicit target-RRI provenance for q_train labels, multi-target q_h target propagation, PoseTW root-relative transforms, per-rollout split/lineage fields, validator checks, and regression tests."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-061"
title = "Implement kg-claim-check verdict logic"
description = "Add deterministic verdict, confidence, supporting_evidence, and contradicting_evidence output to kg-claim-check context packs."
priority = "high"
status = "resolved"
labels = ["kg", "litkg", "claim-check", "thesis"]
issue_ids = ["issue-025"]
loc_min = 180
loc_expected = 420
loc_max = 900
context = ["Advisor-facing claim discipline depends on kg-claim-check returning a verdict instead of a generic context pack.", "Heuristic v1 should be deterministic and source-backed; LLM-backed multi-claim checking remains deferred under issue-025."]
references = ["repo:.agents/external/litkg-rs/crates/litkg-core/src/context_pack.rs", "repo:.agents/external/litkg-rs/crates/litkg-cli/src/main.rs", "repo:.agents/external/litkg-rs/crates/litkg-cli/tests/inspect_cli.rs", "repo:.agents/references/litkg_quick_reference.md", "repo:.agents/issues.toml#issue-023", "repo:.agents/issues.toml#issue-025", "repo:.agents/issues.toml#issue-029", "repo:.agents/refactors.toml#refactor-016"]
implementation_notes = ["Populate check-only ContextPack fields: verdict, confidence, supporting_evidence, and contradicting_evidence.", "Classify trusted top-source spans with lexical overlap plus negation/limitation markers, including V0/V1 GT-OBB actor-visibility language.", "Render verdict in text output before top sources.", "Keep kg-route output behavior unchanged except for shared routing-quality hardening."]
acceptance = ["A known true V0 GT-OBB target-RRI claim returns verdict=supported with non-empty supporting_evidence.", "A false V1 GT-OBB actor-visible main-result claim returns verdict=contradicted with non-empty contradicting_evidence.", "An unsupported simulator claim returns verdict=unverifiable with low confidence.", "Text output prints the verdict and confidence for claim-check packs."]
verification = ["cd .agents/external/litkg-rs && cargo test -p litkg-core context_pack::tests::context_pack_claim_check_returns_supported_contradicted_and_unverifiable", "cd .agents/external/litkg-rs && cargo test -p litkg-cli claim_check_context_pack_reports_verdict_in_json_and_text", "make kg-claim-check KG_CLAIM=\"ARIA-NBV uses GT-OBB-cropped target RRI as V0 sanity/upper-bound\" KG_FORMAT=json", "make kg-claim-check KG_CLAIM=\"GT OBBs are actor-visible in the V1 main thesis result\" KG_FORMAT=json"]
resolved_at = "2026-05-08"
resolution_note = "Verdict logic shipped in litkg-rs commit 152f13b: ClaimVerdictParts struct (context_pack.rs:1627), claim_verdict() helper (line 1634), trusted_claim_source filter, classify_claim_span with negation detection. Smoke confirmed all three plan-grill scenarios: V0-OBB GT support claim verdict=supported(1.0); V1-actor-visible claim verdict=contradicted(0.9); Habitat-as-main-simulator verdict=unverifiable(0.2). Closes the issue-029 contract gap noted before resolution."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-066"
title = "Phase A: BM25 + Porter stemming + synonyms + Levenshtein fuzzy for kg-search"
description = "kg-search uses naive term-frequency scoring today (inspect.rs:1297 score_lexical: score as f32). A typo'd query (hierachical viewpoint) returns PB-NBV instead of Hestia because hierachical matches nothing and viewpoint frequency wins by sheer count. Replace the lexical scorer with BM25, add Porter stemming to the tokenizer, expand query tokens via a config-driven synonym table, and add a Levenshtein distance<=2 fallback that fires only on zero-exact-hits. Ships independently of Phase B (todo-067) and works even when Neo4j is unavailable."
priority = "high"
status = "resolved"
labels = ["litkg", "kg-search", "ranking", "bm25", "lexical"]
issue_ids = ["issue-025"]
loc_min = 250
loc_expected = 420
loc_max = 700
context = ["The 2026-05-11 'hierachical viewpoint' query incident exposed three lexical gaps: typo intolerance, no stemming, no synonym expansion.", "BM25 is the standard improvement over raw count: IDF penalises common terms (viewpoint) and rewards rare ones (hierarchical), and the document-length normalisation prevents long papers with many similarly-titled sections from dominating.", "Porter stemming via rust-stemmers crate (~4 KB, no transitive deps) collapses viewpoint/viewpoints and hierarchical/hierarchically.", "Synonyms live in a new [synonyms] table in .configs/litkg.toml; query tokens expand to (self + synonyms) capped at one count per token to avoid over-fire.", "Fuzzy fallback fires only on zero-exact-hits for tokens length>=5; surfaces query_fixups in JSON output."]
references = ["repo:.agents/external/litkg-rs/crates/litkg-core/src/ranking.rs", "repo:.agents/external/litkg-rs/crates/litkg-core/src/inspect.rs", "repo:.agents/external/litkg-rs/crates/litkg-core/src/config.rs", "repo:.configs/litkg.toml", "repo:scripts/kg/compact_search.jq", "repo:.agents/issues.toml#issue-025"]
implementation_notes = ["Add rust-stemmers crate (Porter English stemmer) to crates/litkg-core/Cargo.toml.", "In ranking.rs, extend the existing tokenize/is_search_stopword pipeline with stem(token) -> String; apply to BOTH index build and query tokens.", "Implement BM25 scorer in ranking.rs: score_bm25(q, d) = sum over query terms t of IDF(t) * (tf*(k1+1)) / (tf + k1*(1 - b + b*|d|/avgdl)) with k1=1.5, b=0.75 defaults.", "Compute IDF table once during index build; cache avgdl. Persist alongside the existing index data.", "Replace score_lexical at inspect.rs:1297 with score_bm25 call.", "Add [synonyms] deserialisation to config.rs; expand query tokens with synonyms after stemming.", "Add Levenshtein fuzzy fallback using strsim or inline DP impl; fire only when exact-match returned zero hits.", "Surface query_fixups in the JSON response; update scripts/kg/compact_search.jq to print fixup line.", "Regression-test on RRI / oracle / counterfactual / viewpoint queries; flag ordering changes >25 percent for manual review."]
acceptance = ["make kg-search KG_QUERY='hierachical viewpoint' returns Hestia in top 5; query_fixups field shows hierachical->hierarchical.", "make kg-search KG_QUERY='viewpoints' returns identical top-5 ordering as KG_QUERY='viewpoint' (stemming works).", "make kg-search KG_QUERY='NBV view selection' returns >=2 NBV papers in top 5 (synonym expansion).", "Existing exact-match queries (RRI, oracle) keep stable top-5 within +-1 position (BM25 regression baseline).", "Phase A works independently when Neo4j is down."]
verification = ["cd .agents/external/litkg-rs && cargo fmt --all --check && cargo test", "make kg-search KG_QUERY='hierachical viewpoint' KG_FORMAT=json | jq '.[0:5] | map({title, query_fixups})'", "make kg-search KG_QUERY='viewpoints' | head -10", "make kg-search KG_QUERY='NBV view selection' | head -10"]
resolved_at = "2026-05-12"
resolution_note = "Implemented BM25 lexical search for kg-search with Porter stemming, config-driven synonyms, typo fixups/query_fixups, lexical-only fallback, tests, and ARIA synonym config. Verified with cargo test and kg-search smoke queries on 2026-05-12."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-070"
title = "Phase B0 prerequisite: ingest Paper/DocSection/ProjectMemory nodes into Neo4j with embeddings"
description = "Verified 2026-05-11 that the Neo4j runtime contains only CodeGraphContext code symbols (1234 Function + 352 Module + 209 Class + 141 File = 1936 nodes). The JSONL export at .agents/kg/generated/neo4j-export/nodes.jsonl has 6500+ nodes including 84 Paper, ~5000 DocSection (paper sections), and ProjectMemory entries -- but these have never been loaded into the live Neo4j DB. Until they are, Phase B (todo-067) Neo4j vector search will only surface code, not papers. Pick one of two routes: (a) extend kg-ingest-docs/kg-enrich to ingest the JSONL paper+docsection+memory nodes into Neo4j and run enrich_embeddings.py on them; or (b) pivot to the JSONL-sidecar approach (write embeddings.jsonl alongside nodes.jsonl; run_kg_find reads it directly and does cosine in Rust without Neo4j)."
priority = "high"
status = "resolved"
labels = ["litkg", "neo4j", "ingestion", "semantic-prereq"]
issue_ids = ["issue-025"]
loc_min = 150
loc_expected = 350
loc_max = 700
context = ["Without this, todo-067's Hestia case (semantic query for 'hierarchical viewpoint' should find Hestia) cannot work because Hestia paper sections are not in Neo4j with embeddings.", "Route (a) Neo4j-ingest is consistent with the user's 2026-05-11 direction to use Neo4j vector index. Requires loading the JSONL bundle into Neo4j via APOC (apoc.load.jsonl) or a Rust-side loader, then running enrich_embeddings.py to add embeddings to the newly-loaded nodes.", "Route (b) JSONL-sidecar keeps semantic search Neo4j-independent. Smaller surface; faster to ship; doesn't require Neo4j Docker for query-time semantic.", "Recommended route: (a) for symmetry with existing infra; (b) only if (a) proves blocked.", "kg-ingest-docs already uses Graphiti for doc ingest, but [backends].graphiti=false today so that path is dormant."]
references = ["repo:.agents/external/litkg-rs/scripts/kg/ingest_docs.sh", "repo:.agents/external/litkg-rs/scripts/kg/enrich_embeddings.py", "repo:.agents/external/litkg-rs/crates/litkg-neo4j/src/lib.rs", "repo:.agents/kg/generated/neo4j-export/nodes.jsonl", "repo:.agents/todos.toml#todo-067", "repo:.agents/issues.toml#issue-025"]
implementation_notes = ["Audit current ingestion paths: kg-ingest-docs (Graphiti), kg-ingest-papers (full lit pipeline), kg-export-neo4j (writes bundle).", "Route (a) preferred: add a kg-load-bundle target that reads nodes.jsonl + edges.jsonl into the live Neo4j via APOC apoc.load.jsonl + parametrized CREATE; idempotent by node id.", "After loading, run enrich_embeddings.py with --include-papers / --include-docs / --include-memory flags so the new node kinds get KGEmbeddingNode label + kg_embedding property.", "Verify: MATCH (n:Paper) WHERE n.kg_embedding IS NOT NULL RETURN count(n) returns >0; same for DocSection, ProjectMemory.", "Acceptance must cover the 'hierachical viewpoint' use case end-to-end after this lands + todo-067 lands."]
acceptance = ["MATCH (n:Paper) WHERE n.kg_embedding IS NOT NULL RETURN count(n) > 50 (most curated papers covered).", "MATCH (n:DocSection) WHERE n.kg_embedding IS NOT NULL RETURN count(n) > 500.", "MATCH (n:ProjectMemory) WHERE n.kg_embedding IS NOT NULL RETURN count(n) > 30.", "Manual Cypher vector-search smoke for 'hierarchical viewpoint' (using ollama-computed embedding) returns Hestia or PB-NBV PaperSection in top 5 -- NOT just code symbols.", "make kg-refresh-light (or a new kg-refresh-full) updates these nodes incrementally."]
verification = ["curl -sSf -u neo4j:litkglocal -H 'Content-Type: application/json' -X POST 'http://127.0.0.1:7474/db/neo4j/tx/commit' -d '{\"statements\":[{\"statement\":\"MATCH (n:KGEmbeddingNode) WITH labels(n) AS lbls, count(n) AS cnt RETURN lbls, cnt ORDER BY cnt DESC\"}]}' | jq '.results[].data'", "Should show Paper/DocSection/ProjectMemory rows alongside the existing code-symbol rows."]
resolved_at = "2026-05-12"
resolution_note = "Implemented the Neo4j bundle loader path, loaded 9711 litkg export nodes and 15516 edges into live Neo4j with a LitkgNode marker/index, extended enrichment to Paper/DocSection/ProjectMemory/content nodes, and embedded 6306/6306 nodes on 2026-05-12."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-067"
title = "Phase B: Neo4j vector index for true semantic kg-search (depends on todo-070 paper-ingest gap)"
description = "Embeddings are computed by enrich_embeddings.py and the vector index kg_embedding_index_2560 already exists on KGEmbeddingNode.kg_embedding (HNSW cosine, dim 2560, 1936 nodes today). BUT verified 2026-05-11 that only CodeGraphContext code symbols are embedded; zero Paper/PaperSection/DocSection/ProjectMemory nodes are in the runtime DB. Wiring db.index.vector.queryNodes into run_kg_find works (demoed via raw Cypher) but only surfaces code symbols until todo-070 closes the paper-ingest gap. Blend with Phase A lexical (BM25) at alpha=0.4 by default. Falls back to lexical-only when Neo4j or Ollama is unreachable, with explicit search_mode surfaced in the response."
priority = "high"
status = "resolved"
labels = ["litkg", "kg-search", "neo4j", "embeddings", "semantic"]
issue_ids = ["issue-025"]
loc_min = 350
loc_expected = 600
loc_max = 1000
context = ["User confirmed 2026-05-11 that Neo4j vector index is the right home for embedding cosine; soft dependency on make kg-up is acceptable; lexical fallback always available.", "enrich_embeddings.py already creates the vector index kg_embedding_index_2560 (HNSW cosine, dim 2560) at line 502 and writes node embeddings to Neo4j; this Phase B prerequisite is DONE.", "Index targets nodes labeled KGEmbeddingNode with property kg_embedding (not :Paper or :DocSection directly; the embedder applies KGEmbeddingNode as a marker label).", "Verified 2026-05-11: the live Neo4j contains 1936 KGEmbeddingNode entries, all code symbols (1234 Function, 352 Module, 209 Class, 141 File). Zero Paper, PaperSection, DocSection, ProjectMemory nodes are embedded. todo-070 closes that gap.", "Query path: probe bolt://127.0.0.1:7687, compute query embedding via ollama HTTP, run CALL db.index.vector.queryNodes('kg_embedding_index_2560', $k, $qvec) Cypher, blend with BM25.", "Manual smoke 2026-05-11 with query 'hierachical viewpoint' returned 5 code-symbol hits with cosine ~0.74 -- proves the query path works end-to-end; gap is the missing paper coverage.", "Auto-refresh hook keeps Ollama-up gated; add optional KG_NEO4J_AUTO_UP=1 for symmetric Neo4j warm-start at session Stop.", "Surface search_mode (hybrid|lexical_only) and mode_reason (neo4j_unreachable|ollama_unreachable|paper_coverage_missing) in the JSON response so agents always know which retrieval path produced the hits."]
references = ["repo:.agents/external/litkg-rs/crates/litkg-cli/src/main.rs", "repo:.agents/external/litkg-rs/crates/litkg-neo4j/src/lib.rs", "repo:.agents/external/litkg-rs/crates/litkg-core/src/ranking.rs", "repo:.agents/external/litkg-rs/scripts/kg/enrich_embeddings.py", "repo:scripts/kg/auto_refresh.sh", "repo:scripts/kg/compact_search.jq", "repo:.agents/issues.toml#issue-025", "repo:.agents/todos.toml#todo-066"]
implementation_notes = ["B1 (DONE): scripts/kg/enrich_embeddings.py:502 already creates kg_embedding_index_2560 (CREATE VECTOR INDEX IF NOT EXISTS, idempotent). No work needed.", "B0 prereq (filed as todo-070): get paper/docsection/memory nodes embedded in Neo4j before B2 is fully useful for literature search.", "B2: in crates/litkg-cli/src/main.rs run_kg_find, probe bolt://127.0.0.1:7687; if down, set search_mode=lexical_only and skip to Phase A.", "B2: compute query embedding via the ollama HTTP path (use POST /api/embeddings with model=qwen3-embedding:4b; either factor scripts/kg/ollama_http.py logic into a reusable Rust client OR shell out for the first iteration).", "B2: in crates/litkg-neo4j/src/lib.rs add pub fn query_vector_index(client, index_name, k, qvec) -> Vec<(NodeId, f32)>; use the existing Bolt connection pool.", "B2: blend lexical and cosine: final = alpha*bm25 + (1-alpha)*cosine, alpha=0.4 (semantic-leaning); tunable via [context_pack].lexical_weight.", "B2: emit search_mode (hybrid|lexical_only) and mode_reason fields on the response. When todo-070 isn't yet done, can return search_mode=hybrid_code_only to be transparent about partial coverage.", "B3: add KG_NEO4J_AUTO_UP=1 env opt-in to scripts/kg/auto_refresh.sh; when set, warm-start make kg-up if Neo4j not running and Docker is available.", "B4: scripts/kg/compact_search.jq prints a one-line footer showing search_mode + mode_reason.", "Test: scenario tests in inspect_cli.rs covering hybrid + lexical-fallback + ollama-down paths.", "Cache the last query embedding to keep repeat queries fast (~80 ms saved per repeat)."]
acceptance = ["make kg-up + make kg-refresh-light + make kg-search KG_QUERY='how do hierarchical NBV methods decompose target proposal from pose realization' returns Hestia in top 3 (zero token overlap).", "make kg-search KG_QUERY='RRI' KG_FORMAT=json | jq '.[0].search_mode' returns 'hybrid'.", "make kg-down then make kg-search KG_QUERY='RRI' KG_FORMAT=json | jq '.[0].search_mode' returns 'lexical_only' with mode_reason 'neo4j_unreachable'.", "compact_search.jq footer shows the search_mode and any query_fixups.", "Existing kg-search test suite passes."]
verification = ["cd .agents/external/litkg-rs && cargo fmt --all --check && cargo test", "make kg-up && make kg-refresh-light", "make kg-search KG_QUERY='how do hierarchical NBV methods decompose target proposal from pose realization' KG_FORMAT=json | jq '{mode: .[0].search_mode, top: [.[0:3] | .[].title]}'", "make kg-down && make kg-search KG_QUERY='RRI' KG_FORMAT=json | jq -r '.[0].search_mode'"]
resolved_at = "2026-05-12"
resolution_note = "Implemented kg-search hybrid mode with Ollama query embeddings, Neo4j vector-index lookup, BM25/cosine blending, --lexical-only/--alpha controls, explicit search_mode/mode_reason fallback, paper-coverage guard, wrapper JSON output, and regression tests. Runtime smoke on 2026-05-12 returned Hestia for the hierarchical NBV semantic query and lexical fallback when Neo4j is unreachable."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-072"
title = "Hybrid kg-search overrides paper authority to 'semantic' instead of consulting [authority_tiers]"
description = "graph_hit_from_vector in crates/litkg-cli/src/main.rs hardcodes authority='semantic' for every Neo4j vector-index hit. Combined with todo-062's path-tier fix, this means paper:vin-nbv:section:* now carries authority=canonical in lexical-only mode but reverts to authority=semantic the moment hybrid kicks in. The verdict gate (context_pack.rs:1727 trusted_claim_source) accepts 'canonical|active|canonical_memory|docs' but not 'semantic', so the hybrid path is verdict-blind for paper hits."
priority = "medium"
status = "resolved"
labels = ["litkg", "hybrid-search", "authority", "ranking"]
issue_ids = ["issue-025"]
loc_min = 40
loc_expected = 90
loc_max = 200
context = ["Confirmed 2026-05-13: with todo-062 landed, paper:vin-nbv:section:6 returns authority=canonical via --lexical-only and authority=semantic via hybrid (default).", "graph_hit_from_vector currently sets WeightedScore { score_lexical: 0.0, score_authority: 1.0, score_freshness: 1.0, authority: 'semantic'.into(), ... } regardless of node properties.", "The Neo4j vector-index query already returns repo_path/source_path in the row; the hybrid path could call calculate_weighted_score with that path and the configured authority_tiers to assign a real label."]
references = ["repo:.agents/external/litkg-rs/crates/litkg-cli/src/main.rs", "repo:.agents/external/litkg-rs/crates/litkg-core/src/ranking.rs", "repo:.agents/external/litkg-rs/crates/litkg-core/src/context_pack.rs"]
implementation_notes = ["In graph_hit_from_vector, when hit.repo_path is set, build a Path and call calculate_weighted_score(path, hit.score, config.authority_tiers.as_ref()) instead of synthesizing a flat semantic label.", "Where repo_path is missing (e.g. CodeGraphContext nodes that may not carry it yet), keep the synthetic 'semantic' label as a fallback.", "Add a unit test that constructs a Neo4jVectorHit with repo_path='docs/contents/literature/vin_nbv.qmd' and asserts the converted GraphSearchHit.rank.authority == 'canonical' when authority_tiers tier the path at 1.5+."]
acceptance = ["make kg-search KG_QUERY='VIN-NBV RRI' KG_FORMAT=json | jq '.results[] | select(.node_id|startswith(\"paper:vin-nbv\")) | .authority' returns 'canonical' in hybrid mode (not 'semantic').", "Lexical-only mode result for the same query is unchanged.", "An integration test in inspect_cli.rs covers the hybrid-side authority assignment."]
verification = ["cd .agents/external/litkg-rs && cargo test", "make kg-search KG_QUERY='VIN-NBV RRI' KG_LIMIT=5 KG_FORMAT=json | jq '.results[] | select(.node_id|startswith(\"paper:\")) | {id: .node_id, authority}'"]
resolved_at = "2026-05-15"
resolution_note = "Hybrid kg-search graph_hit_from_vector now calls calculate_weighted_score(path, cosine, authority_tiers) when Neo4jVectorHit.repo_path is Some, inheriting the same authority label that the lexical path assigns. Verified live: make kg-search KG_QUERY='VIN-NBV RRI' returns paper:vin-nbv with authority=canonical, score_authority=1.5 (was 'semantic', 1.0). Synthetic 'semantic' fallback retained only when repo_path is None. Shipped 2026-05-13."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-073"
title = "kg-claim-check verdict pipeline excludes curated literature even when tier is canonical"
description = "todo-062 lifted literature notes (docs/contents/literature/**/*.qmd) to authority=canonical. The verdict gate at trusted_claim_source admits the canonical label. But top_sources at context_pack.rs:267 selects only 8 paths and ranks them via a freshness-weighted score that currently floors literature notes below thesis/code, so vin_nbv.qmd never surfaces in top_sources for a VIN-NBV-RRI claim and the verdict comes back unverifiable. Need either a literature-aware top_sources boost or a path-prefix bypass for canonical literature when the claim text matches the file's terms."
priority = "medium"
status = "resolved"
labels = ["litkg", "claim-check", "verdict", "ranking"]
issue_ids = ["issue-025"]
loc_min = 60
loc_expected = 140
loc_max = 300
context = ["Confirmed 2026-05-13: docs/contents/literature/vin_nbv.qmd contains explicit RRI definition and VIN-NBV objective; reading it directly satisfies the claim. select_spans matches 'RRI', 'VIN-NBV', 'reconstruction' terms correctly. But top_sources for the claim returns only thesis/code/backlog files; literature is dropped before the verdict layer sees it.", "Likely cause: the freshness multiplier in calculate_weighted_score halves a file's score every 30 days. Curated literature notes are stable and may date back months, dropping their score_final below the [context_pack].min_top_source_score = 0.4 floor.", "Knob options: (a) bypass the freshness penalty for files matching docs/contents/literature/**/*.qmd; (b) raise their score floor; (c) admit canonical-tier files to top_sources regardless of freshness when claim terms overlap."]
references = ["repo:.agents/external/litkg-rs/crates/litkg-core/src/context_pack.rs", "repo:.agents/external/litkg-rs/crates/litkg-core/src/ranking.rs", "repo:.configs/litkg.toml"]
implementation_notes = ["Diagnose first: instrument top_sources to print the score breakdown for vin_nbv.qmd on the canonical RRI claim. Confirm whether freshness or term-overlap is the gating factor.", "Likely fix: in top_sources scoring, if a file matches authority=canonical AND has any term overlap with the claim, admit it even when freshness is low.", "Add a regression test: build a ContextPackRequest with task='claim-check: VIN-NBV introduces Relative Reconstruction Improvement' and assert at least one top_source path starts with 'docs/contents/literature/'."]
acceptance = ["make kg-claim-check KG_CLAIM='VIN-NBV introduces Relative Reconstruction Improvement (RRI), an oracle label computed from point-mesh reconstruction-error reduction after adding a query view' KG_FORMAT=json returns verdict='supported' with confidence > 0.6.", "Top_sources for the same claim includes at least one docs/contents/literature/*.qmd entry."]
verification = ["cd .agents/external/litkg-rs && cargo test", "make kg-claim-check KG_CLAIM='VIN-NBV introduces Relative Reconstruction Improvement (RRI)' KG_FORMAT=json | jq '{verdict, confidence, top_lit: [.top_sources[] | select(.path|test(\"contents/literature\"))]}'"]
resolved_at = "2026-05-15"
resolution_note = "top_sources now uses a two-pass admission: pass 1 fills 8 slots score-ordered (preserving the owning-route precedence test), pass 2 rescues up to 4 additional canonical-tier spans with term overlap that would otherwise be dropped. apply_confidence_floor keeps canonical entries even when score_final is below the floor. configured_source_paths cap now preserves canonical-tier paths first when the alphabetical truncation kicks in. truncate_spans_to_budget reserves CANONICAL_RESERVED_TOKENS=2400 for canonical spans before the budget filter. Verified: docs/contents/literature/vin_nbv.qmd now surfaces in evidence_spans for the RRI claim (was absent before); unit test context_pack_admits_canonical_literature_into_top_sources_and_verdict covers the synthetic case. The live RRI claim still returns verdict=unverifiable due to classify_claim_span's narrow shape gate (filed as todo-074)."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-074"
title = "Generalize classify_claim_span beyond V0/V1/Habitat shapes"
description = "claim_verdict admits canonical-tier evidence (todo-072 + todo-073 closed the data path) but classify_claim_span only returns ClaimRelation::Supports for three hand-coded claim shapes: positive_v0_gt_rri_claim (V0 GT-OBB sanity/upper-bound), v1_gt_actor_visible_claim (V1 actor-visible contradiction), deferred_main_simulator_claim (Habitat-as-main-simulator). For any other claim, the fallback is overlap>=0.45 over a 3-line span window, which requires 7 of 16 typical claim terms in a 3-line snippet -- effectively unreachable for diverse research claims. Live RRI test claim ('VIN-NBV introduces RRI...') returns unverifiable even though paper:vin-nbv now ranks authority=canonical in hybrid search and docs/contents/literature/vin_nbv.qmd reaches evidence_spans."
priority = "medium"
status = "resolved"
labels = ["litkg", "claim-check", "verdict", "heuristic"]
issue_ids = ["issue-025"]
loc_min = 80
loc_expected = 220
loc_max = 500
context = ["Resolved 2026-05-13 via plan-grill: data path is correct; the gate is the heuristic.", "Three options: (a) expand the shape-specific Supports patterns to cover RRI, oracle-label, hierarchical-NBV, etc.; (b) lower the fallback overlap threshold from 0.45 to ~0.25 and add a length-normalised match-density signal; (c) wire an LLM call (gemma4 via Ollama) for fuzzy claim-evidence alignment when the heuristic falls through to Neutral.", "Option (c) is the most general but adds latency and a Mac-tunnel dependency to the claim-check path; option (a) is the smallest immediate diff; option (b) risks false positives on tangentially related spans.", "Whichever lands needs a regression suite spanning the existing three shapes plus 3-5 new claims (RRI definition, hierarchical NBV decomposition, ARIA-NBV vs Hestia objective)."]
references = ["repo:.agents/external/litkg-rs/crates/litkg-core/src/context_pack.rs:1818", "repo:.agents/external/litkg-rs/crates/litkg-core/src/context_pack.rs:1820-1856", "repo:.agents/resolved.toml#todo-061", "repo:.agents/memory/history/2026/05/2026-05-08*"]
implementation_notes = ["Start with option (a): add a positive_rri_definition_claim helper and a positive_hierarchical_nbv_claim helper. Pair each with evidence-text predicates (e.g. evidence mentions 'RRI' AND 'reconstruction' AND ('oracle' OR 'label')).", "Defer option (c) until at least two more shapes have been hand-coded and the pattern is clear.", "Add a regression test array in context_pack.rs tests covering RRI definition, V0 sanity (existing), V1 actor-visible (existing), Habitat (existing), Hestia hierarchical decomposition. Each asserts the expected verdict label."]
acceptance = ["make kg-claim-check KG_CLAIM='VIN-NBV introduces Relative Reconstruction Improvement (RRI), an oracle label computed from point-mesh reconstruction-error reduction after adding a query view' returns verdict='supported' with confidence > 0.6.", "Existing three shape tests in context_pack::tests::context_pack_claim_check_returns_supported_contradicted_and_unverifiable still pass.", "At least one new test asserts the RRI definition claim resolves to supported."]
verification = ["cd .agents/external/litkg-rs && cargo test context_pack::tests::context_pack_claim_check", "make kg-claim-check KG_CLAIM='VIN-NBV introduces Relative Reconstruction Improvement (RRI)' KG_FORMAT=json | jq '{verdict, confidence}'"]
resolved_at = "2026-05-15"
resolution_note = "Verdict canary closes via two-part fix in litkg-rs. (A) top_sources() adds a literature-class reservation pass (cap +2) between the score-ordered primary and the canonical rescue; literature-class = paths under docs/contents/literature/**, docs/literature/tex-src/**, or docs/references.bib. (B) classify_claim_span() gains three positive_* / evidence_* helper pairs for RRI definition, hierarchical NBV decomposition, and ARIA-NBV objective claims, slotted into the Supports ladder above the generic overlap>=0.45 fallback. deferred_main_simulator_claim() picks up 'only if' / 'thesis-grade' markers so Habitat's unverifiable verdict survives the broader top_sources admission. New unit test context_pack_claim_check_handles_diverse_research_shapes covers RRI / hierarchical NBV / ARIA-NBV objective + the three previously-resolved shapes. Live verification 2026-05-13: RRI canary -> supported(1.0), V0 GT-OBB -> supported(1.0), V1 actor-visible -> contradicted(0.9), Habitat -> unverifiable(0.2). Doctor stays all-green."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-076"
title = "Add LRZ rollout shard-manifest CLI and atomic promotion"
description = "Make nbv-build-rollouts consume deterministic shard manifests and write each rollout shard through temp, validation, atomic promote, and completion markers before any multi-step offline sample scale-up."
priority = "high"
status = "resolved"
labels = ["rollouts", "cluster", "lrz", "data-generation", "storage"]
issue_ids = ["issue-022", "issue-018"]
loc_min = 180
loc_expected = 420
loc_max = 900
context = ["The current nbv-build-rollouts CLI loads one config path and writes one rollout store, which is sufficient for smoke tests but not LRZ array jobs.", "scripts/templates/lrz/rollout_generation_dry_run.sbatch still uses placeholder shard arguments and states that it does not generate rollout traces or Q_H data.", "Before large multi-step offline sample generation, each Slurm array task must own one final rollout shard path, skip only validated completed shards, write to a temp directory, validate, atomically promote, and write a completion marker plus owner sidecar.", "This is a prerequisite for high-throughput DSS-safe rollout generation rather than a training-model concern."]
references = ["repo:aria_nbv/aria_nbv/rollouts/cli.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:scripts/templates/lrz/rollout_generation_dry_run.sbatch", "repo:.configs/build_rollouts_v1_smoke.toml", "repo:.agents/issues.toml#issue-022"]
implementation_notes = ["Extend the rollout CLI with shard-oriented options such as --shard-manifest, --shard-id, --output-tmp, --output-final, and --dry-run while retaining the existing config-path smoke path.", "Define a compact JSONL shard manifest over source rows, scene split, target/recipe/seed ranges, output shard id, retention profile, and expected source manifest hash.", "Write shard outputs to a temp path, run rollout store validation, replace or rename into the final shard directory atomically where the filesystem allows, then write _SUCCESS.json and _owner.json.", "On rerun, skip only shards with a valid store, matching manifest hash, and success marker; failed temp paths must not be treated as complete.", "Update the LRZ sbatch template to call the real entrypoint and pass Slurm array task metadata into owner/runtime provenance."]
acceptance = ["A dry-run command can print the resolved shard manifest row and output paths without loading source data.", "One smoke shard can be generated through temp -> validate -> final -> _SUCCESS.json using the same CLI surface intended for LRZ.", "Rerunning the same shard skips only after validating the existing final shard and matching manifest/provenance.", "The owner sidecar records Slurm job/task ids when present, config hash, source manifest hash, shard id, retention profile, and runtime provenance.", "The existing single-config smoke flow remains available for local development."]
verification = ["cd aria_nbv && uv run nbv-build-rollouts --config-path ../.configs/build_rollouts_v1_smoke.toml --dry-run", "cd aria_nbv && uv run pytest tests/rollouts", "rg -n \"shard-manifest|output-tmp|output-final|_SUCCESS|_owner|SLURM_ARRAY_TASK_ID\" aria_nbv/aria_nbv/rollouts scripts/templates/lrz .configs .agents"]
resolved_at = "2026-05-15"
resolution_note = "Implemented shard manifest planning, shard-aware nbv-build-rollouts, strict temp-to-final promotion with _owner/_SUCCESS sidecars, and LRZ template wiring."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-077"
title = "Populate VIN source-shard lineage in rollout stores"
description = "Carry VIN offline shard id and shard-local row into rollout source lineage so rollout shards can be joined, debugged, and regenerated without scanning only global sample indices."
priority = "high"
status = "resolved"
labels = ["rollouts", "data", "lineage", "zarr", "throughput"]
issue_ids = ["issue-018", "issue-022"]
loc_min = 60
loc_expected = 160
loc_max = 360
context = ["Rollout Zarr already has sources/source_shard_id and sources/source_shard_row fields, but the rollout writer currently fills lineage from VinOfflineSample fields that do not expose the backing VinOfflineIndexRecord shard id and row.", "The VIN offline dataset owns sample_index.jsonl with global sample index, scene, snippet, split, shard_id, and row; large rollout shards need those fields persisted to avoid ambiguous source joins.", "This is a small blocker before scale generation because source-row provenance, DSS debugging, and partial regeneration should not depend on reconstructing shard membership externally."]
references = ["repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/aria_nbv/data_handling/_offline_format.py", "repo:aria_nbv/aria_nbv/data_handling/_offline_dataset.py", "repo:aria_nbv/tests/rollouts/test_zarr_store.py", "repo:.agents/issues.toml#issue-018"]
implementation_notes = ["Expose or carry the selected VinOfflineIndexRecord fields through VinOfflineSample or an internal rollout-writer lookup without changing the public VIN training batch DTO.", "Set RolloutLineage.source_shard_id and source_shard_row from the source record when building each RolloutZarrRecord.", "Add validation or tests that rollout sources contain non-placeholder shard ids and rows when the source dataset provides them.", "Keep source_row_id as the global sample index; do not replace it with shard-local row ids."]
acceptance = ["A rollout store built from a VIN offline dataset records source_shard_id and source_shard_row for every sources row.", "Manifest source_coverage includes the same source shard id and shard-local row values.", "Validation fails or warns for missing shard lineage when the source dataset is known to provide it.", "Existing tests using synthetic rollout records can still use placeholder shard lineage intentionally."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_zarr_store.py tests/rollouts/test_dataset_writer.py tests/data_handling/test_vin_offline_store.py", "rg -n \"source_shard_id|source_shard_row|VinOfflineIndexRecord\" aria_nbv/aria_nbv/rollouts aria_nbv/aria_nbv/data_handling aria_nbv/tests/rollouts"]
resolved_at = "2026-05-15"
resolution_note = "Implemented VinOfflineSample source_shard_id/source_shard_row propagation, rollout lineage persistence, manifest source_shard coverage, and validation for missing source shard lineage."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-079"
title = "Implement ASE-depth-root RRI eval stream and root-normalized rollout rewards"
description = "Finish and validate the oracle label contract that builds P_eval from ASE observed-prefix GT depth roots plus rendered counterfactual candidates, while keeping MPS semi-dense actor-visible only."
priority = "high"
status = "resolved"
labels = ["rri", "depth", "rollouts", "oracle", "contracts", "thesis"]
issue_ids = ["issue-031", "issue-007", "issue-010", "issue-018"]
loc_min = 260
loc_expected = 700
loc_max = 1500
context = ["GPT5.5 Pro's RRI mismatch review and follow-up local inspection agreed that semi-dense-root RRI biases first counterfactual steps because later steps compare dense candidate views against dense-fused history.", "The chosen thesis-core lane uses ASE `rgb/distance_m` GT depth for logged observed-prefix root frames, rendered PyTorch3D depth for counterfactual candidates, canonical fusion before scoring, and root-normalized additive gain for rollout/Q_H rewards.", "ASE depth is ray distance in metres after ATEK preprocessing; it must use EFM3D ray-distance unprojection and must not be passed through the PyTorch3D z-depth unprojection path.", "Actor-visible geometry remains MPS semi-dense/EVL/observed target data. GT depth, GT meshes, GT crops, and rendered candidate points are oracle label/evaluation assets."]
references = ["repo:.agents/issues.toml#issue-031", "repo:.agents/work/rri-metrics/dense-semidense-rri-missmatch.md", "repo:aria_nbv/aria_nbv/rri_metrics/eval_pointclouds.py", "repo:aria_nbv/aria_nbv/rri_metrics/oracle_rri.py", "repo:aria_nbv/aria_nbv/pose_generation/counterfactuals.py", "repo:aria_nbv/aria_nbv/pose_generation/target_counterfactuals.py", "repo:aria_nbv/aria_nbv/data_handling/efm_views.py", "repo:external/efm3d/efm3d/utils/depth.py", "url:https://facebookresearch.github.io/projectaria_tools/docs/open_datasets/aria_synthetic_environments_dataset/ase_data_format", "bib:ProjectAria-ASE-2025", "bib:EFM3D-straub2024"]
implementation_notes = ["Keep `ase_gt_depth_root` as the default eval point-cloud source for scene and target rollout scorers; keep `legacy_semidense_root` explicitly labeled as diagnostic.", "Build root eval points from all observed RGB/depth frames up to the rollout reference pose; for the default final-pose root this is the full snippet.", "Use `dist_im_to_point_cloud_im` for ASE ray-distance maps, filter finite depths >0 and <= zfar/default 20m, transform camera->rig->world with PoseTW, then canonical-fuse root/history points.", "Score rendered candidate point clouds against the canonical current eval state; persist state-relative RRI, root-normalized gain, root error, and log-error gain diagnostics.", "Add rendered-logged-depth root as a future parity ablation only after it can share renderer/backprojection lineage with candidate renders."]
acceptance = ["Default scene and target oracle rollout scorers no longer use MPS semi-dense points as current evaluation geometry.", "Missing `rgb/distance_m` fails explicitly for ASE-depth-root mode instead of silently falling back to semi-dense.", "Observed-prefix root frame selection excludes frames after a non-final reference pose.", "Root-normalized gain is the default rollout score, while state-relative RRI remains present as a diagnostic metric.", "Legacy semi-dense-root RRI can still be requested explicitly for comparisons.", "Targeted tests cover ASE depth root construction, missing-depth failure, observed-prefix frame policy, canonical fusion, and root-normalized return preference."]
verification = ["cd aria_nbv && uv run pytest tests/rri_metrics/test_eval_pointclouds.py tests/rri_metrics/test_rollout_metrics.py", "cd aria_nbv && uv run pytest tests/pose_generation/test_counterfactuals.py", "cd aria_nbv && uv run ruff check aria_nbv/rri_metrics aria_nbv/pose_generation tests/rri_metrics/test_eval_pointclouds.py tests/rri_metrics/test_rollout_metrics.py tests/pose_generation/test_counterfactuals.py", "make agents-db AGENTS_ARGS='validate'", "make agents-db"]
resolved_at = "2026-05-18"
resolution_note = "ASE-depth-root RRI eval stream and root-normalized rollout reward defaults are implemented and validated by targeted eval_pointclouds, rollout_metrics, and counterfactual tests. Remaining downstream lineage/audit propagation stays tracked by issue-031 and related target/rollout todos."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-093"
title = "Resolve inline thesis TODO comments"
description = "Convert the plain inline TODO comments in active Typst thesis sections into advisor-ready prose, equations, and Mermaid diagram updates."
priority = "high"
status = "resolved"
labels = ["thesis", "typst", "docs", "mermaid"]
issue_ids = ["issue-027"]
loc_min = 80
loc_expected = 220
loc_max = 520
context = ["The active thesis sections contain plain inline TODO comments for geometric-learning equations, figure layout, missing state-space definitions, architecture ladder coverage, stacked target-RRI equations, table-heavy design scaffolds, explicit WIP marking, descriptor-symbol compression, directional-memory semantics, CORAL adaptation equations, and candidate-query diagram legibility.", "The explicit #validation_todo/#decision_todo/#research_todo draft markers are future evidence gates and should remain until their M1-M8 evidence exists.", "The immediate cleanup target is to remove every plain // TODO comment returned by `rg -n \"//\\\\s*TODO\" docs/typst/thesis/sections docs/typst/thesis/main.typ` by resolving local prose/equation/diagram clarity without inventing final experiment results."]
references = ["repo:.omx/context/thesis-inline-todos-20260618T215319Z.md", "repo:docs/typst/thesis/sections/03-01-formal-state.typ", "repo:docs/typst/thesis/sections/02-02-geometric-learning.typ", "repo:docs/typst/thesis/sections/03-02-data-generation.typ", "repo:docs/typst/thesis/sections/03-method.typ", "repo:docs/typst/thesis/figures/qh_actor_oracle_contract.mmd", "repo:docs/typst/thesis/figures/qh_vin_gnn_architecture.mmd"]
implementation_notes = ["Add compact equations or equation-backed prose for candidate-row equivariance, mask isolation, and local-frame/gauge structure in the geometric-learning theory section.", "Define cal(S)^hist, cal(S)^cf0, and cal(S)^oracle before the NBV tuple by introducing the shared #eqs.rl.s_hist/#eqs.rl.s_cf0/#eqs.rl.s_oracle equations.", "Convert raw WIP/internal comments around design-contract tables into explicit thesis marker prose or approved draft-marker surfaces.", "Compress table-heavy descriptor material using shared symbols and equations where possible.", "Split the target-RRI reward, finite-horizon return, endpoint gain, and log gain equations into separate conceptual paragraphs with source roles and adaptation caveats.", "Clarify directional-memory domains on S^2, weight semantics, moment/SH alternatives, and the candidate novelty query without claiming implemented performance evidence.", "Document ARIA-NBV's CORAL adaptation as empirical quantile binning, CORAL threshold levels, class probabilities, ordinal expectation, and optional monotone bin-value calibration.", "Update the two Mermaid diagrams only enough to address layout and legibility; preserve actor/oracle and candidate-query contracts."]
acceptance = ["The exact extraction command `rg -n \"//\\\\s*TODO\" docs/typst/thesis/sections docs/typst/thesis/main.typ` returns zero matches.", "The future #*_todo marker gates remain available for missing final evidence and advisor-facing protocol decisions.", "The actor/oracle and candidate-query Mermaid sources lint and render locally to the exact PDFs consumed by Typst.", "The thesis Typst surface compiles far enough to validate the edited sections and figure inclusions."]
verification = ["make agents-db AGENTS_ARGS='validate'", "make agents-db", "rg -n \"//\\s*TODO\" docs/typst/thesis/sections docs/typst/thesis/main.typ", "aria_nbv/.venv/bin/python tools/mermaid/scripts/aria_mermaid_lint.py docs/typst/thesis/figures/qh_actor_oracle_contract.mmd docs/typst/thesis/figures/qh_vin_gnn_architecture.mmd", "PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable npx --yes @mermaid-js/mermaid-cli -i docs/typst/thesis/figures/qh_actor_oracle_contract.mmd -o docs/typst/thesis/figures/qh_actor_oracle_contract.pdf -b white -t default --pdfFit", "PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable npx --yes @mermaid-js/mermaid-cli -i docs/typst/thesis/figures/qh_vin_gnn_architecture.mmd -o docs/typst/thesis/figures/qh_vin_gnn_architecture.pdf -b white -t default --pdfFit", "cd docs && typst compile typst/thesis/main.typ /tmp/aria-nbv-thesis-inline-todos.pdf --root .", "make agents-db AGENTS_ARGS='resolve todo todo-093 --note \"Resolved plain inline thesis TODO comments; verified DB, grep, Mermaid lint/render, Typst compile, and debrief.\"'", "make check-agent-memory"]
resolved_at = "2026-06-19"
resolution_note = "Resolved plain inline thesis TODO comments; verified agents DB, zero-match TODO grep, Mermaid lint/render, Typst compile, visual figure QA, and native debrief."
resolved_from = ".agents/todos.toml"

[[todo]]
id = "todo-094"
title = "Index remaining thesis draft-marker gates"
description = "Keep all explicit thesis #*_todo markers mapped to agents DB owners so final-writing, protocol-freeze, evidence-refresh, and bridge-scope gates are not lost after inline TODO cleanup."
priority = "high"
status = "resolved"
labels = ["thesis", "typst", "agents-db", "evidence-gates"]
issue_ids = ["issue-027", "issue-013", "issue-020", "issue-028", "issue-032"]
loc_min = 40
loc_expected = 160
loc_max = 360
context = ["After resolving plain // TODO comments, the active thesis seed still contains intentional #question_todo/#decision_todo/#validation_todo/#research_todo/#impl_todo/#conflict_todo marker gates.", "These markers are not local prose defects; they represent final-writing, protocol-freeze, manifest-regeneration, ablation-evidence, implementation-audit, and bridge-scope decisions that must remain visible until the relevant evidence exists.", "This record indexes the marker inventory and maps it to existing thesis-seed, evidence-registry, target/RRI, Q_H, rollout, and final-writing backlog owners instead of deleting or prematurely resolving the markers."]
references = ["repo:.omx/context/thesis-marker-todos-20260618T224945Z.md", "repo:docs/typst/thesis/main.typ#draft-markers", "repo:docs/typst/thesis/sections/01-introduction.typ#draft-markers", "repo:docs/typst/thesis/sections/02-background.typ#draft-markers", "repo:docs/typst/thesis/sections/02-02-geometric-learning.typ#draft-markers", "repo:docs/typst/thesis/sections/03-02-data-generation.typ#draft-markers", "repo:docs/typst/thesis/sections/03-method.typ#draft-markers", "repo:docs/typst/thesis/sections/04-evaluation.typ#draft-markers", "repo:docs/typst/thesis/sections/05-conclusion.typ#draft-markers", "repo:docs/typst/thesis/sections/06-draft-open-work.typ#draft-markers", "repo:.agents/todos.toml#todo-018", "repo:.agents/todos.toml#todo-037", "repo:.agents/todos.toml#todo-038", "repo:.agents/todos.toml#todo-048", "repo:.agents/todos.toml#todo-049", "repo:.agents/todos.toml#todo-050", "repo:.agents/todos.toml#todo-052", "repo:.agents/todos.toml#todo-055", "repo:.agents/todos.toml#todo-080", "repo:.agents/todos.toml#todo-084", "repo:.agents/todos.toml#todo-085", "repo:.agents/todos.toml#todo-087", "repo:.agents/todos.toml#todo-091", "repo:.agents/todos.toml#todo-092"]
implementation_notes = ["Do not remove marker macros merely to make grep clean; replace each only when the owning final evidence, protocol decision, or writing pass is complete.", "During thesis-freeze work, use the context artifact to resolve markers by class: final prose/title/RQ/structure, target/RRI protocol, scale/evidence manifests, architecture/Q_H ablations, and bridge/future-work routing.", "When a marker becomes obsolete because an existing active todo owns it, update the thesis text and resolve through the owning todo rather than creating duplicate issue records."]
acceptance = ["All remaining explicit thesis marker TODOs are inventoried in the context artifact.", "Each marker class is mapped to at least one existing DB owner or this record.", "Future thesis cleanup can distinguish intentional evidence gates from accidental inline TODO comments."]
verification = ["rg -n \"#(validation|research|decision|impl|question|conflict)_todo\" docs/typst/thesis/main.typ docs/typst/thesis/sections", "make agents-db AGENTS_ARGS='validate'", "make agents-db", "make check-agent-memory"]
resolved_at = "2026-06-19"
resolution_note = "Indexed all remaining thesis draft-marker TODO macros in .omx/context/thesis-marker-todos-20260618T224945Z.md and mapped them to existing active thesis, evidence, target/RRI, Q_H, rollout, and bridge-scope owners. Marker macros remain in Typst source until those owning gates are complete."
resolved_from = ".agents/todos.toml"

[[refactor]]
id = "refactor-013"
title = "Pilot optional OMX and Codex templates after scaffold cleanup"
description = "Keep orchestration local and template-based until CI, docs partitioning, and skills are clean enough to support team workflows."
priority = "low"
status = "resolved"
labels = ["agent", "workflow", "omx", "scaffold"]
issue_ids = ["issue-006", "issue-012"]
loc_min = 60
loc_expected = 140
loc_max = 320
context = ["The PRML cross-review recommended using Oh My Codex only as an optional orchestration layer after pruning wrong skills and docs sprawl.", "ARIA-NBV now has a .codex-plugin scaffold, but committed personal runtime config and host-specific paths must remain out of the repo."]
implementation_notes = ["Add example/template config only if needed; keep .omx/ and personal .codex runtime files ignored.", "Use OMX/team workflows first for audits and planning, not geometry/RRI code changes.", "Document how this differs from durable repo guidance in AGENTS.md and .agents/skills."]
acceptance = ["Any Codex/OMX config committed to the repo is a template, not operator-local runtime state.", "Optional orchestration does not replace make ci, agents-db, or targeted tests.", "No API keys, host paths, or personal MCP assumptions are committed."]
verification = ["rg -n \"api_key|/home/|/Users/|C:\\\\|\\.omx\" .codex-plugin .agents AGENTS.md .gitignore || true", "make check-agent-memory"]
resolved_at = "2026-04-29"
resolution_note = "Added optional OMX operator quick reference, checked-in Codex example templates, plugin README, and ignores for personal .codex/.omx runtime state."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-001"
title = "Thin root AGENTS.md dispatcher"
description = "Convert the root AGENTS.md into a thinner router and move detailed guidance into scoped skills/references over time."
priority = "medium"
status = "resolved"
labels = ["guidance", "hygiene"]
issue_ids = ["issue-006"]
loc_min = 120
loc_expected = 280
loc_max = 700
context = ["Root AGENTS.md currently carries broad setup, workflow, verification, and recurring gotcha content.", "The desired direction is a progressive-disclosure dispatcher into nested AGENTS.md files, .agents/references, and focused skills."]
implementation_notes = ["Do this only after active package/docs boundaries are stable enough to avoid losing critical guidance.", "Keep non-negotiable constraints in root until nested references are proven discoverable."]
acceptance = ["Root AGENTS.md is shorter and routes tasks to scoped guides.", "No critical verification or safety rule is removed without a replacement link."]
verification = ["make check-agent-memory", "rg -n \"PoseTW|CameraTW|setup_target|check-agent-memory\" AGENTS.md .agents/references .agents/skills"]
resolved_at = "2026-04-29"
resolution_note = "Replaced dense root AGENTS.md with a short dispatcher into nested guides, skills, references, memory, and verification surfaces."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-005"
title = "Remove second oracle-render oversampling stage"
description = "Delete the rendering-layer oversampling and dead post-render filtering path so candidate budgets have one owner."
priority = "high"
status = "resolved"
labels = ["oracle", "rendering", "simplification", "performance"]
issue_ids = ["issue-007", "issue-010"]
loc_min = 40
loc_expected = 120
loc_max = 260
context = ["CandidateViewGeneratorConfig already owns candidate oversampling before pruning.", "CandidateDepthRendererConfig.oversample_factor adds a second budget and _filter_valid_candidates is only exercised by direct logging tests.", "When rendering oversampling exceeds max_candidates_final, prepare_vin_offline_sample can receive more rendered candidates than the writer max_candidates budget."]
implementation_notes = ["Remove CandidateDepthRendererConfig.oversample_factor and render at min(num_candidates, max_candidates_final).", "Delete CandidateDepthRenderer._filter_valid_candidates and tests/rendering/test_candidate_depth_renderer_filter_logging.py unless a real render call path starts using it.", "Update configs, app signature expectations, and docs/reference generation inputs if they mention the depth renderer oversample field."]
acceptance = ["Candidate generation remains the only oversampling stage in the online oracle path.", "CandidateDepthRenderer.render never returns more rows than max_candidates_final.", "No tests rely on invoking a private unused post-render filter."]
verification = ["cd aria_nbv && uv run pytest tests/rendering/test_candidate_renderer_integration.py tests/rendering/test_pytorch3d_renderer.py tests/data_handling/test_vin_offline_store.py", "rg -n \"depth\\.oversample_factor|CandidateDepthRendererConfig\\(.*oversample_factor|_filter_valid_candidates\" aria_nbv .configs docs"]
resolved_at = "2026-04-29"
resolution_note = "Removed CandidateDepthRendererConfig.oversample_factor and the unused post-render filter; renderer now caps directly at max_candidates_final."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-006"
title = "Collapse duplicate depth backprojection implementations"
description = "Move to one public PyTorch3D depth-unprojection implementation and remove looped or commented-out duplicates."
priority = "high"
status = "resolved"
labels = ["oracle", "rendering", "frames", "simplification"]
issue_ids = ["issue-007", "issue-010"]
loc_min = 120
loc_expected = 320
loc_max = 700
context = ["rendering/candidate_pointclouds.py owns a vectorized private _backproject_depths_p3d_batch path used by the oracle pipeline.", "rendering/unproject.py owns a looped backproject_depth_with_p3d/backproject_batch path used by plotting and integration tests.", "tests currently import the private batch helper directly, which makes the frame contract harder to stabilize."]
implementation_notes = ["Create one public batched unprojection function in rendering/unproject.py and have build_candidate_pointclouds call it.", "Keep the existing PyTorch3D NDC/pixel-center convention and tests that assert sign/frame behavior.", "Delete commented-out old backprojection functions and stop tests from importing private helpers."]
acceptance = ["There is one canonical implementation for depth-to-world backprojection.", "Candidate point-cloud construction, plotting, and integration tests use the same unprojection code path.", "Frame-convention tests remain explicit and pass."]
verification = ["cd aria_nbv && uv run pytest tests/rendering/test_depth_backprojection_conventions.py tests/rendering/test_rendering_plotting_helpers.py tests/rendering/test_candidate_renderer_integration.py", "rg -n \"# def backproject_depth|# def _backproject_depths_p3d_batch|_backproject_depths_p3d_batch\" aria_nbv/aria_nbv aria_nbv/tests"]
resolved_at = "2026-04-29"
resolution_note = "Moved vectorized PyTorch3D depth unprojection to rendering.unproject.backproject_depths_p3d_batch and updated callers/tests to use the public path."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-007"
title = "Default-off redundant VIN offline diagnostic payloads"
description = "Stop writing duplicate rich diagnostic payloads by default when numeric blocks already reconstruct the required offline sample state."
priority = "high"
status = "resolved"
labels = ["data", "offline-store", "oracle", "performance", "simplification"]
issue_ids = ["issue-007", "issue-010"]
loc_min = 100
loc_expected = 300
loc_max = 700
context = ["VinOfflineWriter stores numeric depth/camera/oracle blocks and can also store oracle.depths_payload, oracle.candidate_pcs, and backbone.payload.", "VIN training disables most diagnostic loads through VinOfflineSourceConfig, so the duplicated payloads mainly increase build size and maintenance surface.", "Diagnostics should rebuild from numeric blocks unless a deliberate rich-payload mode is enabled."]
implementation_notes = ["Make rich payload materialization explicit and default-off, or remove payloads that are fully reconstructible from numeric blocks.", "Keep training-critical Zarr numeric blocks stable and bump OFFLINE_DATASET_VERSION if on-disk format semantics change.", "Update tests to assert that diagnostics still work through numeric reconstruction."]
acceptance = ["A default offline store does not duplicate candidate depths, candidate point clouds, or full backbone payloads unless requested.", "VIN diagnostics can still inspect candidates, depths, RRI, and basic backbone availability from canonical blocks.", "Store versioning and rebuild guidance are explicit if payload layout changes."]
verification = ["cd aria_nbv && uv run pytest tests/data_handling/test_vin_offline_store.py tests/data_handling/test_public_api_contract.py", "cd aria_nbv && uv run pytest tests/app/panels/test_vin_bin_values_tab.py tests/vin/test_vin_utils.py"]
resolved_at = "2026-04-29"
resolution_note = "Made rich offline diagnostic payload records opt-in via include_diagnostic_payloads; default writer stores numeric training blocks without duplicate DTO payloads."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-009"
title = "Make oracle mesh cropping strict and tensor-native"
description = "Replace silent full-mesh fallback and CPU scalar conversion in oracle mesh cropping with an explicit crop contract."
priority = "high"
status = "resolved"
labels = ["rri", "oracle", "geometry", "contracts"]
issue_ids = ["issue-007", "issue-010"]
loc_min = 80
loc_expected = 220
loc_max = 520
context = ["_crop_mesh_to_aabb calls aabb.tolist(), which forces scalar CPU synchronization on GPU tensors.", "The current empty-crop fallback silently scores the full mesh, which would corrupt target-specific RRI when an entity crop misses.", "Entity-aware RRI needs a trusted crop rule before GT-OBB labels are used as thesis evidence."]
implementation_notes = ["Keep the crop calculation tensor-native and document whether faces are kept by any-vertex, all-vertex, or triangle-AABB intersection.", "Return an explicit empty-crop result or raise a clear invalid-target error instead of falling back to full-scene scoring.", "Add tests covering empty crops, partial crops, and device preservation."]
acceptance = ["Empty target crops are visible as invalid crop failures, not scene-level RRI.", "Cropping preserves device/dtype and does not call .tolist() on hot-path tensors.", "The chosen crop semantics are documented in code and RRI docs if metric meaning changes."]
verification = ["cd aria_nbv && uv run pytest tests/rri_metrics/test_oracle_rri_chunking.py tests/rri_metrics", "rg -n \"tolist\\(|fallback to full mesh|_crop_mesh_to_aabb\" aria_nbv/aria_nbv/rri_metrics docs/contents/impl"]
resolved_at = "2026-04-29"
resolution_note = "Changed oracle mesh AABB cropping to tensor-native bounds and explicit empty-crop failure instead of full-mesh fallback."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-012"
title = "Remove premature counterfactual offline-store hooks"
description = "Delete future counterfactual flags and readers from the immutable offline store until rollout artifacts have a concrete M5 contract."
priority = "medium"
status = "resolved"
labels = ["data", "offline-store", "rollouts", "simplification"]
issue_ids = ["issue-007", "issue-010"]
loc_min = 60
loc_expected = 180
loc_max = 420
context = ["VinOfflineManifest and VinOfflineDataset contained counterfactual metadata, load flags, and readers, but VinOfflineWriter never materialized a real counterfactual block.", "The roadmap puts multi-step rollouts in standalone rollout artifacts rather than the immutable VIN offline store.", "Premature storage hooks created stale contract surface before rollout artifacts had a concrete contract."]
implementation_notes = ["Removed VinOfflineCounterfactuals, materialized_blocks.counterfactuals, include_counterfactuals, load_counterfactuals, load_counterfactuals_for_batch, and _build_counterfactuals.", "Bumped OFFLINE_DATASET_VERSION from 6 to 7 and updated the local vin_offline and Rerun smoke stores to strict v7 manifests with no compatibility reader.", "Renamed the Rerun smoke sidecar config and store from vin_offline_rerun_smoke_v6 to vin_offline_rerun_smoke_v7."]
acceptance = ["The offline-store schema no longer advertises counterfactual data.", "Training, diagnostics, and Rerun configs no longer carry no-op counterfactual toggles.", "The current local persisted stores open under the strict v7 reader and old v6 manifests are rejected."]
verification = ["cd aria_nbv && uv run pytest tests/data_handling/test_vin_offline_store.py tests/data_handling/test_public_api_contract.py", "cd aria_nbv && uv run nbv-summary --config-path offline_only.toml", "cd aria_nbv && uv run pytest tests/rerun_inspector -q", "cd docs && quarto render contents/setup.qmd", "cd docs && quarto render contents/impl/one_scene_smoke.qmd", "cd docs && quarto render contents/thesis/m1_contract_report.qmd"]
resolved_at = "2026-05-07"
resolution_note = "Removed premature counterfactual offline-store hooks, bumped the immutable VIN offline dataset format to v7, upgraded local persisted manifests without leaving migration code, and verified strict data/Rerun/doc checks."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-017"
title = "Delete kg-query alias and references"
description = "Remove kg-query from the agent-facing Make/doc/skill surface because it duplicated kg-route without synthesis."
priority = "medium"
status = "resolved"
labels = ["kg", "litkg", "scaffold", "simplification"]
issue_ids = ["issue-023"]
loc_min = 30
loc_expected = 80
loc_max = 200
context = ["kg-query trained agents to expect answer synthesis while returning the same context-pack shape as kg-route.", "The narrowed command surface keeps kg-search for fast retrieval and kg-route for context packs; synthesis is deferred until a real answer layer exists."]
implementation_notes = ["Remove kg-query from Makefile and .PHONY.", "Remove active quick-reference and skill guidance that tells agents to invoke kg-query.", "Keep historical resolved/debrief/generated mentions as archive evidence unless a separate generated-artifact refresh owns them."]
acceptance = ["No active Make target or active agent guidance tells agents to use kg-query.", "litkg_quick_reference names kg-search for retrieval and kg-route for context packs.", "semantic-scholar-litkg explicitly says synthesis is deferred."]
verification = ["! rg -n \"make kg-query|KG_QUERY=.*kg-query\" Makefile .agents/references .agents/skills .agents/kg/README.md docs", "make kg-search KG_QUERY=\"bounded oracle RRI\" KG_FORMAT=json", "make kg-route KG_TASK=\"bounded oracle RRI\" KG_FORMAT=json"]
resolved_at = "2026-05-08"
resolution_note = "kg-query Make target deleted in commit d3e980d. kg-search remains the killer retrieval verb; kg-route owns context packs. Synthesis deferred until LLM-backed implementation lands; semantic-scholar-litkg note pending in this same cleanup pass."
resolved_from = ".agents/refactors.toml"

[[refactor]]
id = "refactor-018"
title = "Add genre and precedent ranking for litkg routes"
description = "Add an optional document-genre/precedent tier so process-shaped tasks can surface prior report templates such as m1_contract_report.qmd."
priority = "low"
status = "resolved"
labels = ["kg", "litkg", "ranking", "docs"]
issue_ids = ["issue-023"]
loc_min = 120
loc_expected = 320
loc_max = 800
context = ["The current ranking can miss structurally relevant precedent docs when lexical overlap is weak.", "This is lower priority than claim verdicts, backlog filtering, task-grounded risks, verification-path injection, worktree exclusion, and confidence floors."]
implementation_notes = ["Define a small optional QMD genre frontmatter enum such as contract_report, runbook, architecture, thesis_section, and scratch.", "Parse frontmatter into litkg source metadata and add configurable genre boosts for matching task terms.", "Seed the first useful precedent tag on docs/contents/thesis/m1_contract_report.qmd after docs-frontmatter validation is updated."]
acceptance = ["A task like design experiment evidence registry surfaces m1_contract_report.qmd as a precedent when it is tagged.", "Docs without genre frontmatter keep current ranking behavior.", "The optional genre key is documented and validated before relying on it."]
verification = ["make qmd-frontmatter-check", "cd .agents/external/litkg-rs && cargo test -p litkg-core context_pack::genre_boost", "make kg-route KG_TASK=\"design experiment evidence registry\" KG_FORMAT=json"]
resolved_at = "2026-05-08"
resolution_note = "Smoke evidence (2026-05-08): make kg-route KG_TASK='design final experiment evidence registry...' KG_FORMAT=json now returns m1_contract_report.qmd 4x in evidence_spans and required_reads. The verification-injection + lexical-filter changes in litkg-rs 152f13b solved the precedent-discovery gap that motivated this refactor without requiring a genre frontmatter scheme. top_sources still does not promote precedents structurally; re-open if a future task surfaces an unrecoverable miss."
resolved_from = ".agents/refactors.toml"
