# ARIA-NBV TODOs

[[todo]]
id = "todo-001"
title = "Classify and prune docs/"
description = "Inventory docs/ and triage pages into public thesis narrative, paper/slides source, generated context, internal agent material, or archive."
priority = "high"
status = "todo"
labels = ["docs", "hygiene"]
issue_ids = ["issue-003"]
loc_min = 200
loc_expected = 700
loc_max = 1600
context = ["Current docs expose roadmap, TODOs, ideas, generated agent scaffold mirrors, literature notes, implementation guides, and scratchpad-like pages in the same public tree.", "docs/AGENTS.md treats current thesis roadmap/questions as active direction, generated glossary output as terminology, ideas as read-only archive/scratch, and seminar paper as historical implemented evidence.", "The raw docs/contents/ext-impl vendor-stack pages were identified as public-doc role mixing: their retained ATEK/EFM3D/Project Aria facts belong in an internal developer reference or code docstrings, not public thesis navigation.", "docs/contents/ideas.qmd is intentionally retained as rendered human-maintained scratch/archive material and must be marked agent read-only rather than converted into an agent-editable backlog."]
references = ["repo:docs/AGENTS.md", "repo:docs/_quarto.yml", "repo:docs/contents/thesis/questions.qmd#rq-kg-writing", "repo:.agents/references/external_stack_contracts.md", "repo:docs/contents/ideas.qmd"]
implementation_notes = ["Start with scripts/nbv_qmd_outline.sh --compact and docs/_quarto.yml navigation instead of broad manual browsing.", "Keep source pages until there is an explicit archive/move decision; first remove public navigation pressure and stale links.", "Move raw internal/vendor scratch to .agents/archive/docs/ and retain only curated public thesis pages under docs/contents.", "Keep docs/contents/ideas.qmd as public archive scratch with valid taxonomy frontmatter and `agent_editing: read-only`.", "Do not delete history records under .agents/memory/history/."]
acceptance = ["Each docs subtree has an explicit role: public, paper, slides, generated, internal mirror, archive, or human-owned scratch archive.", "Public nav no longer surfaces internal agent scaffolding, raw vendor-stack dumps, or stale work-management pages as primary project content.", "docs/contents/ideas.qmd remains rendered, human-owned, scratch/archive scoped, and agent read-only.", "Stale typo links such as coral_intergarion and hesita are either renamed or intentionally documented."]
verification = ["scripts/nbv_qmd_outline.sh --compact", "make qmd-frontmatter-check", "cd docs && quarto render index.qmd contents/ideas.qmd contents/thesis/roadmap.qmd contents/thesis/questions.qmd contents/archive/index.qmd", "rg -n \"Seminar|hesita|coral_intergarion|old NBV repo URL|contents/ext-impl|External Implementations\" README.md docs -g '!_site/**'"]

[[todo]]
id = "todo-003"
title = "Implement oracle-rri-contracts skill"
description = "Create a skill to protect technical layers: poses, cameras, RRI binning semantics, and oracle reporting."
priority = "medium"
status = "todo"
labels = ["agent", "research"]
issue_ids = ["issue-007"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["RRI semantics are contract changes, not local refactors.", "aria_nbv/aria_nbv/rri_metrics/AGENTS.md already defines the boundary for oracle labels, binning, ordinal loss, and reported metric meaning."]
references = ["repo:aria_nbv/aria_nbv/rri_metrics/AGENTS.md", "repo:docs/contents/theory/rri_theory.qmd", "repo:aria_nbv/tests/rri_metrics"]
implementation_notes = ["Base the skill on rri_metrics/AGENTS.md, focused geometry/RRI tests, and docs/contents/theory/rri_theory.qmd.", "Require tensor shapes, frames, units, and metric interpretation in any public RRI docstrings."]
acceptance = ["The skill routes RRI/oracle tasks to the right docs, tests, and gotchas.", "No copied wrong-repo text remains."]
verification = ["rg -n \"PRML|Rust\" .agents/skills/oracle-rri-contracts .agents/skills || true", "make check-agent-memory"]

[[todo]]
id = "todo-004"
title = "Deferred vin-training-eval skill"
description = "Create a deferred skill for VIN scorer experiments, offline-store training, diagnostics, and W&B/Optuna logging once VIN training work resumes."
priority = "medium"
status = "todo"
labels = ["agent", "research"]
issue_ids = ["issue-007"]
loc_min = 80
loc_expected = 180
loc_max = 360
context = ["M2 requires a reproducible one-step VIN baseline, calibration/ranking plots, and a fixed ablation matrix.", "The current app and Lightning stack already expose VIN diagnostics, summary, W&B, and Optuna surfaces.", "The scaffold cleanup intentionally adds only diagnose-aria and plan-grill now; VIN training/eval should remain backlog until active VIN experiment work resumes."]
references = ["repo:aria_nbv/aria_nbv/vin/AGENTS.md", "repo:docs/reference/aria_nbv.vin.model_v3.VinModelV3.qmd", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m2", "bib:VIN-NBV-frahm2025"]
implementation_notes = ["Route to aria_nbv/aria_nbv/vin/AGENTS.md, app VIN diagnostics helpers, Lightning CLI, and docs/reference/aria_nbv.vin.model_v3.VinModelV3.qmd.", "Capture validation gotcha: trainer_config.enable_validation must be true for validation to run.", "Use the final name vin-training-eval unless a later plan selects a narrower name."]
acceptance = ["The skill gives a compact experiment workflow for offline_only.toml, nbv-summary, targeted VIN tests, and diagnostics interpretation.", "The skill names current ARIA-NBV surfaces only."]
verification = ["make check-agent-memory", "rg -n \"PRML|Rust\" .agents/skills/vin-training-diagnostics .agents/skills || true"]

[[todo]]
id = "todo-005"
title = "Build entity-aware oracle RRI"
description = "Implement the V0 GT-OBB-cropped oracle target-RRI contract and keep it separate from the V1 observed-target actor input."
priority = "high"
status = "todo"
labels = ["research", "core"]
issue_ids = ["issue-007", "issue-031"]
loc_min = 350
loc_expected = 900
loc_max = 1800
context = ["Thesis RQ2/RQ4/RQ5 require target-specific RRI before target-conditioned scorer or Q_H training can be evaluated.", "The target contract splits actor-visible target input from oracle labels: V0 uses GT OBB input plus GT label crop as a sanity/upper-bound path, while V1 uses observed/predicted OBB input matched to GT-OBB target-RRI labels.", "Target-RRI labels must consume the oracle evaluation stream P_eval rather than MPS semi-dense current points before V0/V1 labels are trusted; semi-dense remains actor-visible support and legacy diagnostic only.", "Current EVL OBB batch behavior must be verified against the target pipeline and focused tests before selecting a batch policy.", "The Rerun inspector should eventually render scene-level and target-level RRI side by side once target-aware fields are persisted.", "Before scale, target-crop policy must be evidence-backed: report target crop mesh-face counts, empty-crop rate, current target support, candidate target support, and examples where any-vertex OBB cropping misses or includes surprising geometry."]
references = ["repo:docs/contents/thesis/questions.qmd#rq2-target-encoding", "repo:docs/contents/thesis/questions.qmd#rq4-planning", "repo:.agents/skills/entity-aware-rri/SKILL.md", "repo:.agents/work/rri-metrics/dense-semidense-rri-missmatch.md", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md", "repo:aria_nbv/aria_nbv/rollouts/target_counterfactuals.py", "repo:aria_nbv/aria_nbv/rri_metrics/eval_pointclouds.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "bib:VIN-NBV-frahm2025", "bib:EFM3D-straub2024"]
implementation_notes = ["Start with GT OBBs and a small trusted subset for V0; do not conflate this privileged sanity path with the V1 observed/predicted target input.", "Build target current points from P_eval = ASE-depth observed-prefix root plus selected dense candidate history, canonical-fused before target crop and oracle scoring.", "Keep target mesh/point support, invalid target reasons, crop metadata, eval-source metadata, and scene-level-vs-target-level diagnostics explicit.", "Use existing PoseTW and CameraTW contracts; do not introduce raw matrix interfaces.", "Expose enough metadata for Rerun diagnostics to compare scene RRI and GT-OBB-cropped target RRI for the same candidate set.", "Add a target-crop audit before large writes: crop margin, face retention rule, face/vertex counts, empty crop failures, adjacent-geometry leakage examples, and small-target support histograms."]
acceptance = ["V0 GT-OBB cropped RRI runs on a small subset with side-by-side target RRI and scene RRI diagnostics.", "The target-RRI label/evaluation crop can be matched to later V1 observed/predicted OBB inputs without making GT actor-visible in the main protocol.", "Target current/support points come from P_eval by default, while legacy semi-dense-root RRI is available only as a labeled diagnostic.", "Unsupported targets have explicit invalid reasons rather than being encoded as low RRI.", "Docs and tests describe target frames, crop margin, units, eval-source depth convention, and failure modes.", "Persisted or logged diagnostics are sufficient for a Rerun view to compare scene-level and target-level candidate rankings.", "A crop-policy audit reports empty target crops, target support, crop geometry size, and representative success/failure cases before thesis-scale target labels are generated."]
verification = ["cd aria_nbv && uv run pytest tests/rri_metrics tests/data_handling/test_vin_offline_store.py", "cd docs && quarto render contents/thesis/questions.qmd contents/thesis/roadmap.qmd"]

[[todo]]
id = "todo-006"
title = "ARIA-NBV Gym simulator setup"
description = "Build a Gym-style online simulator only after the ASE mesh/oracle rollout loop and candidate-query Q_H dataset path are stable."
priority = "medium"
status = "todo"
labels = ["research", "sim"]
issue_ids = ["issue-007"]
loc_min = 300
loc_expected = 900
loc_max = 1800
context = ["The thesis-core simulator substrate is the ASE mesh/oracle counterfactual rollout loop for offline rollout and Q_H data generation.", "SB3 DQN or other online Gymnasium baselines are deferred until an online simulator exists and the candidate-query Q_H path is trusted.", "Current canonical state frames full continuous RL as a stretch direction rather than the thesis core."]
references = ["repo:docs/contents/theory/rl_planning.qmd", "repo:aria_nbv/aria_nbv/rl/counterfactual_env.py", "url:https://gymnasium.farama.org/api/env/", "url:https://stable-baselines3.readthedocs.io/en/master/guide/algos.html", "bib:GenNBV-chen2024"]
implementation_notes = ["Begin with discrete candidate shell rollouts and hard masks if this surface is opened.", "Keep oracle reward, VIN surrogate reward, state modalities, and invalid action handling separated.", "Do not let SB3/Gym work displace the mandatory offline candidate-query Q_H baseline."]
acceptance = ["Simulator API can replay bounded discrete rollouts with cumulative target or scene RRI accounting.", "Invalid actions are masked or reported with reasons, not silently treated as low-RRI candidates.", "Any SB3 baseline is explicitly labeled as post-Q_H or stretch unless the roadmap is revised."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation tests/rl", "make context-contracts"]

[[todo]]
id = "todo-007"
title = "M1 data/cache/oracle contract audit and stabilization"
description = "Freeze the offline store, split, frame/CW90, candidate-label ordering, and oracle RRI contract before new target-conditioned model work."
priority = "high"
status = "todo"
labels = ["data", "rri", "contracts", "thesis"]
issue_ids = ["issue-007", "issue-031"]
loc_min = 160
loc_expected = 420
loc_max = 1000
context = ["Roadmap M1 exit criteria require stable offline store and RRI contracts, documented frame/CW90 risks, candidate-label alignment proof, Rerun smoke evidence, and measured oracle throughput.", "M1 is now a hard stop before target/RL scaling: target RRI, rollout generation, stochastic branching, and Q_H training should not scale until the evidence report is passable.", "The M1 oracle report must explicitly cover the dense-vs-semi-dense RRI mismatch, ASE ray-distance depth convention, observed-prefix root frame policy, canonical fusion settings, and legacy-vs-new RRI diagnostic.", "The follow-up reviews recommend a short thesis-facing M1 evidence artifact, concretely docs/contents/thesis/m1_contract_report.qmd, before target RRI, VIN, rollout, or Q_H claims depend on the substrate.", "data_handling/AGENTS.md says readers should validate strictly and fail with rebuild guidance rather than mutate derived artifacts.", "rri_metrics/AGENTS.md says metric semantics, binning definitions, and reported metric meanings are contract changes.", "The current local vin_offline store is only a partial strict-v7 root cache: 48 rows from scene 81286, sample-key split policy, finalized_after_interrupt=true, and seminar-era scene-RRI semantics. It is usable for smoke joins but not for thesis-scale or target-conditioned evidence.", "Scene-level splits must be frozen before thesis-scale source, target-label, rollout, or Q_H generation; sample-level sha1(sample_key) splits remain diagnostic only."]
references = ["repo:docs/contents/thesis/roadmap.qmd#roadmap-m1", "repo:aria_nbv/aria_nbv/data_handling/AGENTS.md", "repo:aria_nbv/aria_nbv/rri_metrics/AGENTS.md", "repo:.agents/skills/nbv-geometry-contracts/SKILL.md", "repo:.agents/work/rri-metrics/dense-semidense-rri-missmatch.md", "repo:.agents/resolved.toml#todo-046", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md", "repo:aria_nbv/aria_nbv/data_handling/_offline_writer.py", "repo:aria_nbv/aria_nbv/rri_metrics/eval_pointclouds.py"]
implementation_notes = ["Run make context-contracts and targeted data/RRI tests first; record failures in issues/todos before changing package semantics.", "Create or update a thesis-facing M1 contract report that records offline store version/sample count, train/val split source, sample_index contract, pose-frame contract, CW90/display-only convention, candidate shell/valid/RRI alignment, ASE-depth root eval source, depth render/backprojection convention, root frame policy, canonical fusion policy, oracle throughput on representative snippets, known limitations, commands used, and pass/block status.", "Record whether the active root store is partial/interrupted or thesis-scale, and explicitly separate seminar scene-RRI source rows from target-conditioned H=1 rollout sidecar labels.", "Define or reference the scene_id-to-split manifest that will replace sample-key hashing for thesis evidence.", "Attach or reference Rerun normal, boundary, and failure `.rrd` smoke artifact paths when available, without committing generated recordings.", "Confirm offline_only.toml uses apply_cw90_correction=false and that pose/camera frame alignment tests cover this setting.", "Collect oracle throughput on representative snippets only after correctness checks pass."]
acceptance = ["Targeted data_handling, rri_metrics, and rollout/candidate-ordering tests pass or DB records identify exact blockers.", "docs/contents/thesis/m1_contract_report.qmd exists or is explicitly blocked with missing artifact/access details.", "The report states whether vin_offline is partial/interrupted, which scenes/snippets it covers, and whether its splits are sample-key or scene-level.", "Frame/CW90, candidate-label ordering, ASE ray-depth root eval, rendered candidate z-depth, canonical fusion, and offline-store split contracts are documented in active docs or gotchas.", "Rerun proof paths cover normal, boundary, and failure samples, and an oracle throughput command/report path is identified for representative snippets.", "Roadmap M1 can be marked passed or still blocked from evidence rather than from chat memory.", "No target-RRI scale-up, rollout scale-up, stochastic rollout scale-up, or Q_H training proceeds before the report is passable or blockers are explicitly recorded."]
verification = ["make context-contracts", "cd aria_nbv && uv run pytest tests/data_handling/test_vin_offline_store.py tests/data_handling/test_public_api_contract.py", "cd aria_nbv && uv run pytest tests/rri_metrics tests/pose_generation/test_counterfactuals.py", "cd docs && quarto render contents/thesis/roadmap.qmd", "cd docs && quarto render contents/thesis/questions.qmd", "cd docs && quarto render contents/thesis/m1_contract_report.qmd"]

[[todo]]
id = "todo-012"
title = "Add GitHub issue and PR governance templates"
description = "Create the repository collaboration scaffold recommended by the PRML cross-review: labels, milestones, issue templates, PR template, and draft-PR disposition guidance."
priority = "medium"
status = "todo"
labels = ["github", "workflow", "scaffold"]
issue_ids = ["issue-011"]
loc_min = 80
loc_expected = 180
loc_max = 420
context = ["The PRML comparison called out missing milestones, labels, project-board views, issue templates, PR template, and explicit draft-PR disposition.", "The local agents DB now owns active backlog, so GitHub templates should route work into reproducible acceptance and verification rather than duplicating all TOML records."]
references = ["repo:.agents/AGENTS_INTERNAL_DB.md", "repo:AGENTS.md#instruction-capture", "url:https://docs.github.com/en/issues/using-labels-and-milestones-to-track-work/about-milestones"]
implementation_notes = ["Add .github/ISSUE_TEMPLATE entries for bug, research task, docs task, and refactor work.", "Add .github/pull_request_template.md with summary, affected contracts, verification, docs/memory updates, and limitations.", "Document label/milestone taxonomy in a repo-local reference if it cannot be created directly through GitHub in the current task."]
acceptance = ["New GitHub issues ask for affected files, contract impact, acceptance criteria, and verification.", "PR template is reviewer-first and asks for docs/memory updates when behavior changes.", "Open draft PR triage has a repeatable local checklist or issue template."]
verification = ["find .github -maxdepth 3 -type f | sort", "rg -n \"Acceptance|Verification|Affected contracts|Docs|Memory\" .github"]

[[todo]]
id = "todo-018"
title = "Create experiment registry and final claim matrix"
description = "Add a reproducibility surface that maps final figures, tables, ablations, and thesis claims to run IDs, configs, cache hashes, split manifests, metrics, and source artifacts."
priority = "medium"
status = "todo"
labels = ["experiments", "thesis", "reproducibility"]
issue_ids = ["issue-013"]
loc_min = 120
loc_expected = 260
loc_max = 620
context = ["Roadmap M7/M8 require final tables, figures, failure cases, and reproducible configs.", "The PRML-style issue register recommended a final experiment registry, run-report generator, claim matrix, and release checklist.", "Experiments and cache manifests must let readers trace thesis claims to run IDs, config hashes, cache IDs, split manifests, metrics, and artifacts without ingesting raw tensor stores.", "The old main-seminar findings page is archived and no longer represents current thesis evidence, so final thesis claims need a clean future-facing evidence index."]
references = ["repo:docs/contents/thesis/roadmap.qmd#roadmap-m7", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m8", "repo:docs/contents/thesis/questions.qmd#rq-evidence-protocol", "repo:.agents/references/source_order.md", "repo:.agents/references/direct_source_claim_checklist.md"]
implementation_notes = ["Start with a small docs or .agents-owned registry schema before building automation.", "Add docs/contents/thesis/evidence.qmd as the advisor-facing claim/evidence index once current thesis results exist; keep it separate from archived seminar findings.", "Record run ID, config path/hash, cache/store ID, split manifest, candidate profile, seed, checkpoint, metric table, figure output, and supported claim ids.", "Keep curated registry and manifest summaries source-owned; exclude raw sample tensors, heavy shards, and full backbone stores.", "Keep final claims explicit: do not claim continuous RL, real-device deployment, or VLM/global planning without evidence."]
acceptance = ["There is a canonical place to trace final experiment artifacts to source inputs.", "docs/contents/thesis/evidence.qmd exists as the advisor-facing evidence index or this TODO records why it is intentionally deferred.", "At least one smoke/final-row example is represented.", "The registry uses direct source locators and can be referenced from paper/slides without copying unverified run notes."]
verification = ["rg -n \"claim matrix|run_id|config_hash|cache_hash|split|supported claims|thesis/evidence\" docs .agents", "cd docs && quarto render contents/thesis/evidence.qmd", "make check-agent-memory"]

[[todo]]
id = "todo-026"
title = "Harden bounded oracle-RRI lookahead versus greedy comparison"
description = "Make the first thesis-grade non-myopic comparison executable: random-valid, one-step greedy, and bounded oracle lookahead under equal acquisition and candidate budget."
priority = "high"
status = "todo"
labels = ["rollouts", "rri", "planning", "thesis"]
issue_ids = ["issue-018", "issue-007", "issue-031"]
loc_min = 180
loc_expected = 420
loc_max = 900
context = ["The thesis RQ2 contract locks bounded oracle-RRI lookahead versus one-step greedy as the first non-myopic comparison and oracle lookahead as the upper-bound reference for Q_H.", "docs/contents/thesis/questions.qmd defines the ArgTopK to ArgTop1_h ladder and requires equal acquisition budget.", "The greedy/lookahead comparison must rank rollout candidates by root-normalized target gain or log-error gain rather than pure state-relative cumulative RRI; otherwise denominator shrinkage can preserve a greedy first-step bias even after the dense/semi-dense root mismatch is fixed.", "The current rollout code has horizon, branch_factor, beam_width, and oracle scoring, but needs a reportable comparison path with budget parity and diagnostics before Q_H training is meaningful.", "The doc-alignment review recommends a tiered M5 gate: random-valid, one-step greedy, bounded oracle lookahead, and Q_H are required first; stochastic rollout sources remain preferred but should not block the first deterministic evidence path."]
references = ["repo:docs/contents/thesis/questions.qmd#rq2-offline-qh", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m5", "repo:docs/contents/theory/rl_planning.qmd", "repo:.agents/work/rri-metrics/dense-semidense-rri-missmatch.md", "repo:aria_nbv/aria_nbv/rollouts/replay", "repo:aria_nbv/tests/rollouts"]
implementation_notes = ["Implement or expose deterministic one-step greedy, random-valid, and h-step oracle lookahead from the same candidate generator and scorer.", "Ensure candidate budget, acquisition budget, horizon, branch factor, beam width, selected first action, seed, eval depth source, root frame policy, fusion config, and reward mode are logged in the trace and report.", "Report cumulative root-normalized target gain as the rollout/Q_H return, plus state-relative cumulative target RRI, endpoint gain, scene RRI, path length, invalid fraction, runtime, and selected first action.", "Use this deterministic comparison as the trusted upper-bound/evidence layer that Q_H must later beat against one-step greedy/model scoring.", "Do not require both temperature-softmax and Gumbel traces before this deterministic comparison is reportable."]
acceptance = ["A smoke experiment compares random-valid, one-step greedy, and bounded oracle lookahead on one trusted snippet or scene.", "The comparison enforces equal candidate and acquisition budgets.", "Outputs include cumulative root-normalized target gain, endpoint target gain, state-relative target RRI diagnostics, and acquisition-cost metrics suitable for the M5 roadmap gate.", "Oracle lookahead is clearly labeled as an upper-bound baseline for the later Q_H result.", "Representative success/failure traces can be inspected or plotted.", "This deterministic gate can pass before the full stochastic rollout suite exists."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation/test_counterfactuals.py", "cd docs && quarto render contents/thesis/questions.qmd contents/thesis/roadmap.qmd"]

[[todo]]
id = "todo-027"
title = "Add stochastic rollout selectors and branch schedules"
description = "Implement temperature-softmax, Gumbel-Top-k beam sampling, explicit branch schedules, and late-exploration mode for bounded rollout generation."
priority = "high"
status = "todo"
labels = ["rollouts", "stochastic", "planning", "rl"]
issue_ids = ["issue-019", "issue-018"]
loc_min = 180
loc_expected = 460
loc_max = 1000
context = ["The literature synthesis maps Gumbel-Top-k to diverse branch sampling without replacement and soft Q / maximum-entropy ideas to softmax-over-score exploration.", "Current rollout code supports deterministic selectors, seeded temperature-softmax, branch-factor schedules, stochastic branch factors, robust median/IQR softmax logits, and optional sibling diversity guards for distance, yaw, strategy, and target bearing.", "Gumbel-Top-k, late exploration, and more granular per-step stochastic replay audits still need evidence before stochastic traces become final thesis evidence.", "Branching only at step 0 can miss later setup actions that unlock valuable views.", "The multimodal rollout alignment locks the first stochastic branch target as explicit seeded bounded beam generation with horizon 3 and branch/beam cap 3, not an unbounded random tree.", "The curated thesis decision makes oracle-scored temperature-softmax required before first Q_H training; Gumbel-Top-k remains preferred later diversity evidence rather than a first-attempt blocker.", "Current rollout lineage records seed-once RNG provenance with the job seed and split manifest hash; final stochastic evidence should also report sampled outcomes and diversity-guard fallback counts.", "The target-selection/candidate-sampling review notes that diversity guards are data-generation tools rather than thesis contributions; if no candidate passes the guards, the current sampler falls back to remaining candidates, so guard activation and fallback counts must be reported before diversity claims."]
references = ["repo:docs/contents/theory/rl_planning.qmd", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd#mixture-rollout-branch-selection", "repo:.agents/skills/counterfactual-rollout-planner/SKILL.md", "repo:.agents/work/target-selection-sampling/02-review-gpt55pro.md", "repo:aria_nbv/aria_nbv/rollouts/counterfactuals.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "bib:GumbelTopK-kool2019", "bib:DeepEnergyPolicies-haarnoja2017", "bib:TrajectoryTransformer-janner2021"]
implementation_notes = ["Keep robust temperature-softmax as the default stochastic selector: normalize finite candidate scores by median/IQR before applying selection_temperature.", "Keep sibling diversity guards optional and explicit; use distance, yaw, strategy, and target-bearing thresholds only when the rollout recipe requests them.", "Persist or report diversity_guard_active_count and diversity_guard_fallback_count so strict guards cannot silently become unconstrained sampling.", "Report policy entropy, unique selected chains, selected component/position histograms, valid count by step, target-root-gain distribution, and selected action provenance for stochastic runs.", "Extend seed-once lineage for stochastic evidence with sampled outcome tables, branch order, and optional per-step derived seed hashes where needed for replay/debug.", "Represent branch_schedule as explicit per-step policy parameters such as horizon=3, beam_width<=3, branch_cap<=3, gumbel_top_k, top_k, random_branch_prob, and late_explore probability.", "Keep computational complexity bounded by beam_width, horizon, and candidate count; do not materialize unbounded random per-node trees.", "Persist policy provenance so Q_H training can distinguish random-valid, greedy, softmax, Gumbel, and oracle-lookahead source traces."]
acceptance = ["Seeded temperature-softmax uses robust scale-invariant logits and tests cover affine score-scale invariance.", "Branch diversity can require distinct sibling strategy or yaw/target-bearing separation when configured.", "Diversity-guard activation and fallback counts are inspectable, and reports state when guards did not constrain a branch.", "Rollouts support Gumbel-Top-k selection policies before the preferred full stochastic diversity evidence suite.", "The first reportable stochastic rollout target can run as seeded H3/B3 bounded beam generation with explicit branch schedule metadata.", "Branch schedules can vary branching by step instead of only at the root.", "Late exploration can sample valid medium/high-entropy candidates under an explicit probability.", "Trace output records policy parameters, selected strategy, selected position/component, and replayable RNG/seed provenance per stochastic step.", "Oracle-scored temperature-softmax can produce replayable traces for first Q_H training."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation/test_counterfactuals.py", "rg -n \"temperature|gumbel|branch_schedule|late_explore|diversity_guard|selection_entropy|position_id\" aria_nbv/aria_nbv/pose_generation aria_nbv/tests/pose_generation"]

[[todo]]
id = "todo-028"
title = "Harden realistic target-aware candidate mixtures"
description = "Finish scale-readiness around the realistic observed-target candidate mixture, keeping the legacy shell as an explicit upper-bound ablation."
priority = "high"
status = "todo"
labels = ["candidate-generation", "target", "entity-aware", "rollouts"]
issue_ids = ["issue-020", "issue-007"]
loc_min = 220
loc_expected = 520
loc_max = 1200
context = ["Candidate generation now separates position families from view-direction families: the main thesis mixture uses forward-local, target-bearing-local, and lateral target-bypass position modes; local-refinement, revisit/backtrack, and the legacy free shell are named ablations.", "CandidateSamplingResult records full-shell view strategy id, position id, mixture id, sampler probability, component name, and sparse component diagnostics.", "The thesis RQ3 working hypothesis expects object-centric candidates plus default exploration candidates.", "The hard thesis core requires a mixed observed candidate set with categorical-probability hyperparameters: default exploration, predicted-target LookAt, random orientation, and forward/trajectory modes are core; GT LookAt is an upper-bound ablation only.", "The synthesis calls for LookAt entity center, missing-surface centroid, frontier, random orientation, and trajectory-forward modes.", "The target-selection/candidate-sampling review confirms the position/view factorization but flags five-family default complexity, permissive local-motion limits, and the fact that target-bearing/lateral position transforms can move final center directions outside the raw angular caps."]
references = ["repo:docs/contents/thesis/questions.qmd#rq3-candidates", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd#candidate-center-sampling", "repo:.agents/work/target-selection-sampling/02-review-gpt55pro.md", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation.py", "repo:aria_nbv/aria_nbv/pose_generation/candidate_mixture.py", "repo:aria_nbv/aria_nbv/pose_generation/positional_sampling.py", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation_rules.py", "repo:aria_nbv/aria_nbv/pose_generation/orientations.py", "repo:.configs/build_rollouts_v1_realistic.toml", "repo:.agents/skills/entity-aware-rri/SKILL.md"]
implementation_notes = ["Keep position_mode and view_mode separate in configs, tests, rollout traces, and diagnostics; do not treat TARGET_POINT as a position sampler.", "Use observed/predicted target points for target-bearing and LookAt components in the main mixed candidate set; keep GT OBB center LookAt explicitly marked as oracle/upper-bound if added later.", "Interpret azimuth/elevation caps as raw draw priors unless a final post-mode clamp/check is implemented; log final azimuth, elevation, height, backward step, and step length after the position-mode transform.", "Consider horizontalizing target-bearing center displacement so target height affects orientation, not the candidate-center movement prior.", "Use the motion-realism rule for local V1 mixtures: step length, height delta, backward step, and yaw delta must be logged when collect_debug_stats is enabled.", "Add named profile configs before scale: v1_realistic_3family, v1_relaxed_3family, v1_current_5family, and upper_bound_free_shell.", "Treat forward_local, target_bearing_local, and lateral_target_bypass as the first production three-family profile; keep local_refinement and revisit_backtrack as ablation components until valid-count and gain diagnostics justify them.", "Keep upper_bound_free_shell as an explicit ablation recipe rather than the main V1 distribution.", "Allow per-rollout or per-step strategy_weights only after the fixed realistic mixture has smoke diagnostics.", "Keep frontier/missing-surface centroid behind clear data availability checks."]
acceptance = ["Main V1 candidate generation uses realistic local target-aware position families by default in the mixture config.", "Each candidate set records view strategy, position strategy, mixture id, sampler probability, component name, and motion/target diagnostics when requested.", "Entity-aware rollout smoke can generate target-directed candidates without using GT target pose in the main actor-visible protocol.", "The legacy shell can still be requested only as an explicit upper_bound_free_shell ablation.", "A profile matrix can compare v1_realistic_3family, v1_relaxed_3family, v1_current_5family, and upper_bound_free_shell on the same roots/targets/seeds.", "Docs and diagnostics state whether angular caps constrain only the raw draw or the final transformed movement direction.", "Existing candidate-generation tests continue to pass for legacy configs.", "Scale-readiness diagnostics show whether the realistic mixture produces enough valid target-supporting candidates before large rollout writes."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation", "rg -n \"v1_realistic_3family|v1_relaxed_3family|v1_current_5family|upper_bound_free_shell|target_bearing_local|lateral_target_bypass|position_id\" aria_nbv/aria_nbv/pose_generation aria_nbv/tests/pose_generation .configs docs .agents"]

[[todo]]
id = "todo-029"
title = "Audit actor-visible observed-target selector thresholds"
description = "Validate and tune the V1 actor-visible observed-target selector that scores predicted OBBs/classes/confidence, projected area, and semidense/weighted-EVL support without GT actor input."
priority = "high"
status = "todo"
labels = ["entity-aware", "target", "rri", "diagnostics"]
issue_ids = ["issue-020", "issue-007"]
loc_min = 200
loc_expected = 480
loc_max = 1100
context = ["Oracle target-task sampling now owns data-generation target pools; this TODO is only for the actor-visible V1 selector that a deployable scorer/Q_H input may consume later.", "The V1 protocol is OBS-SEL / PRED-Q / GT-EVAL: the selector uses observed/predicted OBBs, classes, confidence, smooth projected-area score, semidense support, and weighted EVL support; GT is label/evaluation only.", "Missing 2D projection is now a configurable penalty rather than full visibility credit, and support defaults are no longer the one-point smoke threshold.", "The selector exposes diagnostic_summary plus row-level confidence, projected-area, support, effective support, selection utility, invalid reasons, and GT match audit fields.", "The synthesis identified filters for projected area, distance, visible-surface fraction, GT mesh-face support after matched OBB crop, OBB tightness, category validity, and not-yet-complete targets.", "The current target score deliberately combines confidence, visibility, support, and support deficit as an implementation utility; the review warns that using the same support/visibility evidence for hard eligibility, target ranking, and GT match reliability can obscure failure causes.", "The current checkout clips projected 2D OBB area to image bounds before computing visibility, so the latest raw-area review finding is stale; a raw-vs-clipped audit channel may still be useful for diagnostics.", "Temperature-softmax target selection currently samples from raw product scores, unlike rollout branch selection's robust logits; this can become nearly uniform or too brittle depending on temperature and score scale."]
references = ["repo:docs/contents/thesis/questions.qmd#rq2-target-encoding", "repo:docs/contents/thesis/questions.qmd#rq3-candidates", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd#actor-visible-target-selection", "repo:.agents/skills/entity-aware-rri/SKILL.md", "repo:docs/contents/ase_dataset.qmd", "repo:.agents/work/target-selection-sampling/02-review-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md", "repo:aria_nbv/aria_nbv/data_handling/_target_selection.py", "repo:aria_nbv/tests/data_handling/test_target_selection.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "bib:EFM3D-straub2024"]
implementation_notes = ["Keep V0 GT-OBB sanity selection separate from the mandatory V1 observed-only selector; do not let the main selector inspect GT mesh crops before choosing a target.", "Keep score as actor-visible selection utility and keep GT match fields as oracle/evaluation audit fields.", "Separate audit channels for hard target eligibility, target-interest ranking, and GT association: report which gate failed rather than relying only on the product score or match score.", "Evaluate whether GT matching should rank by semantic compatibility plus 3D IoU/gap only, with support/visibility used for eligibility and audit rather than for choosing among GT matches.", "Keep clipped projected 2D OBB area as the V1 eligibility/scoring field; optionally persist raw_projected_area and clipped_projected_area separately for audit.", "Before broad generation, produce threshold diagnostics for selected, rejected, unmatched, ambiguous, unsupported, duplicate predicted-to-GT, and missing-projection cases, including IoU, match score, confidence, projected-area, and support histograms.", "Tune min_support_points, evl_support_weight, missing_projection_visibility_score, min_gt_iou, and min_gt_match_score on a trusted subset before scale; treat current min_support_points=3 as smoke-level until coverage histograms justify it.", "Add named target-selector profiles such as v1_smoke and v1_strict, and start strict calibration with n_min candidates such as 5, 10, and 20 before thesis-scale generation.", "Add robust target logits or stratified target sampling as an ablation for temperature-softmax top-K instead of relying only on raw product scores.", "Keep target selector output compatible with later VIN target fields, Q_H batches, and Rerun diagnostics.", "Do not allow empty GT mesh crops after matching to fall back to scene-level labels."]
acceptance = ["A small trusted subset can choose at least one eligible observed/predicted target or explain why none exists.", "Selection output includes entity id/class/observed OBB, matched GT OBB id, eligibility score components, support signals, effective support, and rejection reasons.", "Unsupported or complete targets are filtered before target RRI generation.", "Diagnostics summarize selected-target distributions, rejected target reasons, unmatched/ambiguous match rates, IoU/match-score/confidence/projected-area/support histograms, raw-vs-clipped projected-area behavior, and at least a few Rerun examples across snippets.", "A selector-profile or ablation report states whether smoke defaults are sufficient or a stricter V1 profile is needed before scale.", "Target softmax behavior is either robust-normalized/stratified or explicitly reported as raw-score sampling with entropy diagnostics.", "GT match audit can distinguish semantic mismatch, geometric mismatch, low actor-visible support, missing projection, and ambiguity.", "No main-protocol target selector path uses GT OBBs as actor-visible input."]
verification = ["cd aria_nbv && uv run pytest tests/data_handling/test_target_selection.py tests/rri_metrics tests/data_handling/test_vin_offline_store.py", "rg -n \"target selector|eligibility|projected_area|clipped_projected|raw_projected|visible_surface|selection_reason|robust target\" aria_nbv/aria_nbv aria_nbv/tests docs/contents .agents"]

[[todo]]
id = "todo-030"
title = "Add candidate-realism diagnostics by strategy"
description = "Measure and report candidate validity, collision rejection, clearance, view-direction distributions, RRI histograms, target visibility, no-depth/no-hit counts, and path feasibility per sampling strategy."
priority = "medium"
status = "todo"
labels = ["candidate-generation", "diagnostics", "experiments"]
issue_ids = ["issue-020", "issue-013"]
loc_min = 120
loc_expected = 320
loc_max = 760
context = ["Candidate-generation realism versus generalization remains an open RQ4 support question.", "CandidateSamplingResult now carries view strategy id, position id, component name, sampler probability, motion-realism extras, and target-bearing diagnostics that should be reused by audits.", "The synthesis calls out strategy-level diagnostics before broad candidate-generation changes are trusted.", "These diagnostics are needed to decide whether broader candidate distributions buy more than deeper VIN architecture work.", "The target-selection/candidate-sampling review flags that mixture components use full-shell counts rather than guaranteed valid-action counts; without post-pruning support reports, no-headroom outcomes can be confounded with collapsed candidate validity."]
references = ["repo:docs/contents/thesis/questions.qmd#rq4-support", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd#mixture-rollout-branch-selection", "repo:.agents/work/target-selection-sampling/02-review-gpt55pro.md", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation_rules.py", "repo:aria_nbv/aria_nbv/pose_generation/candidate_mixture.py", "repo:aria_nbv/aria_nbv/pose_generation/positional_sampling.py", "repo:aria_nbv/tests/pose_generation/test_candidate_mixture.py"]
implementation_notes = ["Aggregate metrics by candidate view strategy, position strategy, component, scene/snippet, stage, and target type where available.", "Compare D0 current/legacy shell, D1 local realistic, D2 target-bearing/lateral, D3 upper_bound_free_shell, and the review-proposed v1_realistic_3family / v1_relaxed_3family / v1_current_5family recipes before scale.", "Include valid ratio, valid_count_by_component, invalid_reason_by_component, oracle_gain_by_component, selected_frequency_by_component, collision/free-space rejection, motion-realism rejection, mesh clearance, distance to reference, final azimuth/elevation after position-mode transform, height delta, backward step, target distance/bearing, candidate RRI histogram, entity visibility, no-hit depth, and path feasibility.", "Add a min_valid_candidates_per_step and min_valid_candidates_per_component warning/blocker or an equivalent status helper before LRZ-scale generation.", "Keep diagnostics lightweight enough for smoke subsets and final report tables.", "Prefer existing candidate result masks/extras rather than recomputing expensive geometry unless needed."]
acceptance = ["A report or helper summarizes candidate realism metrics for at least the realistic local mixture and upper_bound_free_shell on a smoke subset.", "Metrics are grouped by view strategy, position strategy, and component and can reveal whether a sampler produces many invalid or no-hit candidates.", "The report identifies post-pruning valid-budget collapse before interpreting no-headroom or weak-Q_H outcomes.", "Component-level selected-frequency and oracle-gain summaries show whether local_refinement, revisit_backtrack, and lateral_target_bypass contribute useful actions.", "The output can feed the experiment registry or thesis figures without manual spreadsheet reconstruction."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation", "rg -n \"candidate realism|valid_ratio|valid_count_by_component|invalid_reason_by_component|selected_frequency_by_component|mesh_clearance|azimuth|target_visibility\" aria_nbv docs .agents"]

[[todo]]
id = "todo-031"
title = "Separate invalidity taxonomy from RRI labels"
description = "Introduce hard masks and explicit invalid candidate/target reason labels so feasibility is not encoded as low reconstruction quality."
priority = "high"
status = "todo"
labels = ["validity", "rri", "data", "rollouts"]
issue_ids = ["issue-021", "issue-007"]
loc_min = 180
loc_expected = 420
loc_max = 1000
context = ["The shared evidence protocol states that invalidity is not a low RRI class.", "The synthesis lists collision, outside bounds, no target visibility, bad frustum, unsafe transition, no depth hits, and outside EVL voxel extent as distinct invalid reasons.", "Entity-aware target selection, rollout storage, and candidate-query Q_H training all need hard validity masks and explicit unsupported-target/invalid-candidate reasons.", "Separate validity heads or scalar penalty rewards are ablations only after masks and reason codes are stable.", "The invalidity vocabulary is richer than the current population path: rollout.trace maps only a few sampler rule names to specific bits and otherwise collapses unresolved invalid candidates to SAMPLER_RULE_REJECTED. Before scale, reason histograms must show which reason codes are actually populated."]
references = ["repo:docs/contents/thesis/questions.qmd#rq-evidence-protocol", "repo:docs/contents/theory/rl_planning.qmd", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation_rules.py", "repo:aria_nbv/aria_nbv/rri_metrics/types.py", "repo:aria_nbv/aria_nbv/rollouts/trace.py", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md"]
implementation_notes = ["Define an enum or typed code table for invalid reasons and keep it separate from ordinal RRI bins.", "Propagate validity masks/reasons through candidate generation, oracle labeling, rollout traces, Q_H stores, and future VIN batches.", "Audit reason population from real smoke stores and add mapping hooks where important failures currently fall through to SAMPLER_RULE_REJECTED.", "Add hooks for separate validity/collision/invalid-reason heads or reward penalties only after labels are reliable.", "Keep hard masking as the first trusted baseline."]
acceptance = ["Invalid candidates can be represented with reason codes without assigning them an RRI ordinal class.", "Candidate/RRI ordering remains aligned when masks and reason codes are serialized.", "At least collision/out-of-bounds/no-depth/no-target-support/out-of-extent reasons are representable or explicitly deferred.", "A generated smoke/scale audit reports invalid reason histograms and the unresolved SAMPLER_RULE_REJECTED fraction before labels are trusted.", "Q_H and scorer training payloads can consume hard masks without treating invalid rows as the lowest target-RRI bin.", "Tests cover serialization and mask alignment."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation tests/data_handling/test_vin_offline_store.py tests/rri_metrics", "rg -n \"invalid_reason|validity_head|collision_head|candidate_valid\" aria_nbv/aria_nbv aria_nbv/tests docs/contents/thesis/questions.qmd"]

[[todo]]
id = "todo-033"
title = "Harden deterministic sharding for oracle and rollout generation"
description = "Turn the existing rollout shard-manifest and atomic-promotion tracer bullet into a production LRZ generation path for oracle cache and rollout shards, with real Slurm/Pyxis execution, complete source lineage, global run status, and resumable missing/failed-shard retry."
priority = "high"
status = "todo"
labels = ["data-generation", "slurm", "rollouts", "cluster"]
issue_ids = ["issue-022", "issue-018"]
loc_min = 240
loc_expected = 620
loc_max = 1400
context = ["Scaling rollouts requires cluster-safe generation rather than ad hoc local loops.", "Current code already plans deterministic source-row rollout shards and can build one shard through temp-write, validation, atomic rename, _owner.json, and _SUCCESS.json.", "The robust production-like sharding code currently covers rollout sidecars from an existing VIN offline source store. Building a larger VIN offline source cache with VinOfflineWriter is a separate missing campaign path and should not be confused with rollout generation readiness.", "The remaining blocker is production hardening: one real Slurm array task must own one final shard path, the LRZ template must call the real CLI without dry-run, and a run-level manifest must report pending/running/succeeded/failed/skipped/missing shards without a blind DSS tree scan.", "Do not convert rollout generation into a Lightning Trainer predict loop. Trainer/BasePredictionWriter can write distributed predictions, but the rollout pipeline needs shard ownership, source-row lineage, atomic promotion, failure markers, and resume semantics that belong in the CLI/write path.", "Use Fabric only as intra-shard device/distributed setup if one shard needs multi-GPU model inference; otherwise keep the writer a plain Python loop so Slurm controls parallelism.", "Submitit can be a later convenience launcher around nbv-build-rollouts, not a core dependency or replacement for Slurm arrays; Dask/Ray/Snakemake/Prefect/Luigi stay out until profiling shows real need.", "Source shard lineage must be complete enough to join rollout rows back to the immutable VIN offline root store and optional root candidate/root tensor payloads: source_store_dir, source manifest hash, cache version, source_shard_id, source_shard_row, sample key, split hash, root candidate policy, and retained source block availability must be validated and queryable.", "LRZ preflight must prove more than TOML parsing: at least one planned source row must load live snippet, GT mesh, OBB/target assets, candidate generator, renderer/scorer, DSS source paths, and writable temp/final output roots inside the cluster container.", "Path portability and config-hash semantics must be explicit: planning and execution should use the same LRZ/DSS path layout, or the shard manifest must distinguish semantic config identity from runtime paths.", "Parallel write races and resumability are high-priority risks before multi-hundred-GB generation.", "Deterministic sharding and resumable writes are a hard M2/M3 gate before full 100-scene/4,608-snippet target/RL generation and Q_H training data production.", "The multimodal rollout retention decision separates lean training shards from richer validation/audit shards: training shards keep required Q_H/model blocks, while validation/audit shards may retain one-step modality parity and heavier diagnostics for inspection.", "A run-level storage budget must be available before scale, including rows per shard, expected candidates/targets/recipes/horizon, depth retention, estimated GB per shard, file/chunk counts where practical, wall time, and retry/missing-shard accounting.", "Full 100-scene/4,608-snippet coverage remains the target, but a smaller scene-level subset can support a valid thesis result only if M1 passes and exact coverage gaps/blockers are reported separately for scenes, snippets, targets, trajectories, rollout seeds, transitions, and missing coverage."]
references = ["repo:aria_nbv/aria_nbv/rollouts/shard_manifest.py", "repo:aria_nbv/aria_nbv/rollouts/shards.py", "repo:aria_nbv/aria_nbv/rollouts/cli.py", "repo:aria_nbv/aria_nbv/data_handling/_offline_writer.py", "repo:aria_nbv/tests/rollouts/test_dataset_writer.py", "repo:.configs/build_rollouts_v1_lrz.template.toml", "repo:scripts/templates/lrz/rollout_generation.sbatch", "repo:.configs/lrz/README.md#resume-contract", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m5", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md", "repo:.agents/skills/lrz-ai-systems/SKILL.md", "repo:.agents/skills/lrz-ai-systems/references/slurm-partitions.md", "repo:aria_nbv/pyproject.toml", "repo:aria_nbv/uv.lock", "url:https://lightning.ai/docs/fabric/stable/", "url:https://lightning.ai/docs/pytorch/stable/common/trainer.html", "url:https://lightning.ai/docs/pytorch/stable/api/lightning.pytorch.callbacks.BasePredictionWriter.html", "url:https://github.com/facebookincubator/submitit", "url:https://github.com/Lightning-AI/pytorch-lightning/security/advisories/GHSA-w37p-236h-pfx3"]
implementation_notes = ["Keep the shard unit as a deterministic source-row shard for now; only add target/seed subshards if profiling shows one row can exceed walltime or memory.", "Add a run-level manifest/status command that indexes shard_entry, final_dir, temp_dir, content hashes, _SUCCESS, _FAILED markers, counts, runtime, storage footprint, and coverage without walking large payload arrays.", "Add or document an LRZ preflight that instantiates the configured source dataset inside the container and performs one live row load, target selection/match, candidate generation, render/scorer call, validation, and tmp/final write probe.", "Convert the LRZ rollout template from documentation-only dry-run to an opt-in real command path that invokes nbv-build-rollouts with --shard-manifest, --shard-id, --output-tmp, and --output-final.", "Validate source lineage against the immutable VIN store manifest and sample_index records before generation, and persist source_store_dir plus source block availability in owner/run manifests.", "Keep Lightning/Fabric out of the outer generation orchestrator; if Fabric is added, make it an implementation detail inside one shard and keep the CLI contract stable.", "Optionally add a submitit launcher only after the raw sbatch/Pyxis path is proven; it should submit the same CLI command and reuse the same manifests.", "Add a cluster-environment guard or documented preflight that rejects known-compromised Lightning 2.6.2/2.6.3 in LRZ images.", "Encode a retention profile per shard, at minimum training_core and rich_validation_audit, so loaders can skip unavailable optional blocks intentionally.", "Log SLURM_JOB_ID, SLURM_ARRAY_TASK_ID, node name, GPU type, config hash, runtime path policy, retention profile, and runtime metrics when present.", "Keep write operations idempotent and use temp paths followed by atomic commit.", "Dry-run full GT coverage and storage budget before launching heavy writes."]
acceptance = ["A dry-run and a real smoke command partition generation work into deterministic shards and use the same manifest entries.", "A real LRZ/Pyxis Slurm array can build at least one rollout shard, validate it, promote it atomically, and write _owner.json plus _SUCCESS.json.", "Preflight proves live snippet, GT mesh, target/OBB assets, render/scorer, DSS path resolution, and tmp/final write access for at least one planned source row.", "Completed shards are skipped on rerun without rewriting successful outputs.", "Failed shards leave actionable status and error metadata that a run-level status command can list.", "Atomic write semantics prevent partially written rollout groups from being treated as done.", "Rollout generation does not depend on Lightning Trainer epoch/predict semantics; any Fabric usage is intra-shard and preserves the CLI/write contract.", "Training shards can omit rich validation/audit optional blocks while retaining all required Q_H/model inputs and labels.", "Source lineage is complete enough to join rollout rows back to immutable VIN root rows and to report missing root candidates/root tensors explicitly.", "Run-level status reports storage footprint, expected-vs-observed shard counts, succeeded/failed/missing/skipped shards, and retry candidates.", "The sharding plan can scale from a small trusted subset to all 100 GT-mesh scenes and 4,608 snippet windows.", "Any smaller final-generation subset preserves scene-level splits and records exact blockers, achieved coverage, and missing coverage."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_dataset_writer.py tests/rollouts/test_zarr_store.py", "cd aria_nbv && uv run nbv-plan-rollout-shards --config-path ../.configs/build_rollouts_v1_smoke.toml --rows-per-shard 1 --output-manifest /tmp/rollout_shards.jsonl", "cd aria_nbv && uv run nbv-status-rollout-shards --shard-manifest /tmp/rollout_shards.jsonl --final-root /tmp/rollout_shards --output-json /tmp/rollout_shards_status.json", "cd aria_nbv && uv run nbv-build-rollouts --config-path ../.configs/build_rollouts_v1_smoke.toml --dry-run", "bash -n scripts/templates/lrz/rollout_generation.sbatch scripts/templates/lrz/rollout_generation_dry_run.sbatch", "cd aria_nbv && uv run python - <<'PY'\nimport importlib.metadata as m\nfor name in ('lightning', 'pytorch-lightning'):\n    try:\n        print(name, m.version(name))\n    except m.PackageNotFoundError:\n        print(name, 'not installed')\nPY", "rg -n \"SLURM_JOB_ID|ARRAY_TASK|atomic|pending|running|done|failed|rollout\" aria_nbv .configs docs .agents"]

[[todo]]
id = "todo-037"
title = "Create target-conditioned Q_H evidence report"
description = "Produce the report path that verifies whether learned Q_H beats one-step greedy/model scoring on cumulative root-normalized target gain under equal acquisition budget."
priority = "high"
status = "todo"
labels = ["vin", "rollouts", "experiments", "thesis", "q-learning"]
issue_ids = ["issue-013", "issue-018", "issue-028", "issue-031"]
loc_min = 160
loc_expected = 360
loc_max = 800
context = ["The new thesis success bar requires candidate-query Transformer Q_H to beat one-step greedy/model scoring on cumulative root-normalized target gain under equal acquisition budget.", "Bounded oracle lookahead is the upper-bound comparison; random-valid, oracle-greedy/lookahead, oracle-scored temperature-softmax, one-step model/VIN, and Q_H must be reported from compatible traces, while Gumbel remains preferred later diversity evidence.", "The synthesis warned that prior Optuna/VIN comparisons mixed multiple factors and need controlled ablations, so this report must lock seeds, splits, budgets, schedules, eval depth source, root frame policy, fusion config, and reward semantics.", "Full 100 GT-mesh scene / 4,608 snippet coverage remains the target, but a smaller scene-level held-out subset remains valid if M1 passes, root-gain target labels are trusted, Q_H comparison preserves scene-level splits, and exact coverage gaps are reported separately for scenes, snippets, targets, trajectories, rollout seeds, transitions, and blockers."]
references = ["repo:docs/contents/thesis/questions.qmd#rq2-offline-qh", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m5", "repo:docs/contents/theory/rl_planning.qmd", "bib:DoubleDQN-vanHasselt2015", "bib:IQL-kostrikov2021"]
implementation_notes = ["Define a compact report schema or script output covering cumulative root-normalized target gain, diagnostic state-relative target/scene RRI, acquisition cost, oracle-vs-predicted rank agreement, Q_H selected actions, calibration/stage shift, and representative Rerun failure cases.", "Use fixed seeds, scene-level splits, fixed budgets, fixed branch schedules, and one change at a time for any ablation matrix.", "Evaluate learned-policy-selected rollouts with the oracle target-root-gain return, not with the policy's own predicted Q values; keep target_rri as a state-relative diagnostic only.", "Make the report pass/fail the Q_H thesis success bar while also documenting whether Gumbel, IQL, or actor-critic bridge work is justified.", "Link report rows to configs, checkpoints, cache hashes, Zarr store versions, split manifests, reward semantics, eval-stream lineage, and achieved/missing supervision coverage."]
acceptance = ["There is a reproducible report path for the Q_H thesis success bar.", "The report compares candidate-query Transformer Q_H against random-valid, one-step oracle greedy, one-step model/VIN scoring, oracle-scored temperature-softmax, and bounded oracle lookahead; Gumbel comparison is preferred when available.", "All learned-policy-selected rollouts are oracle-evaluated with target_root_gain before they support thesis claims.", "At least one oracle-evaluated model-scored rollout smoke is represented once rollout traces exist.", "If final coverage is below 100 GT-mesh scenes / 4,608 snippets, the report states achieved and missing scenes, snippets, targets, trajectories, rollout seeds, transitions, scene-level split, and blocker.", "The report output can feed the final experiment registry and claim matrix."]
verification = ["rg -n \"Q_H|Double-Q|oracle-evaluated|calibration|stage-shift|top-k|target_root_gain\" docs .agents aria_nbv", "cd aria_nbv && uv run pytest tests/vin tests/lightning"]

[[todo]]
id = "todo-038"
title = "Write simulator and external-data expansion gate"
description = "Create a decision artifact that ranks Habitat, Isaac, 3DGS/world-model, and external dataset paths against thesis timing while preserving ASE mesh/oracle rollouts as the core substrate."
priority = "low"
status = "todo"
labels = ["simulator", "datasets", "scope", "thesis"]
issue_ids = ["issue-024"]
loc_min = 100
loc_expected = 240
loc_max = 560
context = ["The thesis-core simulator substrate is the ASE mesh/oracle counterfactual rollout loop, not Habitat or Isaac.", "The synthesis ranks Habitat fast RL sidecar, Isaac multimodal simulator, and semantic/VLM simulator paths as potential expansion paths, while the roadmap keeps generic simulator-backed online RL distinct from the RQ5 finite-candidate online-discrete Q_H bridge.", "The core scale limitation remains the 100 GT-mesh ASE scene subset and available mesh-supervised snippets.", "A simulator decision should not preempt M1-M5 ASE rollout and Q_H evidence."]
references = ["repo:docs/contents/thesis/questions.qmd#rq5-online", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m6", "url:https://aihabitat.org/docs/habitat-sim/habitat_sim.nav.html", "url:https://facebookresearch.github.io/projectaria_tools/docs/open_datasets/aria_synthetic_environments_dataset/ase_getting_started", "bib:GenNBV-chen2024", "bib:Hestia-lu2026"]
implementation_notes = ["Compare options by sensor fidelity, Aria/EFM compatibility, reward availability, setup cost, data license, scene diversity, and thesis timeline impact.", "Treat Habitat as a fast topology/RL sidecar and Isaac as a higher-fidelity external expansion only if access and evaluation are realistic.", "List external datasets such as Replica, HM3D, Matterport3D, ScanNet, Structured3D, Hypersim, InteriorNet, ProcTHOR, OmniObject3D, Objaverse, and Houses3K as candidates, not commitments.", "End with a go/no-go gate tied to M5/M6 evidence."]
acceptance = ["A short decision artifact states that ASE mesh/oracle rollouts are thesis-core while external simulator/data work is stretch or deferred.", "The artifact preserves the ASE mesh-supervised rollout path as the default until a gate is passed.", "Dataset/simulator recommendations name the evidence required to proceed.", "Public thesis docs avoid claiming generic simulator-backed online RL unless the gate is passed, while preserving the RQ5 finite-candidate online-discrete Q_H bridge."]
verification = ["cd docs && quarto render contents/thesis/roadmap.qmd contents/thesis/questions.qmd", "rg -n \"Habitat|Isaac|Replica|HM3D|Matterport|simulator-backed\" docs .agents"]

[[todo]]
id = "todo-048"
title = "Specify controlled VIN ablation and calibration gate"
description = "Turn the VIN action list into a concrete report slice covering richer Lightning metrics, calibration plots, controlled ablations, stage-aware checks, and a fixed seed/split/schedule protocol."
priority = "medium"
status = "todo"
labels = ["vin", "experiments", "calibration", "thesis"]
issue_ids = ["issue-013", "issue-007"]
loc_min = 160
loc_expected = 360
loc_max = 900
context = ["The distillation listed VIN baseline actions such as torchmetrics, predicted-RRI calibration plots, fixed ablation matrices, learnable CORAL/bin handling, stage-aware features or bins, candidate-relative encodings, reliability embeddings, and EVL checkpoint hardening.", "The active DB now has a Q_H evidence report, but it does not spell out the one-step VIN ablation and calibration protocol needed before VIN/model scoring is used as a Q_H comparator or rollout scorer.", "Prior VIN comparisons mixed multiple factors, so this slice should enforce one-change-at-a-time evidence."]
references = ["repo:docs/reference/aria_nbv.vin.model_v3.VinModelV3.qmd", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m2", "repo:aria_nbv/aria_nbv/vin/AGENTS.md", "bib:VIN-NBV-frahm2025"]
implementation_notes = ["Define a report schema before implementing new VIN heads.", "Keep fixed seeds, scene-level splits, training schedule, binner settings, and evaluation metrics constant across ablations.", "Include calibration and predicted-RRI semantics checks before model scores are used as Q_H comparators, rollout scorers, or planner features.", "Treat architecture additions such as candidate-relative fusion or RGB/DINO projection as candidate ablations, not simultaneous changes."]
acceptance = ["A VIN ablation plan or report template lists exact baseline, metrics, configs, seeds, splits, and one-change-at-a-time variants.", "Calibration plots or tables define predicted-RRI semantics and ordinal-bin interpretation.", "Stage-aware and imbalance/CORAL experiments have explicit acceptance criteria or are deferred.", "The report can feed todo-037 without overclaiming VIN as a planner."]
verification = ["rg -n \"VIN ablation|calibration|fixed seeds|stage-aware|CORAL|predicted RRI\" docs .agents aria_nbv", "cd aria_nbv && uv run pytest tests/vin tests/lightning"]

[[todo]]
id = "todo-049"
title = "Polish and freeze thesis seed"
description = "Make the Typst thesis seed advisor-ready with the five-role thesis boundary and mandatory target-conditioned Q_H scope."
priority = "high"
status = "todo"
labels = ["thesis", "typst", "advisor", "docs"]
issue_ids = ["issue-027"]
loc_min = 180
loc_expected = 520
loc_max = 1200
context = ["The old proposal and advisor handouts were merged into docs/typst/thesis/main.typ and retired to .agents/archive/docs/typst/thesis/ as provenance.", "The active thesis seed includes the proposal/research-contract, method, evaluation, schedule, outline, open decisions, and TODO-marked draft material, but still needs a final advisor-grade pass before freeze.", "The thesis seed should stay a readable research contract before it grows into the full manuscript: RQs/objectives, literature pointers, current method, evidence gates, compact schedule, and explicit draft/open-work markers.", "The thesis seed should explicitly separate implemented substrate, prerequisite/evidence protocol, hard quantitative thesis core, mandatory bridge design, and future-work extensions so it does not overclaim ARIA-NBV as a finished continuous-control or real-device RL policy.", "The hard thesis core now includes target-conditioned candidate-query Transformer Q_H over finite candidate sets trained from ASE oracle rollout data.", "Roadmap and questions now treat thesis-seed freeze as M0/M1 groundwork rather than a late writing afterthought."]
references = ["repo:docs/typst/thesis/main.typ", "repo:docs/typst/thesis/sections/03-method.typ", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m0", "repo:Makefile#thesis-pdf"]
implementation_notes = ["Use a consistent project name: ARIA-NBV for the thesis/system and VIN-style or Aria-VIN scorer for the one-step model family.", "Fix thesis seed metadata such as title spelling and examiner placeholders if the template still renders unfinished fields.", "Keep main.typ as the active thesis seed rather than a competing proposal handout; archived proposal/advisor files are provenance only.", "Add or preserve a compact five-role summary: implemented substrate; prerequisite/evidence protocol covering M1, V0/V1 target contracts, and rollout storage; hard quantitative thesis core covering observed target selector, predicted target-conditioned scorer/Q_H, random-valid/oracle-greedy/lookahead/oracle-scored temperature-softmax rollouts, candidate-query Transformer Q_H, and coverage reporting; mandatory bridge design covering online discrete Q_H, IQL, actor-critic and simulator paths; future-work extensions covering Gumbel later evidence, continuous policies, Habitat/Isaac, SceneScript, real-device guidance, and human-in-the-loop AR.", "Include the 100 GT-mesh ASE scenes, 4,608 snippet windows, and the possibility of a scene-level held-out thesis-grade subset only with exact coverage reporting.", "Move invalidity-is-not-low-RRI into the thesis seed method/objectives text, not only the research questions.", "State that `make thesis-pdf` must succeed and the generated thesis PDF should be tracked only after actual advisor-facing freeze."]
acceptance = ["main.typ renders to an advisor-readable thesis seed PDF with make thesis-pdf.", "The thesis seed body is a compact research contract rather than a long literature essay.", "The included thesis sections are intentional and there are no ambiguous orphan-section assumptions in the compiled contract.", "Thesis seed, roadmap, and questions use the same central thesis claim and do not overclaim full continuous RL or finished real-device NBV policy behavior.", "The thesis seed clearly distinguishes implemented substrate, prerequisite/evidence protocol, hard quantitative thesis core, mandatory bridge design, and future-work extensions.", "Dataset limitations, Q_H success bar, LRZ/full-scale or held-out-subset coverage gate, metadata placeholders, invalidity semantics, and draft markers are stated or explicitly blocked in the thesis seed.", "Generated thesis PDF is tracked only after actual advisor-facing freeze."]
verification = ["make thesis-pdf", "rg -n \"ARIA-NBV|Aria-NBV|Aria-VIN-NBV|Qualitaets|Qualitäts|secondExaminer|invalidity|low RRI|100 ASE|4,608|883|706/177\" docs/typst/thesis docs/contents/thesis", "cd docs && quarto render contents/thesis/roadmap.qmd contents/thesis/questions.qmd"]

[[todo]]
id = "todo-050"
title = "Audit bibliography and citation metadata"
description = "Clean docs/references.bib before thesis-seed freeze so advisor-facing claims cite primary, verified, and non-duplicated sources."
priority = "high"
status = "todo"
labels = ["bibliography", "thesis", "docs", "citations"]
issue_ids = ["issue-027"]
loc_min = 120
loc_expected = 380
loc_max = 900
context = ["The follow-up reviews identified bibliography slop that should not ship in the proposal: Wikipedia entries for core technical concepts, a generated `Citations by ChatGPT` comment, duplicate FiLM records, mixed entry formatting, and possibly unverified 2025/2026 metadata.", "docs/AGENTS.md makes docs/references.bib the single bibliography source of truth for docs and Typst surfaces.", "Citation hygiene is part of the proposal freeze gate because the PDF is advisor-facing and literature-backed.", "The accepted cleanup scope is proposal-critical first: advisor-facing proposal/thesis docs must cite primary verified sources, while unused noisy entries can be quarantined or handled later if they do not render into advisor PDFs.", "Source policy remains: add all cited sources to docs/references.bib, but add to docs/literature/sources.jsonl only implementation-shaping local corpus sources. CQL, BCQ, Decision Transformer, and DQN are already local-corpus additions; POMCP and submodularity references stay bibliography-only unless later needed locally."]
references = ["repo:docs/AGENTS.md", "repo:docs/references.bib", "repo:docs/typst/thesis/main.typ"]
implementation_notes = ["Remove or quarantine Wikipedia entries for active perception, Chamfer distance, point clouds, attention, ordinal regression, histograms, frusta, spherical harmonics, and similar core technical concepts when they are cited by proposal/thesis-critical output; replace with primary or durable scholarly sources where needed.", "Remove the `Citations by ChatGPT` comment and deduplicate FiLM records from proposal-critical bibliography output.", "Verify metadata for Hestia, ObjectCentricNBV, semantic/dynamic 3DGS, Next Best Sense, and other 2025/2026 proposal-cited entries against primary paper pages, arXiv, DOI, or Semantic Scholar before proposal submission.", "Verify that proposal-cited CQL, BCQ, DQN, Decision Transformer, POMCP, Krause/Golovin submodularity, and Lauri et al. NBV submodularity entries are present with primary metadata; keep local literature-corpus additions limited to implementation-shaping sources.", "Check that all proposal citation keys compile and that uncited noisy entries do not bloat the proposal bibliography."]
acceptance = ["docs/references.bib has no generated-citation comments, duplicate FiLM entries, or Wikipedia entries cited by the thesis seed for core technical concepts.", "Metadata for thesis-seed-cited recent papers has a durable source pointer or is intentionally marked for advisor review.", "make thesis-pdf succeeds after the bibliography cleanup.", "The thesis seed bibliography remains focused on thesis-core and controlled-reference papers."]
verification = ["rg -n \"Wikipedia|Citations by ChatGPT|perez2017film|FiLM-perez|Wiki-\" docs/references.bib", "make thesis-pdf", "cd docs && quarto render contents/thesis/roadmap.qmd", "cd docs && quarto render contents/thesis/questions.qmd"]

[[todo]]
id = "todo-051"
title = "Mirror top thesis blockers to GitHub issues"
description = "Create a small GitHub collaboration mirror for the top local agents-db blockers while keeping .agents/*.toml as the source of truth."
priority = "high"
status = "todo"
labels = ["github", "governance", "thesis", "collaboration"]
issue_ids = ["issue-011", "issue-027"]
loc_min = 120
loc_expected = 260
loc_max = 620
context = ["The follow-up review found that GitHub Issues do not reflect the real local backlog, which can mislead collaborators who do not inspect .agents/issues.toml and .agents/todos.toml.", "The accepted decision for this pass is to add a local TODO only and not create remote GitHub issues yet.", "The mirror should expose only the top collaboration-facing blockers and should route detailed acceptance criteria back to the local agents DB."]
references = ["repo:.agents/AGENTS_INTERNAL_DB.md", "repo:.agents/issues.toml", "repo:.agents/todos.toml", "url:https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/about-issues"]
implementation_notes = ["Do not replace .agents/*.toml with GitHub Issues; use GitHub as a collaboration and visibility mirror.", "Create or update GitHub milestones such as M1 Contract Freeze, Thesis Proposal Freeze, M3 Target RRI, M5 Rollout Evaluation, Infrastructure/CI, and Experiment Evidence.", "Mirror at least these local blockers when GitHub sync is requested: M1 contract freeze, proposal polish/freeze, bibliography audit, entity-aware oracle RRI, target-aware candidate mixture and target selector, invalidity reason contract, bounded oracle lookahead versus greedy, root CI/pre-commit, and experiment registry/claim matrix.", "Classify existing GitHub issue #7 about MPS compatibility as not thesis-critical unless local compute plans make MPS necessary."]
acceptance = ["GitHub contains a compact set of issues/milestones that point back to the local agents DB records.", "No local DB detail is lost or moved exclusively to GitHub.", "The README or contributor guidance explains that .agents/*.toml remains the detailed source of truth if this mirror becomes active.", "The mirror avoids exposing internal agent-only scratch material."]
verification = ["make agents-db", "gh issue list --limit 50 || true", "rg -n \"GitHub|milestone|agents DB|source of truth\" README.md .agents docs"]

[[todo]]
id = "todo-052"
title = "Implement target-conditioned candidate-query Transformer Q_H baseline"
description = "Train and evaluate a finite-candidate target-conditioned candidate-query Transformer model that predicts bounded cumulative root-normalized target gain from ASE oracle rollout traces."
priority = "high"
status = "todo"
labels = ["rl", "q-learning", "rollouts", "target", "thesis"]
issue_ids = ["issue-028", "issue-018", "issue-019", "issue-020", "issue-021", "issue-031"]
loc_min = 420
loc_expected = 900
loc_max = 1800
context = ["The thesis hard core now requires Q_H, not only a one-step scorer or optional late RL.", "Q_H is a candidate-query Transformer over scene, target, history, and candidate tokens that outputs one masked finite-horizon Q value per candidate.", "Q_H is trained from ASE mesh/oracle counterfactual rollout traces and predicts bounded cumulative root-normalized target gain over finite candidate sets; target_rri remains a state-relative diagnostic/VIN compatibility label.", "Prerequisites include the M1 contract report, V1 OBS-SEL / PRED-Q / GT-EVAL target protocol, mixed observed candidate sets, hard invalidity masks/reason codes, deterministic rollout evidence, oracle-scored temperature-softmax diversity traces, scene-level splits, Zarr-first rollout/Q storage, and LRZ scale readiness.", "Fitted Double-Q-style targets are first; IQL is a second offline-RL ablation; SB3 DQN is only relevant after an online Gymnasium simulator exists.", "Full 100 GT-mesh scene / 4,608 snippet coverage remains the target, but a smaller scene-level held-out subset can remain valid if M1 and target-root-gain evidence pass and exact coverage gaps are reported."]
references = ["repo:docs/contents/thesis/questions.qmd#rq2-offline-qh", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m5", "repo:aria_nbv/aria_nbv/rollouts/AGENTS.md", "repo:aria_nbv/aria_nbv/rollouts/trace.py", "bib:DoubleDQN-vanHasselt2015", "bib:IQL-kostrikov2021", "bib:VIN-NBV-frahm2025"]
implementation_notes = ["Define Q_H targets as bounded cumulative root-normalized target gain over horizon H with hard masks for invalid candidate rows.", "Implement Q_H as a candidate-query Transformer that encodes scene/target/history/candidate tokens and returns one masked Q value per finite candidate.", "Start with a finite-action batch learner over candidate tables and target-conditioned features; use Double-Q target selection/evaluation separation to reduce overestimation.", "Keep crop/entity tokens, scalar motion/rule penalties, and richer modality synthesis as extensions after the pure cumulative target-root-gain result is reportable.", "Evaluate selected actions through oracle rollouts rather than only supervised Q loss or predicted Q values.", "Train and evaluate on a smoke subset first, then scale toward all 100 GT-mesh scenes and 4,608 snippet windows once LRZ/storage gates pass; if blocked, report exact achieved coverage and blocker."]
acceptance = ["Q_H can train and evaluate on a trusted smoke rollout store with scene-level splits and replayable lineage.", "Q_H beats one-step greedy/model scoring on cumulative root-normalized target gain under equal acquisition budget in the thesis evaluation protocol or produces a documented blocker/failure report.", "Bounded oracle lookahead remains reported as an upper-bound baseline.", "The first comparison includes random-valid, one-step oracle greedy/lookahead, one-step model/VIN scoring, oracle-scored temperature-softmax, and Q_H; Gumbel is preferred later evidence.", "All Q_H-selected rollouts used for claims are oracle-evaluated.", "Trace/config hashes, split manifest, target protocol version, storage version, reward semantics, achieved coverage, and missing coverage are linked from the evidence report."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation tests/vin tests/lightning", "rg -n \"Q_H|Double-Q|fitted|cumulative root-normalized target gain|target_root_gain|candidate_valid\" aria_nbv docs .agents"]

[[todo]]
id = "todo-053"
title = "Implement V1 observed-target / GT-label protocol"
description = "Implement the OBS-SEL / PRED-Q / GT-EVAL target contract that uses observed or predicted OBBs as actor-visible inputs and matched GT OBB crops for target-RRI labels and evaluation."
priority = "high"
status = "todo"
labels = ["target", "entity-aware", "rri", "contracts", "thesis"]
issue_ids = ["issue-020", "issue-007", "issue-028"]
loc_min = 260
loc_expected = 620
loc_max = 1400
context = ["The main thesis protocol cannot use GT OBBs as actor-visible target input; GT remains label/evaluation only.", "Oracle target-task sampling owns data-generation target pools; V1 OBS-SEL / PRED-Q / GT-EVAL owns actor-visible target inputs for a deployable target-conditioned scorer or Q_H path.", "V0 GT OBB input remains a sanity/upper-bound actor-input path, while V1 is mandatory before deployable actor-input target claims.", "Target rows now store observed/predicted OBB input, actor-visible score components, hard invalid reasons, matched GT ids, gt_match_iou, and a distinct gt_match_score that combines IoU with observed support/visibility reliability.", "Matching currently resolves compatible class, IoU, visibility/support, projected area, and point support; any additional criterion must be named from a concrete observed failure mode.", "The numeric target-match and eligibility thresholds are not thesis-frozen yet. Before large generation, the V1 protocol needs reports for confidence, IoU/match score, top-1/top-2 ambiguity gap, support counts, projected area, and target-crop validity."]
references = ["repo:docs/contents/thesis/questions.qmd#rq2-target-encoding", "repo:docs/contents/thesis/questions.qmd#rq4-planning", "repo:docs/contents/thesis/questions.qmd#rq5-bridge", "repo:.agents/skills/entity-aware-rri/SKILL.md", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md", "repo:aria_nbv/aria_nbv/data_handling/_target_selection.py", "repo:aria_nbv/tests/data_handling/test_target_selection.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "bib:EFM3D-straub2024"]
implementation_notes = ["Keep the typed target record aligned with observed/predicted OBB input, matched GT OBB, class/confidence/support signals, match metadata, and invalid target reasons.", "Keep target selector and candidate generator from inspecting GT crops before action selection.", "Persist target protocol version, gt_match_iou, gt_match_score, match status, effective support, and invalid reasons into rollout/Q_H stores and diagnostics.", "Add a scale-readiness audit for threshold choices: selected target counts, unmatched/ambiguous rates, top-1/top-2 gap, IoU/match-score histograms, support/projected-area histograms, target class distribution, and empty-crop/unsupported-target rates.", "Make Rerun and Streamlit diagnostics display actor-visible observed/predicted target evidence together with clearly labeled matched-GT OBB/crop overlays as oracle/evaluation diagnostics.", "Reject or visibly label any GT-derived field that crosses into actor-visible V1 input blocks."]
acceptance = ["A small trusted subset can produce OBS-SEL / PRED-Q / GT-EVAL samples with observed/predicted target inputs and matched GT target-RRI labels.", "V0 GT-input and V1 observed-input paths are named separately in configs, traces, reports, and docs.", "Unmatched, unsupported, or empty-crop targets fail with explicit reasons rather than falling back to scene-level labels.", "Target threshold reports make the current acceptance values defensible or explicitly block scale with measured failure modes.", "Rerun and Streamlit can inspect the selected target with observed/predicted OBB/crop evidence and a clearly labeled matched-GT overlay.", "The target record is consumable by the target-conditioned scorer and Q_H training payload."]
verification = ["cd aria_nbv && uv run pytest tests/rri_metrics tests/data_handling tests/pose_generation", "rg -n \"OBS-SEL|PRED-Q|GT-EVAL|target_protocol|matched_gt|observed.*OBB\" aria_nbv docs .agents"]

[[todo]]
id = "todo-055"
title = "Apply source-backed doc alignment review polish"
description = "Apply the curated page-level edits from the doc-alignment review to literature, roadmap, and research-question pages without creating a backlog item for every sentence."
priority = "high"
status = "todo"
labels = ["docs", "literature", "thesis", "advisor", "review"]
issue_ids = ["issue-003", "issue-027", "issue-028"]
loc_min = 160
loc_expected = 420
loc_max = 900
context = ["The doc-alignment review says the thesis spine is now conceptually strong, but advisor-facing polish and source-backed caveats remain across the literature index, Project Aria, PB-NBV, RL planning, 3DGS, VIN-NBV, GenNBV, Hestia, EFM3D, SceneScript, roadmap, and research questions.", "The accepted ingestion policy is curated: keep one focused todo for source-backed wording and evidence-gate refinements instead of one backlog entry per minor prose suggestion.", "The current accepted doc spine uses five thesis questions, candidate-query Transformer Q_H as mandatory thesis core, V1 OBS-SEL / PRED-Q / GT-EVAL as main target protocol, invalidity/scale as shared evidence protocol constraints, and continuous actor-critic/online learning as post-Q_H bridge work.", "Implementation must re-check current branch state before editing and skip findings already resolved locally."]
references = ["repo:docs/contents/literature/index.qmd", "repo:docs/contents/literature/project_aria.qmd", "repo:docs/contents/literature/pb_nbv.qmd", "repo:docs/contents/literature/rl_planning.qmd", "repo:docs/contents/literature/active_3dgs_nbv.qmd", "repo:docs/contents/thesis/roadmap.qmd", "repo:docs/contents/thesis/questions.qmd"]
implementation_notes = ["Add a short literature-index reading guide and link adoption decisions to roadmap/RQ anchors where useful.", "Project Aria wording should clarify that actor-visible may mean offline/replay or post-processed Aria-compatible state, and target selection uses predicted OBBs, semidense support, EVL evidence, and pose history rather than GT labels.", "PB-NBV should gain a concrete ARIA-NBV proposal-ablation hook and sharper warning that projection/frontier area can repeat coverage-style large-surface bias.", "RL planning should distinguish one-step Q_1 from finite-horizon Q_H, warn that Q_H maximizes over available finite candidates rather than continuous poses, name native PyTorch Lightning/config implementation as the first path, and list Q_H acceptance gates.", "3DGS/FOV-HPE wording should keep those methods as proposal/diagnostic or downstream-task examples and require Spearman/top-k agreement against scene and target RRI for proxy scores.", "Roadmap/questions should remove placeholder smell such as `criterion X`, clarify equal acquisition-budget semantics, false-positive target evaluation, tiered M5 stochastic evidence, full-scale target plus fallback coverage reporting, and the plain-language meaning of candidate-query Transformer Q_H."]
acceptance = ["The touched pages preserve the core thesis claim: target-conditioned finite-candidate Q_H is mandatory and continuous RL/simulator/3DGS control remain gated follow-up.", "No page reintroduces GT target leakage as actor-visible input for the main V1 protocol.", "Review suggestions already false on the current branch, such as missing API reference pages or missing VIN figure, are not implemented as stale fixes.", "Roadmap/questions/literature wording distinguishes required thesis evidence from preferred/stretch evidence.", "All factual additions are source-backed by local docs, code, papers, or existing literature-review references."]
verification = ["cd docs && quarto render contents/literature/index.qmd", "cd docs && quarto render contents/literature/project_aria.qmd", "cd docs && quarto render contents/literature/pb_nbv.qmd", "cd docs && quarto render contents/literature/rl_planning.qmd", "cd docs && quarto render contents/literature/active_3dgs_nbv.qmd", "cd docs && quarto render contents/thesis/roadmap.qmd", "cd docs && quarto render contents/thesis/questions.qmd", "rg -n \"criterion `X`|criterion X|actor-visible.*GT|continuous RL.*thesis-core\" docs/contents/literature docs/contents/thesis"]

[[todo]]
id = "todo-054"
title = "Enrich package docstrings from external stack contracts"
description = "Move the useful ATEK, EFM3D, and Project Aria contract facts into concise package docstrings on the owning ARIA-NBV surfaces."
priority = "medium"
status = "todo"
labels = ["docs", "docstrings", "data", "geometry", "rri", "simplification"]
issue_ids = ["issue-003", "issue-007", "issue-010"]
loc_min = 120
loc_expected = 360
loc_max = 900
context = ["The noisy public ext-impl pages were archived and distilled into .agents/references/external_stack_contracts.md.", "The retained facts are most useful at owning package contracts: data views, dataset loading, candidate generation, rendering, RRI metrics, and VIN/EVL feature provenance.", "The cleanup decision explicitly deferred Python edits so the docs/archive change can stay separate from package API docstring work."]
references = ["repo:.agents/references/external_stack_contracts.md", "repo:aria_nbv/aria_nbv/data_handling/efm_views.py", "repo:aria_nbv/aria_nbv/data_handling/efm_dataset.py", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation.py", "repo:aria_nbv/aria_nbv/rendering/unproject.py", "repo:aria_nbv/aria_nbv/rri_metrics/oracle_rri.py", "repo:aria_nbv/aria_nbv/vin/types.py"]
implementation_notes = ["Use concise Google-style docstrings; do not paste vendor catalogs or tutorial examples into Python.", "Prioritize shapes, frames, units, actor-visible versus oracle-only boundaries, upstream provenance, and failure modes.", "Keep `PoseTW`, `CameraTW`, and `ObbTW` semantics explicit where they cross package boundaries.", "Link package references and public docs rather than reintroducing public ext-impl pages."]
acceptance = ["Core data/geometry/RRI/VIN package surfaces expose the relevant ATEK/EFM3D/Project Aria contract facts in concise docstrings.", "No public docs regain raw vendor-stack dumps or ext-impl links.", "Docstrings distinguish observed EVL/MPS evidence from GT mesh/OBB supervision and evaluation assets.", "Generated API docs can surface the enriched contracts without needing separate public ext-impl pages."]
verification = ["cd aria_nbv && uv run ruff check aria_nbv/data_handling aria_nbv/pose_generation aria_nbv/rendering aria_nbv/rri_metrics aria_nbv/vin", "cd aria_nbv && uv run pytest tests/data_handling tests/pose_generation tests/rendering tests/rri_metrics tests/vin", "rg -n \"contents/ext-impl|External Implementations\" docs aria_nbv/aria_nbv -g '!docs/_site/**'"]

[[todo]]
id = "todo-058"
title = "Define rollout multimodal training and audit blocks"
description = "Specify the rollouts.zarr optional-block contract that keeps Q_H training shards lean while preserving rich validation/audit modality parity for inspection."
priority = "high"
status = "todo"
labels = ["rollouts", "zarr", "data", "modalities", "q-learning"]
issue_ids = ["issue-018", "issue-022", "issue-028", "issue-031"]
loc_min = 140
loc_expected = 360
loc_max = 900
context = ["The multimodal rollout alignment locks V1 strict visibility: observed/predicted inputs are actor-visible, while GT OBBs, crops, meshes, and RRI remain oracle/evaluation diagnostics.", "Training shards must include all Q_H/model-required inputs and labels, but validation/audit shards should be able to retain richer one-step modality parity for Rerun and Streamlit inspection.", "Rollout lineage must identify the oracle eval stream separately from actor-visible data: eval depth source, root RGB/depth frame ids, ray-distance vs z-depth convention, canonical fusion settings, reward mode, and legacy semi-dense-root diagnostic availability.", "RGB-D retention is selected-child-action only rather than every shell proposal; lightweight candidate tables still cover every shell candidate for scoring, masks, and provenance.", "The chosen one-step target-conditioned dataset gate is an H=1 rollout sidecar/profile produced by the current rollout writer. It consumes vin_offline as immutable source substrate and records target records, matched GT-eval metadata, candidate masks/reasons, and target-root-gain rewards with target-RRI diagnostics without introducing a separate target_labels_v0 writer.", "The current persisted selected-depth artifact is fixed 240x240 in rollouts.zarr; 224x224 is a reader/model transform unless a future retention profile explicitly changes stored resolution.", "Current code/docs now align on schema 1.0-target-rollout-core, selected_depth/, optional sampled/audit target_eval_crops/, actor_action_mask as the canonical action-feasibility mask, and persisted q_h/ as root-normalized rewards plus selected-transition TD arrays.", "The target-selection/candidate-sampling review's position_id-not-persisted finding is stale for the current schema because position_id is now hot candidate/q_h provenance and mirrored in candidate_diagnostics for inspection.", "The same review questions whether target_eval_crops should be written for thesis-scale training shards by default; retention profiles should distinguish training_core provenance from audit-heavy crop payloads.", "Current rollouts_v1_smoke.zarr is stale if it still validates as schema 0.6-rollout-core; stale or old local rollout artifacts should be regenerated, not migrated or supported through compatibility readers."]
references = ["repo:.agents/issues.toml#issue-018", "repo:.agents/todos.toml#todo-033", "repo:.agents/todos.toml#todo-053", "repo:.agents/references/rollout_zarr_q_invalidity_contract.md", "repo:.agents/work/target-selection-sampling/02-review-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md", "repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/aria_nbv/rollouts/counterfactuals.py", "repo:aria_nbv/aria_nbv/data_handling/README.md#manifest-backed-rollout-store", "repo:docs/typst/shared/data-layout-trees.typ#rollout-zarr-tree", "repo:docs/figures/diagrams/data_handling/mermaid/rollout_generation_sequence.mmd", "repo:docs/figures/diagrams/data_handling/mermaid/offline_rollout_physical_layout.mmd", "context7:/zarr-developers/zarr-python", "bib:EFM3D-straub2024", "bib:ProjectAria-ASE-2025"]
implementation_notes = ["Define required training_core blocks for target record, candidate table and masks, lineage, sampling provenance, selected-child RGB-D/features, oracle labels, TD/return fields, eval-stream metadata, and split/config hashes.", "Ensure training_core exposes or can efficiently join strategy_id, position_id, mixture_id, sampler_probability, and component provenance for every actor_action_mask candidate row; validator should reject actor-selectable rows with missing/non-placeholder provenance.", "Add an H=1 target-label retention profile or config recipe that disables unnecessary multi-step/heavy blocks while preserving Rerun-inspectable target/candidate/mask/target-RRI facts.", "Define rich_validation_audit blocks for one-step modality parity, EFM/ATEK predictions, observed/predicted OBBs, matched GT OBB and crop, RRI components, legacy semi-dense-root diagnostics, diagnostic point clouds, target_eval_crops payloads, and viewer artifact pointers.", "Represent block availability in root/group metadata so lean readers can skip missing optional blocks without guessing.", "Make target_eval_crops payload retention explicit: keep it required only for audit profiles or justify it as a training_core requirement with a concrete consumer and storage budget.", "Store selected-child RGB-D with explicit pose, intrinsics, 240x240 persisted selected-depth resolution, dtype, modality source, feature provenance, and retention profile metadata; treat 224x224 as a reader transform by default.", "Keep aria_nbv/data_handling/README.md, docs/typst/shared/data-layout-trees.typ, generated rollout tree figures, Mermaid diagrams, and local docs aligned to schema 1.0-target-rollout-core, selected_depth/, optional sampled/audit target_eval_crops/, actor_action_mask, selected-transition q_h/ semantics, and explicit eval-stream lineage.", "Schema 1.0 already pruned candidate_valid_mask, padded_mask, heavy_diag_available_mask, selection_entropy, steps/transition_id, dictionaries/transition, and candidate-major q_h bootstrap/scene-RRI arrays while adding root-gain rewards, hot position_id provenance, and optional target eval crop payloads; remaining retention work should justify any denormalized rollout scene_id/snippet_id/split_id and keep scene-RRI fields as optional factual diagnostics that may be NaN when disabled.", "Do not add backward-compatibility readers or migrations for old rollout stores; regenerate stale artifacts under the current writer.", "Keep GT-derived data out of actor-visible V1 fields; if present in audit blocks, label it oracle/eval-only."]
acceptance = ["A written or implemented rollouts.zarr contract distinguishes required training_core blocks from optional rich_validation_audit blocks.", "The H=1 target-conditioned rollout sidecar/profile can serve as the one-step target-label gate before H=3+ rollout scale, without mutating vin_offline or requiring a separate target_labels_v0 writer.", "Lean training shards validate and load required Q_H/model inputs even when rich validation/audit blocks are absent.", "Actor-selectable candidate rows have joinable strategy_id, position_id, mixture_id, sampler_probability, and component provenance, either in the factual candidate table or a required aligned diagnostics table.", "Rollout metadata records eval depth source, root frame ids/timestamps, depth convention, canonical fusion config, reward mode, and legacy diagnostic availability.", "Validation/audit shards can expose one-step modality parity, target_eval_crops, and selected-child RGB-D/features for Rerun and Streamlit without duplicating full ASE meshes.", "Target_eval_crops storage is gated by retention profile or justified as a training_core requirement with measured footprint and consumer.", "Selected-depth persistence defaults to 240x240 in rollouts.zarr, and every stored render records pose, intrinsics, source, resolution, dtype, and feature provenance; 224x224 is documented as a training-reader transform unless a future profile opts into a different stored artifact.", "README and used figures agree on schema 1.0-target-rollout-core, selected_depth/, optional sampled/audit target_eval_crops/, fixed selected-depth storage, actor_action_mask, selected-transition q_h/ semantics, and the fact that selected/parent depth is materialized successor-state history rather than pre-selection actor-visible evidence.", "Low-value or misleading persisted fields are removed, made derived, or explicitly justified as audit/scan conveniences before large-scale generation; no stale-schema compatibility is required.", "Any actor-visible V1 field derived from GT OBB, GT crop, or GT mesh is rejected or clearly marked oracle/eval-only."]
verification = ["make agents-db AGENTS_ARGS='validate'", "make agents-db", "cd aria_nbv && uv run pytest tests/rollouts/test_zarr_store.py tests/rollouts/test_dataset_writer.py -q", "rg -n \"training_core|rich_validation_audit|target_eval_crops|position_id|selected-depth|selected_depth|240|224|schema 0.4|schema 0.5|depths/\" .agents aria_nbv docs"]

[[todo]]
id = "todo-059"
title = "Add full branch and multimodal inspection to Rerun and Streamlit"
description = "Make both Rerun recordings and the Streamlit app capable of inspecting rollout branches, sampling metadata, selected targets, selected-child RGB-D, and Zarr/entity trees."
priority = "high"
status = "todo"
labels = ["rerun", "streamlit", "rollouts", "zarr", "diagnostics"]
issue_ids = ["issue-018", "issue-019", "issue-020"]
loc_min = 180
loc_expected = 420
loc_max = 1100
context = ["The multimodal rollout alignment requires both Rerun and Streamlit to support full branch inspection rather than treating one viewer as a reduced fallback.", "Streamlit should own explicit controls for root snippet, rollout/chain, branch, step, modality, target, and retention profile selection.", "Rerun should expose branch/chain timelines, stable entity paths, sampling metadata, selected target overlays, selected-child RGB-D/crops, and Zarr-tree/entity-tree metadata in recordings.", "The phrase SAR array tree was resolved as the rollouts.zarr group/array tree; inspectors should show group names, shapes, dtypes, chunks, attrs, and optional-block availability.", "Implementation must re-check current Rerun, Streamlit, and Zarr docs through Context7 before committing viewer API assumptions."]
references = ["repo:.agents/skills/rerun-nbv-inspector/SKILL.md", "repo:.agents/skills/rerun-nbv-inspector/references/nbv-inspector-contract.md", "repo:aria_nbv/aria_nbv/rerun_inspector/_rollout_zarr.py", "repo:aria_nbv/aria_nbv/app/panels/rl.py", "repo:aria_nbv/aria_nbv/app/config.py", "repo:.agents/references/context7_library_ids.md", "context7:/rerun-io/rerun", "context7:/websites/streamlit_io", "context7:/zarr-developers/zarr-python"]
implementation_notes = ["Add Streamlit controls for root snippet, rollout row/chain id, branch id, step index, modality toggles, target overlays, and retention profile availability.", "Add Rerun branch/chain timelines and stable entity paths that let a viewer inspect selected and retained branches with sampling policy, score source, seed, horizon, branch/beam caps, and per-step metadata.", "Display actor-visible observed/predicted target evidence together with clearly labeled matched-GT OBB/crop overlays as oracle/eval diagnostics.", "Expose selected-child RGB-D/crop and selected-child feature provenance when the rollout store provides those blocks.", "Add a Zarr tree summary and a Rerun entity tree summary using the project Console and/or viewer metadata, including shapes, dtypes, chunks, attrs, and missing optional-block warnings."]
acceptance = ["A single H3/B3 root snippet can be inspected in Streamlit by choosing rollout/chain, branch, step, modality, and target controls.", "A saved Rerun recording for the same root exposes branch/chain timelines, stable entity paths, sampling policy metadata, selected target overlays, selected-child RGB-D/crop, and Zarr/entity tree metadata.", "Both viewers show what sampling policy and branch schedule produced the selected branch.", "Lean stores without rich optional blocks report missing modality warnings instead of failing branch inspection.", "Implementation references current Context7 documentation for Rerun, Streamlit, and Zarr before relying on external API behavior."]
verification = ["cd aria_nbv && uv run pytest tests/rerun_inspector tests/app/panels/test_rl_panel.py", "cd aria_nbv && uv run ruff check aria_nbv/rerun_inspector aria_nbv/app", "rg -n \"branch|chain|rollout_step|Zarr tree|entity tree|context7:/rerun-io/rerun|context7:/websites/streamlit_io|context7:/zarr-developers/zarr-python\" .agents aria_nbv docs"]

[[todo]]
id = "todo-060"
title = "Evaluate richer surface-point modality feature blocks"
description = "Plan optional ablations for surface-point normals, predicted semantic/class encodings, confidence/support stats, and deferred coarse S2 view-direction histograms."
priority = "medium"
status = "todo"
labels = ["features", "surface", "semantics", "ablation", "target"]
issue_ids = ["issue-020", "issue-028", "issue-018"]
loc_min = 100
loc_expected = 260
loc_max = 700
context = ["The alignment discussion identified richer surface-point modalities as possible feature blocks for target-conditioned scoring and Q_H, but not as mandatory first-path inputs.", "Candidate ablations include surface normals, predicted class or semantic encodings, confidence, and support statistics derived from actor-visible observations.", "GT-derived normals, classes, crops, or mesh features may be used only for V0 sanity, audit, or oracle/evaluation diagnostics; they must not become actor-visible V1 inputs.", "A coarse S2 view-direction histogram per point is deferred because it needs stable sparse/SLAM point identity, support, and storage contracts before it can be interpreted safely."]
references = ["repo:docs/contents/thesis/questions.qmd#rq3-representation", "repo:docs/contents/thesis/questions.qmd#rq2-offline-qh", "repo:docs/contents/thesis/roadmap.qmd#roadmap-m4", "repo:docs/typst/thesis/sections/04-method", "repo:.agents/todos.toml#todo-052", "repo:.agents/todos.toml#todo-058", "bib:EFM3D-straub2024"]
implementation_notes = ["Define optional feature-block schemas for actor-visible surface normals, predicted class or semantic encodings, confidence, and support stats before wiring them into model inputs.", "Keep each richer modality behind an ablation flag and retention-profile entry so the first Q_H result can run without them.", "Document provenance for each feature as observed/predicted, GT/audit-only, or derived diagnostic.", "Defer coarse S2 per-point view-direction histograms until sparse/SLAM point identity and support semantics are stable enough for repeatable storage and ablation."]
acceptance = ["The first Q_H baseline can train without richer surface-point modalities.", "A later ablation can enable surface normals and predicted class/semantic encodings without changing core rollout labels or masks.", "Feature provenance distinguishes actor-visible observed/predicted fields from GT-derived audit/oracle-only fields.", "Coarse S2 view-direction histograms are explicitly deferred with a dependency on stable sparse/SLAM point identity and support contracts."]
verification = ["make agents-db AGENTS_ARGS='validate'", "rg -n \"surface normals|semantic encod|class encod|S2|sparse/SLAM|feature-block\" .agents docs aria_nbv"]

[[todo]]
id = "todo-078"
title = "Materialize chunked Q_H training views for rollout shards"
description = "Persist a chunked dense training view derived from rollout factual tables so Q_H dataloaders can batch states without rebuilding q_h_view from whole shards at training time."
priority = "high"
status = "todo"
labels = ["rollouts", "q-learning", "zarr", "performance", "training"]
issue_ids = ["issue-018", "issue-022", "issue-028", "issue-031"]
loc_min = 220
loc_expected = 520
loc_max = 1200
context = ["Schema 1.0-target-rollout-core writes q_h/ as a persisted derived cache and RolloutZarrStoreReader.q_h_view normally reads it; overrides still recompute from factual tables for audits.", "The factual rollout tables should remain the source of truth, while the persisted training view caches hot arrays such as state ids, target ids, candidate ids, position ids, action masks, root-normalized rewards, selected successor ids, selected terminal flags, selected discounts, invalid-reason bitsets, eval-stream lineage, and reward semantics.", "The q_h/ group has configurable state-row chunking recorded in metadata and td_semantics=selected_transition_only, reward_metric=target_root_gain, but throughput, source-row joins, candidate-pose/features needed by the first dataloader, and full LRZ-scale read behavior are not yet benchmarked.", "Zarr guidance favors chunk shapes matched to access patterns and chunks around at least 1 MB uncompressed when practical; Q_H view chunks should therefore remain configurable and measured rather than inherited blindly from smoke-store table defaults.", "Advisor-facing evidence requires transition support, successor-table availability, masks/reasons, root-normalized reward fields, state-relative RRI diagnostics, optional target_eval_crops auditability, and scale reporting before interpreting Q_H results.", "The rollout store should own exact full-shell candidate tables for every rollout state, including the root step, while large root EVL/backbone tensors should remain source-linked rather than duplicated as a normal hot-view field.", "H=1/myopic labels should be derived views over the same materialized factual/q_h machinery used by H>1 traces, so target-conditioned one-step scorer data and multi-step Q_H data share masks, target records, candidate ids, eval stream metadata, and reward fields.", "Current q_h successor links are selected-action-only; unselected candidates can support one-step/ranking supervision but not Bellman backups unless candidate-expanded successors are materialized later."]
references = ["repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:.agents/references/rollout_zarr_q_invalidity_contract.md", "repo:docs/typst/thesis/main.typ", "repo:aria_nbv/tests/rollouts/test_zarr_store.py", "repo:.agents/issues.toml#issue-018", "repo:.agents/issues.toml#issue-028", "repo:.agents/work/rolllout_data/rollout-review-01-gpt55pro.md", "repo:.agents/work/rolllout_data/rollout-review-02-gpt55pro.md", "url:https://zarr.readthedocs.io/en/v3.0.10/user-guide/performance.html", "url:https://zarr.readthedocs.io/en/stable/user-guide/arrays/"]
implementation_notes = ["Keep q_h/ generated from validated factual tables and keep sources/targets/rollouts/steps/candidates canonical.", "Support the H=1 target-conditioned rollout sidecar/profile as the first target-label training view, with H3+ rollout traces added only after target/mask/label diagnostics pass.", "Extend the hot dense arrays only where the first Q_H dataloader needs them: source_row_id joins, candidate pose/root-relative features, root-normalized reward, state-relative RRI diagnostics, and eval-stream ids are likely next, while full meshes, EVL tensors, all-candidate depth maps, and rich diagnostics stay source-linked or audit-only.", "Use chunks aligned to minibatch reads rather than one-row inspection; choose chunk sizes from configured values, target practical multi-row chunk byte sizes, and record the chosen chunk policy in metadata.", "Benchmark at least smoke/small shards for q_h_view derived-on-read versus persisted q_h/training_core view, reporting read throughput, file/chunk count, shard size, and peak memory.", "Make TD semantics explicit in loaders and losses: Bellman/TD loss uses selected expanded transitions and root-normalized rewards only; all-candidate rows may train one-step target-RRI/ranking heads unless successor expansion is later materialized.", "Do not persist full meshes, full EVL tensors, all-candidate depth maps, or rich diagnostics in the hot training view; link to the VIN source store and optional audit blocks instead.", "Keep validation checks that persisted q_h/ candidate ids, masks, rewards, target ids, eval-source metadata, and TD links agree with the factual candidate/step/rollout tables."]
acceptance = ["A rollout shard can expose a persisted dense Q_H training view without losing the ability to derive q_h_view from factual tables.", "An H=1 target-conditioned sidecar/profile can expose the same target/candidate/mask/target-RRI training view needed by the one-step target scorer.", "The persisted view contains source and target linkage sufficient to join actor-visible root tensors from the VIN offline store.", "Candidate ids, selected actions, valid masks, q_train masks, root-normalized rewards, state-relative RRI diagnostics, and successor ids match the factual tables.", "Training semantics distinguish selected-transition TD rows from all-candidate one-step/ranking labels.", "Chunk metadata is inspectable and sized for minibatch-oriented reads, not only row-by-row debugging.", "Lean training shards can omit rich audit diagnostics while still serving the Q_H training view."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_zarr_store.py tests/rollouts", "rg -n \"q_h_view|training view|state_step_row_id|td_next_step_row_id|valid_action_mask|chunks\" aria_nbv/aria_nbv/rollouts aria_nbv/tests/rollouts .agents"]

[[todo]]
id = "todo-080"
title = "Finish target-first RRI theory and advisor alignment"
description = "Keep current theory pages, durable decisions, and the thesis seed aligned around target-first root-normalized gain and scene-RRI diagnostic-only semantics."
priority = "high"
status = "todo"
labels = ["docs", "rri", "advisor", "thesis"]
issue_ids = ["issue-032", "issue-031"]
loc_min = 120
loc_expected = 320
loc_max = 700
context = ["The seminar scene-level RRI section is historical implemented evidence and must not override the current target-first thesis contract.", "Current theory pages should explain the dense/semi-dense root mismatch, homogeneous ASE-depth eval stream, root-normalized target gain, and scene-RRI diagnostic role.", "Advisor-facing thesis seed text should distill the public theory pages rather than duplicating a separate source of truth."]
references = ["repo:docs/typst/seminar_paper/sections/05-oracle-rri.typ", "repo:docs/contents/theory/rri_theory.qmd", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd", "repo:docs/contents/theory/rl_planning.qmd", "repo:docs/typst/thesis/main.typ", "repo:docs/contents/thesis/questions.qmd#rq1-objective"]
implementation_notes = ["Render the touched Quarto pages and thesis seed after edits.", "Do not rewrite the seminar paper as current thesis evidence; only reference it as historical oracle substrate where needed.", "Make docs consistently distinguish target_root_gain from diagnostic target_rri and scene_rri."]
acceptance = ["RRI theory explains the delta from seminar scene RRI to target-first root-normalized gain.", "Candidate/rollout theory pages agree on H>1 canonical traces, selected parent depth, target-eval crop retention, and scene-RRI diagnostics.", "The current thesis questions and method own the alignment decisions.", "The thesis seed contains the high-signal version of the same contract."]
verification = ["cd docs && quarto render contents/theory/rri_theory.qmd contents/theory/candidate_sampling_target_selection.qmd contents/theory/rl_planning.qmd", "make thesis-pdf", "rg -n \"scene RRI|target_root_gain|root-normalized|seminar\" docs/contents/theory docs/contents/thesis/questions.qmd docs/typst/thesis/main.typ"]

[[todo]]
id = "todo-081"
title = "Harden rollout schema 1.0 target-rollout-core"
description = "Finish the incompatible rollout schema bump that makes target-first rewards, hot position_id provenance, selected parent depth, optional target-eval crop payloads, and seed-once lineage explicit."
priority = "high"
status = "todo"
labels = ["rollouts", "zarr", "schema", "q-learning"]
issue_ids = ["issue-032", "issue-018", "issue-028", "issue-031"]
loc_min = 260
loc_expected = 620
loc_max = 1400
context = ["The current code already prunes stale candidate-major q_h bootstrap/scene-RRI arrays and persists target_root_gain as the Q_H TD reward.", "The review's position_id-not-persisted finding is stale for the current checkout because position_id is hot candidate/q_h provenance and mirrored in diagnostics.", "Selected/parent rendered depth is a training-core successor observation; target-eval crop point payloads are sampled/audit retention.", "Schema 1.0 should not be treated as production-final until scene splits, preflight JSON, optional audit group policy, chunking policy, H=1 role/profile, invalidity validation, and stochastic provenance are frozen."]
references = ["repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/aria_nbv/rollouts/trace.py", "repo:aria_nbv/tests/rollouts/test_zarr_store.py", "repo:.agents/references/rollout_zarr_q_invalidity_contract.md"]
implementation_notes = ["Keep schema 1.0 incompatible with no migration readers.", "Keep position_id directly in candidates/ and q_h/ while keeping candidate_diagnostics aligned.", "Keep selected_depth/ as selected-parent history with canonical resolution and renderer lineage.", "Gate target_eval_crops payloads behind retention metadata; scalar target distances/gains remain training core.", "Extend the current seed-once lineage with shard order, per-step or derivable branch seeds, sampled outcomes, RNG-state hash, and selection-policy params hash where needed for replay-grade stochastic traces."]
acceptance = ["A fresh store validates under schema 1.0 and stale schema strings fail validation.", "Actor-selectable candidates have strategy_id, position_id, mixture_id, and sampler_probability in hot training surfaces.", "q_h/td_reward uses target_root_gain and target_rri remains diagnostic.", "Default training-core stores can omit target_eval crop payload rows while audit stores can persist them.", "Lineage records replay-grade seed-once stochastic provenance; no stochastic training trace relies on a not-captured placeholder."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_zarr_store.py tests/rollouts/test_dataset_writer.py -q", "cd aria_nbv && uv run ruff check aria_nbv/aria_nbv/rollouts tests/rollouts"]

[[todo]]
id = "todo-082"
title = "Refactor actor-visible target selector into eligibility interest match stages"
description = "Separate hard actor-visible target eligibility, target-interest ranking/sampling, and deterministic class-compatible 3D IoU GT matching for the diagnostic/deployable V1 selector."
priority = "high"
status = "todo"
labels = ["target", "data", "rri", "contracts"]
issue_ids = ["issue-032", "issue-020"]
loc_min = 220
loc_expected = 520
loc_max = 1200
context = ["Oracle target-task sampling defines the data-generation target crop and first-pass target-first reward distribution; this refactor is scoped to the actor-visible V1 selector.", "Support and visibility should decide V1 labelability and target interest, not rank competing GT objects.", "The actor-visible target sampling default remains deferred until coverage audits compare stratified, robust-softmax, and greedy policies.", "Current tests already assert geometry-only GT match after target eligibility, so remaining work should focus on making the three-stage V1 contract explicit in code/docs, clipping projected area to image bounds, threshold profiles, and coverage audits."]
references = ["repo:aria_nbv/aria_nbv/data_handling/_target_selection.py", "repo:aria_nbv/tests/data_handling/test_target_selection.py", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd"]
implementation_notes = ["Use clipped visible projected area as the single projected-area feasibility field.", "Keep baseline product score as an interest utility, not as a full theory of target selection.", "Make GT match score geometry-only class-compatible IoU with ambiguity gap checks.", "Add selector diagnostics for support/projected-area/class/distance strata and target sampling coverage before choosing the scale default."]
acceptance = ["Tests cover clipped projected visibility, eligibility gates, geometry-only match scores, missing projection fallback/invalidity, and ambiguity.", "Target selection result diagnostics expose enough fields to audit target sampling policy.", "Docs state that target sampling default is deferred until coverage audits."]
verification = ["cd aria_nbv && uv run pytest tests/data_handling/test_target_selection.py -q", "cd aria_nbv && uv run ruff check aria_nbv/aria_nbv/data_handling/_target_selection.py tests/data_handling/test_target_selection.py"]

[[todo]]
id = "todo-083"
title = "Simplify production candidate mixture to realistic three-family profile"
description = "Use forward_local, target_bearing_local, and lateral_target_bypass as the main thesis sampler; keep local_refinement, revisit_backtrack, and upper_bound_free_shell as named ablations."
priority = "high"
status = "todo"
labels = ["candidate-generation", "sampling", "target", "diagnostics"]
issue_ids = ["issue-032", "issue-020", "issue-021"]
loc_min = 180
loc_expected = 420
loc_max = 900
context = ["The five-family default increases interpretability burden before the first production dataset.", "The main thesis question should test whether a simple realistic local target-aware distribution contains target-RRI headroom.", "Low-valid-action roots must be gated/reported rather than silently producing degenerate action sets.", "External review reiterated that the current five-family sampler belongs to ablation, not the first production dataset; the production config should be the three-family profile and the old five-family TOML should not be the default thesis path."]
references = ["repo:aria_nbv/aria_nbv/pose_generation/candidate_mixture.py", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation.py", "repo:aria_nbv/tests/pose_generation/test_candidate_mixture.py", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd"]
implementation_notes = ["Default counts: forward_local 24, target_bearing_local 24, lateral_target_bypass 12.", "Default realistic motion limits: max_step_distance_m 1.0, max_height_delta_m 0.25, max_backward_step_m 0.25, max_yaw_delta_deg 70.", "Add or preserve named ablations for rich five-family and free-shell runs.", "Report valid counts, invalid reasons, selected counts, and target gain by position_id.", "Make production and ablation profile names explicit in configs so review artifacts can tell v1_realistic_3family apart from rich/five-family or upper-bound runs."]
acceptance = ["Default mixture has exactly the three production families and 60 rows.", "A rich five-family ablation remains explicitly constructible and is not the default thesis generation profile.", "Tests cover default counts, motion bounds, free-shell ablation, and position_id provenance."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation/test_candidate_mixture.py tests/pose_generation/test_pose_generation.py -q", "cd aria_nbv && uv run ruff check aria_nbv/aria_nbv/pose_generation tests/pose_generation/test_candidate_mixture.py"]

[[todo]]
id = "todo-084"
title = "Require scene-level split manifests for thesis-scale rollout generation"
description = "Scene_id-to-split manifests must override sample-key splits before shard grouping and be recorded in rollout lineage for thesis-grade target/RRI claims."
priority = "high"
status = "todo"
labels = ["splits", "rollouts", "data-generation", "thesis"]
issue_ids = ["issue-032", "issue-007", "issue-022"]
loc_min = 180
loc_expected = 420
loc_max = 900
context = ["Sample-key splits can leak snippets from the same scene across train and validation/test.", "Target-conditioned rollouts are especially sensitive to scene leakage because target identity, layout, and trajectory neighborhoods can repeat.", "Smoke generation may still use current sample splits if clearly labeled non-thesis evidence.", "Current shard grouping still uses record.split in the visible path; thesis-scale generation needs an explicit scene_id-to-split manifest that overrides sample-key split before grouping and writing."]
references = ["repo:aria_nbv/aria_nbv/rollouts/shard_manifest.py", "repo:aria_nbv/aria_nbv/rollouts/shards.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/tests/rollouts/test_dataset_writer.py"]
implementation_notes = ["Add a scene split manifest input to shard planning.", "Apply scene split override before shard grouping, not only as a writer hash.", "Record source_original_split, thesis_scene_split/effective_split, and split_manifest_hash in sources/rollouts/lineage.", "Reject mixed-split thesis shards during validation.", "Smoke profiles may continue to record sample-key splits if the store role explicitly says non-thesis smoke/audit."]
acceptance = ["Shard planning groups by scene-level effective split.", "Writer records source_original_split, thesis_scene_split/effective_split, and split_manifest_hash.", "Validation rejects mixed split shards and missing scene split metadata for thesis profiles."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_dataset_writer.py tests/rollouts/test_zarr_store.py -q", "rg -n \"scene.*split|split_manifest_hash|mixed split\" aria_nbv/aria_nbv/rollouts aria_nbv/tests/rollouts"]

[[todo]]
id = "todo-085"
title = "Define target sampling coverage audit before choosing scale default"
description = "Measure target support, visibility, distance, class, GT-match, and ambiguity strata before locking the thesis-scale target sampling policy."
priority = "high"
status = "todo"
labels = ["target", "sampling", "diagnostics", "thesis"]
issue_ids = ["issue-032", "issue-020"]
loc_min = 120
loc_expected = 260
loc_max = 600
context = ["The alignment grill deferred the default target sampling policy.", "Stratified sampling is the leading candidate because it can explicitly favor just-enough evidence targets, but the final default should be evidence-backed.", "Greedy top-K and robust softmax remain comparison policies."]
references = ["repo:docs/contents/theory/candidate_sampling_target_selection.qmd", "repo:aria_nbv/aria_nbv/data_handling/_target_selection.py", "repo:aria_nbv/tests/data_handling/test_target_selection.py"]
implementation_notes = ["Add a selector audit report with support bins, raw and clipped projected visible area bins, distance bins, class histogram, GT IoU/gap histograms, unmatched/ambiguous rates, duplicate predicted-to-GT counts, and eligible-but-not-selected counts.", "Use the audit to choose stratified, robust-softmax, or greedy as the thesis-scale default.", "Keep transient symbols and thresholds local to theory/config docs rather than adding glossary noise."]
acceptance = ["A small trusted subset can produce target-selection coverage diagnostics.", "Raw and clipped projected-area fields are distinguishable, and eligibility uses clipped image-overlap area.", "The docs state target sampling policy is deferred pending audit.", "Agents-db contains this deferred decision so it does not get lost."]
verification = ["cd aria_nbv && uv run pytest tests/data_handling/test_target_selection.py -q", "rg -n \"stratified|target sampling|support bin|projected\" docs/contents/theory .agents/todos.toml"]

[[todo]]
id = "todo-086"
title = "Clean stale rollout schema and data-handling docs after schema 1.0"
description = "Update operator-facing data-handling and rollout contract docs so they match schema 1.0 target-rollout-core, sampled audit crop retention, hot position_id, and H>1 canonical traces."
priority = "high"
status = "todo"
labels = ["docs", "rollouts", "zarr", "data"]
issue_ids = ["issue-032", "issue-018"]
loc_min = 120
loc_expected = 300
loc_max = 700
context = ["The data-handling README and generated layout figures have stale references to older schema names and target_eval_crops as required default payloads.", "Operator docs should be normative for the desired current schema, not a mix of historical, TODO, and smoke-store fragments.", "Docs should mention stale review findings that are already fixed so future work does not regress them.", "Candidate-generation docs/comments should describe the target-bearing/local/motion-realism mixture and three-family production profile rather than older look-away or five-family defaults."]
references = ["repo:aria_nbv/aria_nbv/data_handling/README.md", "repo:docs/typst/shared/data-layout-trees.typ", "repo:.agents/references/rollout_zarr_q_invalidity_contract.md", "repo:aria_nbv/aria_nbv/rollouts/zarr_store.py"]
implementation_notes = ["Replace stale schema 0.7/0.9 language with schema 1.0 target-rollout-core.", "Document selected_depth as selected/parent state history at canonical resolution.", "Document target_eval_crops payloads as sampled/audit retention, with scalar distances/gains in training core.", "Document position_id as hot candidate/q_h provenance and candidate_diagnostics as inspection detail.", "Update stale candidate-generation comments/docstrings so three-family production plus explicit ablations is the documented default."]
acceptance = ["README and contract references agree on schema 1.0 semantics.", "No public/operator doc says target_eval_crops are required in every training-core store.", "No public/operator doc implies scene RRI is the Q_H reward."]
verification = ["rg -n \"0\\.5-selected-depth|0\\.7-root-gain-target-crops|0\\.9-candidate-diagnostics|required target_eval_crops|one_step_scene_rri|bootstrap_next_step_row_id\" aria_nbv/aria_nbv/data_handling/README.md docs .agents/references", "make qmd-frontmatter-check"]

[[todo]]
id = "todo-087"
title = "Fix rollout hard-diagnostic invalidity consistency"
description = "Fresh schema-1.0 rollout probes fail validation because hard diagnostic masks can lack their corresponding invalidity bits."
priority = "high"
status = "todo"
labels = ["rollouts", "invalidity", "zarr", "blocker"]
issue_ids = ["issue-032", "issue-021", "issue-018"]
loc_min = 80
loc_expected = 220
loc_max = 520
context = ["On 2026-05-20, fresh target-first three-family rollout probes wrote schema 1.0 stores but post-write validation failed with: path_collision_mask rows must include PATH_SEGMENT_COLLISION invalidity bits.", "The failure reproduced even when collect_rule_masks was disabled in the writer-level config because candidate diagnostics still carried path_collision_mask semantics.", "A structural probe only validated after disabling path collision, so normal collision-aware rollout generation is currently blocked.", "The likely semantic mismatch is that _candidate_invalid_reasons records first-failing cumulative rule reasons, while candidate_diagnostics/path_collision_mask can mark every path-colliding row independently. A row that fails clearance first can still have path_collision_mask=true without PATH_SEGMENT_COLLISION in the bitset.", "External review correctly broadened this from a path-collision-only bug to an all-hard-diagnostic contract: motion-realism rejection, invalid free-space margin, mesh-clearance violation, depth/render no-hit, target-support failure, and target/source invalidity need the same bitset/primary-reason consistency when they are hard infeasibility signals."]
references = ["repo:.agents/work/rollout-scale-readiness/01-path-collision-invalidity-plan.md", "repo:aria_nbv/aria_nbv/rollouts/trace.py#_candidate_invalid_reasons", "repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:aria_nbv/tests/rollouts/test_zarr_store.py", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation_rules.py"]
implementation_notes = ["Preferred scale contract: invalid_reason_bitset stores all applicable hard invalidity reasons, primary_invalid_reason stores one deterministic fixed-priority reason, and soft diagnostics are explicitly named audit-only.", "Classify every candidate diagnostic as hard_invalidity or audit_only before tightening validation.", "Use a stable primary priority: target/source invalid, pose/frame invalid, extent/free-space, path collision, mesh clearance, motion realism, depth/render no-hit, target crop/support invalid, oracle scoring failed.", "Add regressions where candidates fail multiple hard diagnostics, including clearance plus path collision and motion realism plus free-space; assert bitset contains all hard bits and primary reason is deterministic.", "Regenerate a small collision-aware schema-1.0 rollout probe and require validate_rollout_zarr_store to pass."]
acceptance = ["Collision-aware fresh rollout generation writes a schema 1.0 store that validates without disabling path collision.", "Rows that have hard diagnostic masks set include the matching invalidity bits, or the validator/docs explicitly treat those diagnostics as audit-only.", "The validator covers path collision, motion realism, free-space, mesh clearance, depth/render no-hit, and target-support invalidity where those diagnostics exist.", "Tests cover multi-reason invalid candidates and preserve the invalidity-as-hard-mask contract."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_zarr_store.py tests/rollouts/test_dataset_writer.py tests/pose_generation/test_pose_generation.py -q", "cd aria_nbv && uv run nbv-build-rollouts --config-path ../.configs/build_rollouts_v1_realistic.toml --dry-run"]

[[todo]]
id = "todo-088"
title = "Make three-family sampler pass per-family validity preflight"
description = "The target-first three-family sampler must produce valid target-aware actions before broad rollout generation."
priority = "high"
status = "todo"
labels = ["candidate-generation", "sampling", "rollouts", "preflight"]
issue_ids = ["issue-032", "issue-020", "issue-021"]
loc_min = 120
loc_expected = 320
loc_max = 760
context = ["The validating 2026-05-20 structural probe used schema 1.0 with path collision disabled and produced only 30 valid candidates out of 600, exactly 3 valid candidates per step.", "All valid and selected candidates in that probe came from forward_local; target_bearing_local and lateral_target_bypass produced zero valid actions.", "The current low-valid-root gate of min_valid_root_candidates=3 is too weak for scale because a root can barely pass while the target-aware families are completely dead.", "This is a thesis blocker: the main sampler should test target-conditioned finite-candidate NBV, not degenerate into forward-only local motion.", "External review proposed first production gates: num_valid_candidates >= max(12, ceil(0.25 * N_q)), at least one valid non-forward target-aware action for smoke, and at least three for production."]
references = ["repo:.agents/work/rollout-scale-readiness/02-three-family-sampler-preflight-plan.md", "repo:aria_nbv/aria_nbv/pose_generation/candidate_mixture.py", "repo:aria_nbv/aria_nbv/pose_generation/candidate_generation.py", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/aria_nbv/rollouts/info_cli.py", "repo:aria_nbv/tests/pose_generation/test_candidate_mixture.py", "repo:docs/contents/theory/candidate_sampling_target_selection.qmd"]
implementation_notes = ["Add a rollout/candidate preflight summary that reports valid_count, invalid_reason counts, selected_count, target_root_gain distribution, and selected policy by position_id and mixture_id.", "Raise root gating from a single minimum valid count to a profile-level gate that can require num_valid_candidates >= max(12, ceil(0.25 * N_q)) and target-aware non-forward family contribution where configured.", "Run a 25-50 root audit across at least five scenes before retuning; include valid, low-valid, forward-only, target-aware-valid, and flat-gain cases.", "Retune radius, clearance, motion-realism, target-bearing, and lateral-bypass settings using the multi-scene subset before scale.", "Keep upper_bound_free_shell and rich_local_five_family as explicit ablations for diagnosing whether failures come from realism constraints or target-aware priors."]
acceptance = ["A current-schema preflight report shows valid target_bearing_local and lateral_target_bypass actions on a representative subset, or blocks roots with explicit reasons.", "Production preflight enforces at least max(12, ceil(0.25 * N_q)) valid actions per trainable state and at least three valid non-forward target-aware actions unless explicitly running an ablation.", "Low-valid roots are skipped or marked non-training before they enter Q_H training views.", "Per-position validity and selected-action histograms are available from the rollout inspection path.", "The default thesis generation config matches the three-family contract rather than the older five-family TOML profile."]
verification = ["cd aria_nbv && uv run pytest tests/pose_generation/test_candidate_mixture.py tests/rollouts/test_dataset_writer.py tests/rollouts/test_inspection.py -q", "cd aria_nbv && uv run nbv-rollouts-info --store <fresh_schema_1_store> --validate --stats"]

[[todo]]
id = "todo-089"
title = "Add rollout generation preflight gate before broader offline samples"
description = "Create a current-schema preflight report that blocks scale generation on invalid stores, stale schemas, degenerate samplers, flat rewards, or excessive file counts."
priority = "high"
status = "todo"
labels = ["rollouts", "preflight", "data-generation", "thesis"]
issue_ids = ["issue-032", "issue-018", "issue-022", "issue-028"]
loc_min = 120
loc_expected = 360
loc_max = 900
context = ["Fresh 2026-05-20 probes showed one current-schema validation blocker, one degenerate target-aware sampler, and selected target_root_gain values around numerical noise on the first inspected scene.", "Local default rollout stores are stale: rollouts_v1_realistic.zarr validates as schema 0.8-global-target-rows and rollouts_v1_smoke.zarr validates as schema 0.6-rollout-core under the current reader.", "Before LRZ or broader local generation, the project needs a single go/no-go report that validates schema 1.0 stores and summarizes coverage, rewards, valid actions, target-aware family contribution, selected-depth retention, optional crop payloads, chunk/file counts, and stale-store status.", "External review recommends extending nbv-rollouts-info with --preflight --profile production --json, unless source/render/scorer checks require a separate command."]
references = ["repo:.agents/work/rollout-scale-readiness/03-rollout-generation-preflight-plan.md", "repo:aria_nbv/aria_nbv/rollouts/info_cli.py", "repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:aria_nbv/aria_nbv/rollouts/shards.py", "repo:.configs/build_rollouts_v1_realistic.toml", "repo:Makefile", "repo:.agents/issues.toml#issue-032"]
implementation_notes = ["Extend nbv-rollouts-info or add a narrow preflight command that exits nonzero on unsupported schema, validation errors, stale default store, missing scene split manifest for thesis profiles, missing stochastic provenance for stochastic training traces, target_eval_crops unexpected retention, low valid-action counts, missing target-aware family contribution, flat reward signal, or excessive file/chunk counts.", "Report stable JSON sections: schema, validation, lineage, coverage, validity, rewards, retention, storage, and go_no_go.", "Report source rows, scenes, snippets, targets, rollouts, steps, candidates, q_h states, selected-depth rows, optional crop rows, file/chunk counts, candidate group size, selected target gain stats, target-vs-scene diagnostic comparison, and per-position valid/selected counts.", "Use first production reward-signal gate as a reviewable default: fail if median absolute valid target_root_gain < 1e-4 and p90 target_root_gain < 1e-3, while smoke/audit only warn.", "Keep stale stores as local artifacts only; do not add compatibility readers for old schemas.", "Wire the preflight into the human runbook before any multi-shard generation command."]
acceptance = ["A fresh small schema-1.0 rollout store can produce a machine-readable preflight report.", "The preflight fails on the current stale rollouts_v1_realistic.zarr and rollouts_v1_smoke.zarr defaults.", "The preflight fails on a degenerate three-family run where only forward_local is valid unless explicitly running a forward-only ablation.", "Production preflight fails on missing scene split metadata, missing or incomplete replay-grade stochastic provenance for stochastic training traces, and flat target-root-gain signal under the reviewed thresholds.", "The report includes enough reward and validity diagnostics to decide whether scale generation is scientifically useful."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_inspection.py tests/rollouts/test_zarr_store.py -q", "cd aria_nbv && uv run nbv-rollouts-info <fresh_schema_1_store> --preflight --profile production --json"]

[[todo]]
id = "todo-090"
title = "Expose rollout readiness plan pack on reviewed branch"
description = "Make the local rollout-scale-readiness plan pack, DB records, and inspection code available on the exact branch/ref used for external review."
priority = "high"
status = "todo"
labels = ["review", "git", "agents-db", "rollouts"]
issue_ids = ["issue-032", "issue-011"]
loc_min = 20
loc_expected = 60
loc_max = 140
context = ["The latest external review could only access PR #14 at 74b7be2190f147da490875c4c3fd7d2f4a34dbe2 and reported that .agents/work/rollout-scale-readiness/*, issue-032, todo-087 through todo-089, refactor-021, and aria_nbv/aria_nbv/rollouts/inspection.py were missing.", "The current local checkout contains those artifacts but is ahead of origin/codex/full-rri-rollout-worktree, so the review target and local working state diverged.", "Future review prompts must include an exact pushed commit SHA/ref that contains the plan pack and linked agents-db records."]
references = ["repo:.agents/work/rollout-scale-readiness/README.md", "repo:.agents/issues.toml#issue-032", "repo:.agents/todos.toml#todo-087", "repo:.agents/todos.toml#todo-088", "repo:.agents/todos.toml#todo-089", "repo:.agents/refactors.toml#refactor-021", "repo:aria_nbv/aria_nbv/rollouts/inspection.py"]
implementation_notes = ["Push the current branch or otherwise expose an immutable commit SHA/ref before requesting another external review.", "Include git log and branch/ref metadata in the review prompt so reviewers can verify that the plan-pack and DB surfaces are fetchable.", "Do not treat review acceptance/rejection of missing files as substantive code review until the target mismatch is resolved."]
acceptance = ["The reviewed remote ref contains .agents/work/rollout-scale-readiness/*, issue-032, todo-087 through todo-089, refactor-021, and aria_nbv/aria_nbv/rollouts/inspection.py.", "The review prompt names the exact commit SHA/ref and distinguishes stale visible-branch findings from current-checkout findings.", "A fresh reviewer can fetch the branch/ref and audit the same artifacts without relying on chat history."]
verification = ["git branch -vv", "git log --oneline --max-count=8", "git ls-tree -r <reviewed-ref> -- .agents/work/rollout-scale-readiness aria_nbv/aria_nbv/rollouts/inspection.py"]

[[todo]]
id = "todo-091"
title = "Add explicit H=1 target-label rollout profile"
description = "Expose H=1 all-candidate target labels as a first-class rollout sidecar profile derived from the same materialized state machinery as H>1 traces."
priority = "high"
status = "todo"
labels = ["rollouts", "target", "q-learning", "schema", "profile"]
issue_ids = ["issue-032", "issue-018", "issue-028"]
loc_min = 120
loc_expected = 300
loc_max = 700
context = ["The current thesis contract says H>1 rollout traces are canonical and H=1/myopic labels are derived views over materialized states, but external review found no first-class visible writer profile for H=1 target-label stores.", "A scale-safe H=1 gate should label all valid candidates for one-step target_root_gain while treating the selected action as audit-only and disabling TD confusion.", "The H=1 profile must be trivially extendable to H>1 because selected/parent rendered depth from materialized states becomes actor-visible successor history."]
references = ["repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/aria_nbv/rollouts/zarr_store.py", "repo:aria_nbv/aria_nbv/rollouts/info_cli.py", "repo:aria_nbv/tests/rollouts/test_dataset_writer.py", "repo:aria_nbv/tests/rollouts/test_zarr_store.py", "repo:docs/contents/theory/rl_planning.qmd", "repo:.agents/todos.toml#todo-078"]
implementation_notes = ["Add or document store_role='target_label_sidecar', rollout_profile='h1_target_labels', td_semantics='none' or 'selected_transition_only_but_unused', and candidate_label_semantics='all_valid_one_step_target_root_gain'.", "Use the existing rollout writer and schema machinery; do not create a separate target-label writer unless the shared path proves unworkable.", "Keep selected/parent depth semantics consistent with H>1: it is successor state history after an action is materialized, not actor-visible all-candidate evidence before selection.", "Preflight should reject H=1 stores that look like H>1 TD training traces or vice versa."]
acceptance = ["An H=1 target-label profile writes all valid candidate target_root_gain labels with masks and invalid reasons.", "Selected action and selected-depth history are present only with clearly labeled audit/successor semantics.", "TD training code cannot confuse H=1 myopic labels with H>1 selected-transition Bellman data.", "The H=1 profile can be extended to H>1 without changing candidate ids, masks, target ids, reward fields, or selected-depth lineage."]
verification = ["cd aria_nbv && uv run pytest tests/rollouts/test_dataset_writer.py tests/rollouts/test_zarr_store.py tests/rollouts/test_inspection.py -q", "rg -n \"target_label_sidecar|h1_target_labels|candidate_label_semantics|td_semantics\" aria_nbv .configs docs .agents"]

[[todo]]
id = "todo-092"
title = "Wire oracle target-task sampler into rollout generation"
description = "Make rollout/data-generation configs consume OracleTargetTaskSampler as the target-task source instead of the legacy actor-visible selector."
priority = "high"
status = "todo"
labels = ["rollouts", "target", "data-generation", "oracle", "contracts"]
issue_ids = ["issue-020", "issue-018", "issue-032"]
loc_min = 120
loc_expected = 320
loc_max = 800
context = ["Automatic target selection for rollout labels is oracle/data-generation work, not actor-visible repo truth.", "G005 added OracleTargetTaskSamplerConfig, OracleTargetTaskSampler, OracleTargetTaskRow, threshold-sweep diagnostics, public exports, and focused tests, but RolloutDatasetWriter still instantiates ActorVisibleTargetSelector.", "The first rollout/H=1 target-label profile should use identity-valid oracle target tasks, persist descriptor/audit fields, and leave actor-visible OBS-SEL/PRED-Q as a separate diagnostic or future deployable-input path."]
references = ["repo:aria_nbv/aria_nbv/data_handling/_target_selection.py", "repo:aria_nbv/tests/data_handling/test_target_selection.py", "repo:docs/typst/thesis/figures/oracle_target_task_sampler_contract.mmd", "repo:aria_nbv/aria_nbv/rollouts/dataset_writer.py", "repo:aria_nbv/tests/rollouts/test_dataset_writer.py", "repo:.agents/issues.toml#issue-020", "repo:.agents/todos.toml#todo-091"]
implementation_notes = ["Add rollout writer config fields or a target_source enum that selects OracleTargetTaskSampler for oracle/data-generation profiles while preserving ActorVisibleTargetSelector for V1 diagnostics.", "Persist oracle target-task identity status, identity IoU/gap, selected rank/probability, projected-area audit, support audit, class/id metadata, and headroom placeholders where the rollout schema expects target records.", "Ensure unsupported, ambiguous, or invalid-geometry target tasks are masks/reasons, not low-RRI samples.", "Keep OBS-SEL/PRED-Q/GT-EVAL configs, app diagnostics, and Streamlit selectors visibly separate from oracle target-task sampling."]
acceptance = ["A rollout writer smoke config can sample capped identity-valid oracle target tasks from GT OBBs.", "The legacy V1 selector remains available only for diagnostic/deployable actor-input profiles.", "Target records in rollout/H=1 outputs preserve oracle identity and descriptor audit fields needed for preflight and Rerun/Streamlit inspection.", "Ambiguous duplicate GT targets are rejected from the sampled task pool with explicit status or invalidity metadata."]
verification = ["cd aria_nbv && uv run pytest tests/data_handling/test_target_selection.py tests/rollouts/test_dataset_writer.py -q", "cd aria_nbv && uv run ruff check aria_nbv/aria_nbv/data_handling/_target_selection.py aria_nbv/aria_nbv/rollouts/dataset_writer.py aria_nbv/tests/data_handling/test_target_selection.py aria_nbv/tests/rollouts/test_dataset_writer.py", "rg -n \"OracleTargetTaskSampler|ActorVisibleTargetSelector|target_source|identity_ambiguity_gap\" aria_nbv/aria_nbv/rollouts aria_nbv/tests/rollouts .configs .agents"]

[[todo]]
id = "todo-095"
title = "Deferred repository-wide docstring coverage checker"
description = "Evaluate a repository-wide public method and field docstring coverage gate after the WP6 scaffold removal, without rebuilding broad generated docstring inventories."
priority = "low"
status = "todo"
labels = ["docs", "python", "deferred", "quality"]
issue_ids = ["issue-012"]
loc_min = 80
loc_expected = 220
loc_max = 500
context = ["WP6 intentionally retains the python-docstrings workflow and current Quartodoc validation while removing the bulk class-docstring context bundle.", "A broad checker needs an explicit public-surface policy and false-positive budget before it becomes a repository gate."]
references = ["repo:.agents/skills/python-docstrings/SKILL.md", "repo:scripts/quarto_generate_api_docs.py", "repo:docs/AGENTS.md"]
implementation_notes = ["Define public modules methods fields generated APIs and exemptions before implementing a checker.", "Prefer source-level diagnostics over generated aggregate documentation.", "Do not add this gate to CI until a measured baseline and migration plan exist."]
acceptance = ["A reviewed policy defines the checked public surface and exclusions.", "A dry-run report measures coverage and false positives without changing package behavior.", "Any future gate has focused tests and does not recreate retired broad context artifacts."]
verification = ["make api-docs-self-test", "make scaffold-audit", "make check-agent-memory"]

[[todo]]
id = "todo-096"
title = "Deferred package README ownership audit"
description = "Audit package READMEs for durable human orientation and remove symbol inventories, generated snapshots, refactor status, or agent routing in a later bounded pass."
priority = "low"
status = "todo"
labels = ["docs", "readme", "deferred", "ownership"]
issue_ids = ["issue-003", "issue-012"]
loc_min = 80
loc_expected = 260
loc_max = 700
context = ["WP6 enforces the README policy prospectively but does not broaden into a package-by-package narrative rewrite.", "Package guidance, typed APIs, tests, and docstrings remain the executable owners; READMEs should orient human maintainers without becoming generated indexes or agent routers."]
references = ["repo:.agents/references/source_order.md", "repo:aria_nbv/AGENTS.md", "repo:docs/AGENTS.md"]
implementation_notes = ["Inventory package READMEs and classify each section as durable orientation, executable contract duplication, generated inventory, status report, or agent routing.", "Keep edits package-scoped and verify the nearest package guidance and tests."]
acceptance = ["Every retained package README has a clear human audience and durable orientation role.", "No retained README contains generated symbol matrices, refactor progress, or agent routing.", "Executable contracts remain in code tests docstrings and nearest package guidance."]
verification = ["rg -n \"generated|inventory|agent routing|refactor status\" aria_nbv/aria_nbv -g 'README.md'", "make check-agent-memory"]
