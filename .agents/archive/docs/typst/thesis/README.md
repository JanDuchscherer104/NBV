# Archived Thesis Typst Sources

`docs/typst/thesis/main.typ` is now the active master's-thesis seed.

The archived proposal and advisor-distillation sources in this directory are
provenance only. Substantive thesis material from them was merged into
`main.typ` and its section files; unresolved, stale, future, and
validation-needed material should remain in the active thesis seed as
`dashy-todo` markers instead of being restored as competing active sources.
